# Bridge4PS Decision Log

Durable decisions for the Bridge4PS Documentation Engineer repository. This log records this project's own decisions. It is separate from the Project-Pipeline decision log (`capcutfor1month-oss/project-Pipline` → `docs/DECISIONS.md`), which governs the pipeline itself.

## PROJ-001 — Pipeline adoption

**Decision:** Bridge4PS adopts the Universal Agentic Project Pipeline from `capcutfor1month-oss/project-Pipline` at source commit `e755ece0caf62f8e7df1a6c168727049c362cd4c` to govern repository-development workflow only.

**Reason:** Give this repository a repeatable, founder-friendly process for clarifying, specifying, implementing, testing, auditing, and releasing changes without adopting any pipeline concept as Bridge4PS product or domain content.

**Status:** Active

---

## PROJ-002 — Inherited pipeline decision: DEC-014 — Collaborative founder decision rule

**Decision (inherited from Project-Pipeline):** ChatGPT is the founder's brainstorming, explanation, and strategy partner. The founder and ChatGPT discuss evidence, options, risks, and recommendations before deciding the next product move. Claude implements approved work, and Codex audits independently when the approved workflow reaches the audit step. When the founder shares agent output, the default response is explanation and discussion — not automatic generation of another agent prompt.

**Applies to Bridge4PS as:** every agent working in this repository follows this rule unchanged.

**Status:** Active

---

## PROJ-003 — Inherited pipeline decision: DEC-017 — Target-domain / development-governance separation

**Decision (inherited from Project-Pipeline):** Two authorities apply together and must never be merged. Bridge4PS's own canonical documents (`docs/PRODUCT.md`, `docs/ARCHITECTURE.md`) govern what the product is. Project-Pipeline governs only how repository changes are clarified, approved, specified, investigated, implemented, tested, audited, and released. Being given, pointed to, or reading the Project-Pipeline repository never authorizes adopting its concepts as Bridge4PS domain content.

**Applies to Bridge4PS as:** the binding authority-boundary rule recorded in `docs/INDEX.md`.

**Status:** Active

---

## PROJ-004 — Baseline import deferred

**Decision:** The canonical raw baseline `B4PS-TUT-main.zip` is designated as the source of the Bridge4PS implementation but is not imported during pipeline bootstrap.

**Reason:** Bootstrap scope is limited to adopting the pipeline. Importing and verifying the baseline is a separately approved task, recorded as the exact next action in `docs/CURRENT.md`.

**Status:** Superseded by PROJ-005.

---

## PROJ-005 — Baseline imported; canonical ZIP is fidelity authority, GitHub materialization is a 50/58 publication view

**Decision:** `B4PS-TUT-main.zip` (SHA-256 `00f5e41f475b8205535481da11f73c4e4f0bd614e0d3b1efff938e921b9eb6ee`, 58 files, embedded source commit `85af51a123e237c14f61f5bb43094292db664561`) was imported and is the founder-approved final publication model: the unmodified archive is committed at `baseline/source-artifacts/B4PS-TUT-main.zip` as the authority for exact raw-baseline fidelity. Its extraction is committed at `baseline/B4PS-TUT-main/` with its original `.gitattributes` unmodified, containing 50 of the 58 source files byte-for-byte. The remaining 8 files are pre-existing Git LFS pointer-text files whose real binaries were never available; GitHub's server-side Git LFS enforcement (`GH008`) rejects any push referencing those objects regardless of `.gitattributes` content (confirmed by two prior failed attempts, one editing `.gitattributes` and one additionally squashing history — both still rejected). The founder approved omitting those 8 paths from the extracted GitHub tree only, while keeping their exact pointer representations preserved inside the canonical ZIP. The full omission manifest (path, LFS OID, declared size) is recorded in `docs/CURRENT.md`.

**Reason:** The canonical ZIP alone cannot be the only published artifact without also giving GitHub a materialized, browsable tree; but GitHub's LFS enforcement makes publishing pointer-text files impossible without either fabricating binaries (forbidden) or altering their content (forbidden, would destroy evidence they originated as LFS pointers). Splitting the two artifacts — ZIP as immutable fidelity authority, extracted tree as a 50/58 publication view with a documented, non-fabricated gap — satisfies "preserve raw-baseline defects and limitations as baseline evidence" without either fabricating content or leaving the import unpublishable.

**Status:** Active. This decision does not authorize Repository Hardening Batch 1, resolving the 8 missing binaries, or any other baseline content change.

---

## PROJ-006 — Repository Hardening Batch 1 completed

**Decision:** Repository Hardening Batch 1 was implemented directly on the imported baseline tooling at `baseline/B4PS-TUT-main/.b4ps-tools/`, scoped to reproducibility, path/filesystem safety, failure behavior, configuration/state, tests, and operator usability — not to Safe PPT Engine feature work. Concrete fixes: (1) removed a hardcoded personal absolute path (`/Users/aamir/Documents/B4PS TUT`) from `scripts/capture_and_rename.sh`, replaced with a run-time-resolved project root and a `TMPDIR`-honoring temp file path; (2) added `requirements.txt` and `requirements-dev.txt` declaring the previously-undeclared `Pillow`/`numpy`/`opencv-python-headless`/`pytest` dependencies (already named in prose in the tool's own README but not machine-installable); (3) added `config.MissingDeckSourceError` and `config.require_pptx()`, wired into `DeckReader.__init__`, `deck.backup()`, and the CLI's top-level dispatch in `b4ps.py`, so a missing, empty, or corrupt deck `.pptx` now fails with one clear, actionable message and exit code 1 instead of a raw `FileNotFoundError`/`BadZipFile` traceback; (4) added 16 automated tests under `.b4ps-tools/tests/` (`pytest`) covering the fixed failure paths with small synthetic fixtures, plus static regression checks against the hardcoded-path defect and a CLI smoke test proving the tool runs clean from a fresh checkout; (5) added a Setup section, a Running-tests section, and a truthful known-limitation note to `.b4ps-tools/README.md`.

**Reason:** This repository's own baseline import (PROJ-005) made the missing-deck-source failure path immediately, concretely reproducible — running any deck-touching command in this checkout hits exactly the defect fixed in (3). Hardening this foundation before Safe PPT Engine work begins prevents that engine from being built against tooling that crashes opaquely on the exact condition this repository is currently in, and removes a machine-specific path that would silently break for any contributor other than the original author.

**What was deliberately not done:** the 8 missing LFS binaries were not obtained, fabricated, or otherwise resolved (out of scope, unchanged limitation); `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, `lib/geometry.py` were inspected for the same defect classes but not found to need a fix in this pass, and were not otherwise modified; no Safe PPT Engine feature, semantic-annotation, or workflow-intelligence work was started; the canonical ZIP `baseline/source-artifacts/B4PS-TUT-main.zip` was not touched.

**Status:** Active.

---

## PROJ-007 — Safe PPT Engine Build 1

**Decision:** Built the first Safe PPT Engine layer at `baseline/B4PS-TUT-main/.b4ps-tools/lib/ppt_engine.py`, using `python-pptx` (added to `requirements.txt`) rather than extending the baseline's existing hand-rolled XML-surgery approach in `lib/deck.py` — the two serve different purposes (`deck.py` is a purpose-built, high-performance surgical writer scoped to the two known production decks and their known shape conventions; the engine needs to safely load and describe *any* explicit `.pptx` path generically, which `python-pptx` already does safely and correctly). The engine provides: `load_deck` (typed, never-fallback loading), `inspect_deck` (deterministic structural JSON with no semantic interpretation), `create_working_copy` (mutation always staged off both the original and the final output path), and `set_shape_text` (the one controlled mutation primitive built this pass — explicit `(slide_index, shape_index)` targeting only, validated by reopening the saved result before it is placed at the caller's output path). Exposed via `engine-inspect` and `engine-set-text` CLI commands. Verified with 21 new tests (37 total) against synthetic fixtures built programmatically with `python-pptx`/`Pillow` — the 8 unavailable Git LFS assets were not touched.

**Reason:** The approved build order requires a safe, deterministic mechanical foundation (read/inspect/copy/mutate/save/validate/preserve) before any Documentation Intelligence or semantic capability can be layered on top. Establishing the pattern with one real, tested mutation primitive (text update) proves the safety invariants — source never modified, output path never collides, invalid input fails before mutation, output is reopened and content-verified after save — without yet committing to which richer primitives (move/resize/image-replace) Documentation Intelligence will actually need.

**What was deliberately not done:** no semantic shape identification, fuzzy matching, screenshot intelligence, or Documentation Intelligence; no additional mutation primitives beyond `set_shape_text`; no changes to `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py` (existing anchor/matching workflow is untouched and independent of the new engine layer); the 8 missing LFS binaries were not obtained or fabricated; the canonical ZIP was not touched.

**Status:** Active.

---

## PROJ-008 — Safe PPT Engine Build 2: visual mutation primitives

**Decision:** Added four visual mutation primitives to `lib/ppt_engine.py`: `move_shape` (left/top, EMU only), `resize_shape` (width/height, EMU only), `set_shape_geometry` (left+top+width+height as one atomic operation, for future red-box relocation), and `replace_picture`. All coordinates/dimensions are EMU — python-pptx's and OOXML's own native integer unit; no other unit is accepted, and callers are responsible for converting from inches/points/pixels if needed. `move_shape` and `resize_shape` are each verified not to touch the other's attributes (a shared regression test proves resize does not move the shape, matching PowerPoint's own semantics — no repositioning side effect was observed or needed handling). `set_shape_geometry` validates all four values *before* any working copy is staged, so an invalid value cannot leave a partially-applied geometry anywhere, in memory or on disk.

`replace_picture` rejects a non-picture target shape and a missing/empty/unreadable replacement image before staging anything. Its default (and only, for this build) behavior preserves the existing shape's left/top/width/height frame exactly, regardless of the replacement image's own pixel dimensions or aspect ratio — chosen as the safest deterministic behavior, since it requires no cropping/scaling decision and matches how PowerPoint's own `blipFill` model already renders an image into a fixed frame. Implementation touches OOXML directly in exactly one place — repointing the picture's `p:blipFill/a:blip/@r:embed` relationship to a newly-added image part via python-pptx's own `SlidePart.get_or_add_image_part()` plus the `CT_Blip.rEmbed` attribute descriptor (both existing, documented python-pptx internals, not raw string/XML surgery) — isolated entirely inside `replace_picture`; no other code touches OOXML for image handling. Any existing crop data (`a:srcRect`) is preserved by construction, since it is never touched.

Build 1's final-publish step (a bare `shutil.copy2(staged, output_path)`) was not fully atomic — an interruption mid-copy could leave a partially-written file at `output_path`. Hardened this pass with `_publish_atomically()` (copy to a same-directory temp file, then `os.replace()`, which is atomic on the same filesystem) and retrofitted it onto `set_shape_text` as well as all four new primitives, per the explicit instruction to harden Build 1's atomicity within this build if it did not already guarantee it.

**Reason:** These are the specific geometry/image operations future red-box relocation and screenshot-swap work will call. Building them as small, independently-tested, deterministic primitives — reusing Build 1's staged-copy/validate/publish pattern rather than inventing a parallel one — keeps the mechanical foundation trustworthy before any semantic targeting is layered on top of it.

**What was deliberately not done:** no semantic shape identification, fuzzy matching, screenshot intelligence, automatic affected-slide detection, Editorial Memory, or Documentation Intelligence; no rotation/flip or other geometry attributes beyond left/top/width/height; no shape creation/deletion; no automatic aspect-ratio cropping or aesthetic image adjustment; no changes to `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the 8 missing LFS binaries (including both production decks) were not obtained, fabricated, or needed by any test; the canonical ZIP was not touched.

**Status:** Active.

---

## PROJ-009 — Safe PPT Engine Audit Repair 1 (Codex findings F-01–F-08)

**Decision:** An independent Codex audit of Safe PPT Engine Builds 1–2 found 8 defects. All 8 are fixed in `lib/ppt_engine.py`/`b4ps.py`, per-finding:

- **F-01 (critical, source aliasing):** `_assert_safe_output_path` now compares `os.path.realpath` of both paths (resolves symlinks even when the output's final path component does not yet exist) and, when both paths currently exist, additionally checks `os.path.samefile` (catches hardlinks/bind-mount aliasing realpath alone can miss). Applies uniformly to `set_shape_text`, `move_shape`, `resize_shape`, `set_shape_geometry`, and `replace_picture` (all route through this one function). The pre-existing before/after source-hash check is kept as defense-in-depth, not the primary protection.
- **F-02 (high, no-overwrite publication race):** `_publish_atomically` now branches on `overwrite`. `overwrite=False` opens the destination itself with `os.O_CREAT | os.O_EXCL`, which atomically fails if anything exists there at that instant — no separate exists-check followed by a write. `overwrite=True` keeps the prior same-directory-temp-file + `os.replace` atomic-rename approach.
- **F-03 (high, raw filesystem errors leak):** every mutation primitive now calls `load_deck(input_path)` (the typed validation boundary) before `file_sha256`/staging, via a shared `_validate_and_hash_source` helper. A missing input now raises `DeckSourceError`, never a bare `FileNotFoundError`. Verified at both the engine API and CLI boundary (all 5 mutation CLI commands exit 1 with a clean `error:` message, no traceback, for missing/empty/corrupt input).
- **F-04 (medium, stale media left in package):** `replace_picture` captures the old relationship id before repointing the blip, then calls `_drop_relationship_if_unreferenced`, which scans the slide's shape tree for any remaining `a:blip` still using that id and only then drops the relationship. python-pptx's own package writer serializes only parts still reachable via some relationship, so an unreferenced image part is automatically absent from the next save with no separate manual part-deletion step. Verified against actual ZIP package contents (not just API-level state): single-picture replacement removes the old media file; two pictures on one slide sharing the same image (python-pptx assigns them the same relationship id automatically) — replacing one leaves the other's image completely intact.
- **F-05 (medium, staging/failure cleanup gaps):** `create_working_copy` now wraps `shutil.copy2` in try/except, removing the allocated temp path and raising `DeckSourceError` on any copy failure. Failure injection tests cover all four transaction points (staging copy, save, validation, publication) and confirm: source untouched, no partial/incorrect final destination, no leaked `b4ps_engine_*`/`.b4ps_publish_*` temp file, typed error where applicable.
- **F-06 (medium, missing verification records):** this decision entry plus `openspec/changes/safe-ppt-engine/verification-report.md`, recording Builds 1–2 and this repair's classification, rationale, applicable profiles, commands, results, limitations, and — critically — that Builds 1–2 were marked complete *before* independent audit and *failed* that audit; re-audit of this repair is still required and has not happened.
- **F-07 (low, non-picture target validated after staging):** `replace_picture` now calls `_preflight_resolve_shape` (a throwaway `load_deck` against the original input) and checks `shape_type` before `create_working_copy` is ever called, so an invalid target creates no temp file. The same preflight-resolve pattern was applied uniformly to `set_shape_text` and the shared geometry-mutation path too, as a direct, minimal implementation of the cross-cutting contract's stated order ("validate source + target + path safety" before "stage privately") rather than a change scoped to F-07 alone.
- **F-08 (low, canonical inspection includes absolute path):** `inspect_deck`'s return value no longer includes a `path` key, so two byte-identical `.pptx` files at different filesystem paths now produce an equal result. A new `describe_deck(path)` wraps it as `{"source_path": ..., "structure": inspect_deck(path)}` for callers that want provenance; `engine-inspect`'s CLI/JSON output now uses `describe_deck`, so its JSON shape changed from a flat dict to this nested form (documented, deliberate, and covered by updated CLI tests).

48 new adversarial/fault-injection tests added (`tests/test_ppt_engine_audit_repair.py`), 120 total in the suite. Two pre-existing tests were updated, each with an explicit reason: one `OutputPathError` message-wording assertion (F-01 improved the message text) and the CLI JSON-shape assertions in `test_engine_cli.py`/`test_engine_cli_geometry_image.py` (F-08's `structure`/`source_path` split). No test was weakened; all 72 pre-existing behavioral assertions still hold.

**Reason:** These are safety-critical defects in the exact guarantees the Safe PPT Engine exists to provide before any semantic/AI layer is built on top of it. Fixing them now, with adversarial regression coverage proving each fix, keeps the mechanical foundation trustworthy rather than deferring known gaps into Documentation Intelligence work that would inherit them.

**What was deliberately not done:** no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py` (unrelated baseline modules, untouched); the canonical ZIP was not touched. Independent Codex re-audit of this repair was **not** performed as part of this decision — that is the explicitly required next action, not something Claude can self-certify.

**Status:** Active. Re-audit pending.

---

## PROJ-010 — Safe PPT Engine Audit Repair 2 (re-audit findings F-02, F-04, F-05, F-06, R-01, R-02)

**Decision:** An independent Codex re-audit of Audit Repair 1 confirmed F-01, F-03, F-07, and F-08 correctly closed (no regression), and found 6 findings remaining. All 6 are fixed:

- **F-02 (high, no-overwrite publication not fully atomic):** `_publish_atomically` no longer opens the final `output_path` directly at all for `overwrite=False`. It now writes the complete staged content to a same-directory temp file, flushes and `os.fsync`s it, then publishes via `os.link(tmp, output_path)` - an atomic hard-link create that fails with `FileExistsError` (converted to `OutputPathError`) if the destination already exists, without the destination ever being observable as a partial file (unlike the prior `O_CREAT|O_EXCL` stream, which created the destination filename before any bytes were written into it). The temp file is removed in both the success and failure case. `overwrite=True` is unchanged (atomic `os.replace`).
- **R-01 (high, source-integrity check ran after publication):** a new shared `_finalize_transaction` helper is now the only path to `_publish_atomically` for every mutation primitive: it re-checks the source hash **before** calling `_publish_atomically` (the actual gate - if the source changed, publication is never attempted) and, only as defense-in-depth, once more after. A test asserts the literal call order (`_finalize_transaction` before `_publish_atomically`) for all four geometry/text mutation families, and separate tests prove a tampered source blocks publication entirely, leaving no output and no leaked temp files.
- **F-05 (medium, transaction I/O errors could leak as raw OSError):** a new `TransactionIOError` type normalizes filesystem/library failures during working-copy creation (`create_working_copy`), save (`_save_staged`, used by all three mutation code paths), temp-output preparation, and publication. Writing the adversarial test for this surfaced a genuine, previously-unwrapped bug: `_publish_atomically`'s own `tempfile.mkstemp` call was not inside any try/except, so a permission-denied output directory produced a raw traceback through the CLI - found via a real (unmocked) read-only-directory test, not a mock, and fixed by wrapping that call too.
- **F-04 (medium, relationship cleanup only scanned spTree):** `_drop_relationship_if_unreferenced` now scans `slide._element.iter(qn("a:blip"))` - the entire `<p:sld>` XML tree - instead of only `slide._element.spTree`. This correctly preserves a relationship still used by the slide background (`p:cSld/p:bg/p:bgPr/a:blipFill/a:blip`, a sibling of `p:spTree`, not a descendant of it) or any other non-shape slide element. Verified with a fixture where a foreground picture and the slide background share one relationship id (python-pptx's own default behavior when the same image content is related to the same slide part twice) - replacing the foreground picture leaves the background's relationship and media completely intact, which the prior spTree-only scan would have incorrectly dropped. A crop-preservation regression test (`crop_left`/`crop_top`/`crop_right`/`crop_bottom` before and after replacement) was added alongside it.
- **F-06 (medium, verification/governance evidence incomplete):** added `openspec/changes/safe-ppt-engine/spec.md` - the canonical classification record (base tier, rationale, applicable profiles, explicit audit requirement) `docs/TESTING.md` → "Canonical recording surface" requires live in the change's `spec.md`, not only `verification-report.md`, which was the actual gap. Expanded `verification-report.md` with builder review, founder-preview status (recorded truthfully as **pending** - no human has previewed a generated `.pptx` in real PowerPoint - not fabricated as complete), the Data-sensitive profile's specific gates (migration test and tenancy-isolation N/A with stated reasons; data-preservation and rollback-plan evidence stated), and full commands/results. Added `openspec/changes/safe-ppt-engine/audit-report.md`, the canonical location for independent-audit findings per `docs/PIPELINE.md`, recording the complete audit history (Audit 1 FAIL → Repair 1 → Audit 2 FAIL → Repair 2 → Audit 3 pending) truthfully, without claiming any audit passed that has not.
- **R-02 (low, adversarial tests proved end-state, not mechanism):** strengthened tests to prove the actual mechanism, not just outcomes: a call-count spy on `create_working_copy` proves a non-picture target results in *zero* staging calls (F-07), not merely no leftover temp file; call-order assertions prove the R-01 integrity gate runs strictly before publish for every mutation family; the F-03 missing/empty/corrupt/valid-ZIP-but-not-PPTX matrix now runs against every mutation family (`set_shape_text`, `move_shape`, `resize_shape`, `set_shape_geometry`, `replace_picture`) at both the engine-API and CLI boundary; image-replacement coverage was extended with crop-bearing, shared-picture, and background-sharing fixtures.

65 new tests added (`tests/test_ppt_engine_audit_repair_2.py`), 185 total in the suite. Two pre-existing tests updated with explicit justification (both now assert `TransactionIOError`, the more specific type F-05 introduces, in place of the broader error type they previously accepted) - no test was weakened; explicit regression tests confirm F-01/F-03/F-07/F-08 remain closed.

**Reason:** These were exactly the gaps a second independent audit round exists to catch - a mostly-correct first repair with residual atomicity, sequencing, error-typing, and cleanup-scope defects, plus genuinely incomplete governance recordkeeping. Fixing them, with tests that prove the mechanism rather than only the outcome, keeps the mechanical foundation trustworthy before any further capability is built on it.

**What was deliberately not done:** no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the canonical ZIP was not touched; no founder approval or independent-audit-pass was fabricated - both the founder-preview and the Audit 3 re-audit gates are recorded as genuinely pending.

**Status:** Active. Subsequently re-audited (Audit 3); see PROJ-011.

---

## PROJ-011 — Safe PPT Engine Audit Repair 3 (Audit 3 findings F-05, F-06, R-01, R-02) — local only, not merged

**Decision:** An independent Codex re-audit of Audit Repair 2 (Audit 3, against merged commit `69bb4761921ac7f7b843beb04ab255a2b272865e`) confirmed F-01, F-02, F-03, F-04, F-07, and F-08 correctly closed with no regression, and found 4 findings remaining. Per explicit task instruction, all 4 are fixed as **local, uncommitted working-tree changes only** - not committed, pushed, or opened as a PR, so `main` is unaffected by this decision and still reflects Audit Repair 2.

- **F-05 (wrap `create_working_copy`/`mkstemp` and cleanup failures; cleanup failure must not be silently swallowed):** `create_working_copy` now wraps its `tempfile.mkstemp` call (previously unwrapped) as well as the `shutil.copy2` call in typed-error handling. A new `_remove_reporting_failure(path)` helper replaces the previous `_safe_remove`, which discarded cleanup `OSError`s with a bare `pass`; the new helper never itself raises, but returns a description of any removal failure, which every caller folds into the error it is already raising (or, when nothing else is failing, raises on its own). A new `_staged_copy` context manager centralizes the create/cleanup pairing for every mutation primitive: if the wrapped mutation raised, a cleanup failure is reported as a stderr warning (visible, but not replacing the primary exception's type); if the wrapped mutation succeeded, a cleanup failure is itself raised as `TransactionIOError`, since a leaked temp file is then the only remaining problem and must not be silently accepted as success. The same reporting-not-swallowing treatment was applied to every cleanup call site inside `_publish_atomically`.
- **R-01 (source integrity must be checked immediately before publication; remove the post-publication check as a failure gate; source deletion/read errors during the pre-publish check must be typed; once atomic publication succeeds, the transaction is successful):** `_finalize_transaction` now calls a new `_verify_source_hash_or_raise(input_path, before_hash)` exactly once, immediately before `_publish_atomically`, and nothing after it. The previous second hash comparison that ran *after* `_publish_atomically` returned - the "defense-in-depth" check from Audit Repair 2 - has been removed entirely, per explicit instruction that it must not remain a failure gate: once publication succeeds, the transaction is complete, and re-checking afterward could only produce a confusing partial-success-that-raises state, never actually protect anything (the publish had already happened). `_verify_source_hash_or_raise` itself now distinguishes a source that is unreadable/deleted (typed `DeckSourceError`, since that is a read failure, not a content mismatch) from a source that is readable but has changed (typed `SafeDeckError`, the existing safety-invariant-violation case) - previously, `file_sha256` could raise a bare `OSError` at this exact point if the source vanished between the initial hash and this check.
- **R-02 (add regressions for the exact residual cases above):** 19 new local tests (`tests/test_ppt_engine_audit_3.py`) cover: `mkstemp` failure during both staging and publication; a cleanup failure that is itself induced (via mocking `os.remove`) on both the "mutation also failed" path (proving the secondary failure is reported, not substituted for the primary exception type) and the "mutation succeeded" path (proving the cleanup failure becomes the reported outcome, not silently accepted); source deletion and source tampering exactly at the pre-publish integrity boundary, parametrized across every mutation family (`set_shape_text`, `move_shape`, `resize_shape`, `set_shape_geometry`) plus a dedicated pair of tests proving `replace_picture` goes through the identical single integrity gate; and a call-order assertion proving the integrity check runs exactly once, immediately before (never after) `_publish_atomically`. All 185 pre-existing tests pass unmodified (204 total).

Governance recordkeeping (F-06) was also corrected this round, per explicit instruction: `spec.md` now states outright, under a "Process deviation" heading, that its classification was written retroactively (after Builds 1-2 were already implemented and merged) rather than presenting it as though it happened at the normal pre-implementation time; no approval is invented anywhere in `spec.md`'s "Approval" section (still truthfully "Not approved - no founder sign-off has occurred"); `verification-report.md` now states the *actual* recorded CI result for the last pushed round (Audit Repair 2 / PR #7: GitHub Actions `validate` passed, confirmed via `gh pr checks` and the merge-commit run) in place of the prior placeholder "expected to pass... updated once inspected" language that was never actually updated; `audit-report.md`'s verdict table now includes Audit 3's actual result and a fourth (not-yet-performed) row for Audit 4; and founder-preview status remains explicitly recorded as pending in both files, unchanged by this round.

**Reason:** A third independent audit round finding a narrower but still real set of defects (a silently-discarded cleanup failure that could mask data loss context, and an integrity check sequenced so a corrupted-transaction case could theoretically still report success after publication had already happened) is exactly the kind of residual risk that should not go unfixed just because two prior rounds already passed. Fixing it, and correcting the governance record to state plainly what has and has not been independently verified, keeps both the code and its paper trail honest before a fourth audit round and before any further capability is built on this engine.

**What was deliberately not done:** no commit, push, PR, or merge - these changes exist only in the local working tree, per explicit instruction, so Codex can audit them directly; no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the canonical ZIP was not touched (its SHA-256 was re-verified); no founder approval or independent-audit-pass was fabricated - the founder-preview gate and the pending Audit 4 are both recorded as genuinely open.

**Status:** Active, local only. Subsequently re-audited (Audit 4, directly against this local diff — no push required); see PROJ-012.

---

## PROJ-012 — Safe PPT Engine Audit Repair 4 (Audit 4 findings F-05, F-06, R-02) — local only, not merged

**Decision:** An independent Codex re-audit of Audit Repair 3's local, uncommitted diff (Audit 4 — reviewed directly, without requiring a push or PR) confirmed R-01 correctly closed, and found 3 findings remaining. Per explicit task instruction, all 3 are fixed as **local, uncommitted working-tree changes only** - not committed, pushed, or opened as a PR, so `main` is unaffected by this decision and still reflects Audit Repair 2.

- **F-05 (`create_working_copy` must type/clean up `os.close(fd)` failures too; no raw `OSError`, no leaked temp pathname):** `create_working_copy`'s `os.close(fd)` call - previously the one remaining unwrapped I/O call in the function, since only `tempfile.mkstemp` and `shutil.copy2` had been wrapped in Audit Repair 3 - is now wrapped in its own `try/except OSError`. On failure it calls `_remove_reporting_failure(staged)` to remove the temp file `mkstemp` had already created on disk (folding any secondary removal failure into the message), then raises typed `TransactionIOError`. Previously a close failure (e.g. `EBADF`/`EIO` on some filesystems) would surface as a raw, untyped `OSError` at the CLI and leak the already-created temp file, since nothing downstream would ever remove a path that `create_working_copy` never returned. **This round verified and fixed the temp-pathname leak only - it did not verify that the underlying file descriptor itself stayed closed after a reported close failure, and made no claim either way.** That narrower descriptor-leak question was raised by Audit 5 and is fixed in PROJ-013.
- **F-06 (correct governance records to reflect the actual workflow; remove statements saying Audit 4 requires a push; do not call N/A gates "approved" unless an approving authority is actually recorded):** every governance record's claim that "Audit 4... cannot occur until they are pushed" was factually contradicted by this very task - Audit 4 audited exactly such a local, uncommitted diff. `spec.md`, `verification-report.md`, `audit-report.md`, and `docs/CURRENT.md` were all corrected to state plainly that Codex reviewing a local diff directly (demonstrated first by Audit 4) is a normal, exercised part of this workflow, and that only the actual next step (Audit 5, not yet performed) remains open - with no claim that a push is a precondition for it. Separately, `verification-report.md`'s Data-sensitive profile table previously labeled its two not-applicable gates (migration test, tenancy-isolation check) as "N/A (approved)" - no approving authority reviewed or recorded approval of either determination, so "approved" was an invented status. Both rows now read plain "N/A" with the factual reason, explicitly noting no approving authority is recorded for the determination.
- **R-02 (add regressions for the `os.close` failure; make cleanup-warning tests assert the warning/output they claim to verify; correct any overstated coverage wording):** a new `test_f05_create_working_copy_os_close_failure_typed_and_no_leak` test selectively patches `os.close` to fail only for the fd `mkstemp` actually returned (so pytest's own file descriptors are unaffected), asserting both the typed `TransactionIOError` and that the temp file is not left behind. Two pre-existing Repair-3 tests - `test_f05_staged_copy_cleanup_failure_on_primary_failure_does_not_mask_it` and `test_f05_publish_temp_cleanup_failure_reported_on_link_exists_race` - had docstrings claiming a secondary cleanup failure "is reported (as a warning)" but only asserted the raised exception's type, never the warning text actually printed; both now use `capsys` to assert the literal stderr warning text, closing the gap between claimed and actual coverage. No existing test was weakened - these are additive assertions on already-passing tests.

1 new local test added (`tests/test_ppt_engine_audit_3.py`), 205 total in the suite (up from 204); the 2 strengthened tests are counted within that total, not as new tests. All 204 pre-existing tests still pass, with only those 2 gaining an additive assertion.

**Reason:** A fourth independent audit round finding a narrow but real residual error-typing gap (the exact same class of "one more unwrapped I/O call" defect the F-05 lineage keeps surfacing, this time at `os.close` rather than `mkstemp`/`copy2`) plus two governance-recordkeeping defects - one where the record's own language was already contradicted by the very audit round that flagged it, and one where "N/A" had drifted into implying an approval that never happened - is exactly the kind of residual risk that should not go unfixed just because three prior rounds already passed. Fixing it, and correcting the governance record to describe the workflow this project actually follows rather than an assumed and inaccurate one, keeps both the code and its paper trail honest before a fifth audit round.

**What was deliberately not done:** no commit, push, PR, or merge - these changes exist only in the local working tree, per explicit instruction, so Codex can audit them directly, exactly as it did for Audit 4; no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the canonical ZIP was not touched (its SHA-256 was re-verified); no founder approval or independent-audit-pass was fabricated - the founder-preview gate and the pending Audit 5 are both recorded as genuinely open, and the two Data-sensitive N/A gates no longer claim an approval that was never recorded.

**Status:** Active, local only. Subsequently re-audited (Audit 5, directly against this local diff — no push required); see PROJ-013.

---

## PROJ-013 — Safe PPT Engine Audit Repair 5 (Audit 5 findings F-05, F-06, R-02) — local only, not merged

**Decision:** An independent Codex re-audit of Audit Repair 4's local, uncommitted diff (Audit 5 - reviewed directly, without requiring a push or PR) found 3 findings remaining, all narrower than the round before. Per explicit task instruction, all 3 are fixed as **local, uncommitted working-tree changes only** - not committed, pushed, or opened as a PR, so `main` is unaffected by this decision and still reflects Audit Repair 2.

- **F-05 (when the staging descriptor close path fails, ensure the descriptor itself is not leaked; must still return typed `TransactionIOError`, remove the temp pathname, and leave the fd actually closed):** Repair 4 wrapped `create_working_copy`'s `os.close(fd)` call in a typed error and removed the temp pathname on failure, but never verified the fd itself stayed closed - POSIX `close(2)` deallocates a process's fd slot on return even when it reports an error (e.g. a delayed write-back failure surfacing as `EIO`), which is normally why retrying `close()` is unsafe (the fd number may already have been reused by something else). `create_working_copy` now handles the narrow case where that guarantee doesn't hold: on a close failure it calls `os.fstat(fd)` to check whether the descriptor is still genuinely open, and only if it is, retries `os.close(fd)` once (swallowing any further failure from that retry, since the original failure is already what's being reported). This retry is safe specifically because a `mkstemp`-returned fd is not yet shared with any other code at this point in the function, so `fstat` finding it "still open" cannot be a false positive caused by an unrelated file reusing the same number. The temp pathname is still removed via `_remove_reporting_failure` and folded into the raised `TransactionIOError`, exactly as before. **Correction (Audit 6): the "this retry is safe" reasoning in the paragraph above was wrong and is retracted, not merely qualified.** The fd being "not yet shared with any other code" only holds until the moment `close()` fails; nothing prevents another thread, signal handler, or library elsewhere in the process from opening a new file and being handed that exact fd number back before this function's `fstat`/retry runs. Audit 6 found this defect and PROJ-014 removes the retry entirely rather than trying to make it safer.
- **R-02 (strengthen the regression to verify descriptor closure, not just pathname removal; must fail if `os.fstat(fd)` still succeeds after the operation):** `test_f05_create_working_copy_os_close_failure_typed_and_no_leak` (`tests/test_ppt_engine_audit_3.py`) was rewritten from a mock that always fails `os.close` for the target fd (which could never actually prove closure, since it never let a close succeed) to a stateful mock that fails only the first call and lets a second, retried call actually close the fd - modeling the real "close() failed to report success once" scenario the fix targets. The test now asserts three things: the typed error is raised, the temp pathname was removed, and - the actual descriptor-leak regression - `create_working_copy` retried the close exactly once and `os.fstat(fd)` raises `OSError` afterward, proving the fd is genuinely closed. Confirmed this test fails against the pre-fix code (no retry attempted, so the mocked close call count stays at 1 and `os.fstat(fd)` still succeeds). **Correction (Audit 6): this test proved a retry happened and that the fd eventually became closed - it never proved the fd being retried was still safely the engine's own descriptor at the moment of the retry, which is exactly the property that mattered.** It was structurally incapable of catching the fd-reuse risk Audit 6 found. PROJ-014 replaces it.
- **F-06 (correct remaining governance overstatements - do not claim "no staging resource leaks" until this fix is verified; fix `spec.md` saying "two rounds" when the audit history now records four completed audits before this repair):** `spec.md`'s Classification → Rationale line, which still read "two rounds of independent audit" from before this engine's audit history grew past Audit 2, now reads "four completed rounds of independent audit as of this record, with a fifth pending." Separately, `docs/CURRENT.md`'s and `docs/DECISIONS.md`'s (PROJ-012) write-ups of Audit Repair 4's F-05 fix both said or implied "no leaked staging resource remains" - true only of the temp *pathname*, which was the one thing Repair 4 actually verified; whether the file descriptor itself could still leak was never checked, and Audit 5 found that it could. Both records were corrected to state plainly that Repair 4 closed the pathname leak only, and to point to this entry for the descriptor-leak fix - rather than let the overstatement stand uncorrected now that it's known to have been one.

1 test strengthened in place (no new test file); 205 total in the suite (unchanged from Audit Repair 4 - the strengthening added assertions to an existing test rather than adding a new one). All 204 other pre-existing tests pass unmodified.

**Reason:** A fifth independent audit round finding that the previous round's fix addressed the visible symptom (an untyped error, a leaked file on disk) but not the full claim implied by its own commit message (no staging resource leaked) is exactly the kind of gap that matters for infrastructure whose entire purpose is not corrupting or leaking resources tied to an irreplaceable source file. Fixing the descriptor-closure gap itself, strengthening the regression to actually detect it, and correcting the governance record to stop repeating a claim that was never verified, keeps both the code and its paper trail honest before a sixth audit round.

**What was deliberately not done:** no commit, push, PR, or merge - these changes exist only in the local working tree, per explicit instruction, so Codex can audit them directly, exactly as it did for Audits 4 and 5; no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the canonical ZIP was not touched (its SHA-256 was re-verified); no founder approval or independent-audit-pass was fabricated - the founder-preview gate and the pending Audit 6 are both recorded as genuinely open.

**Status:** Active, local only. Subsequently re-audited (Audit 6, directly against this local diff - no push required); found this repair's own F-05 fix unsafe. See PROJ-014.

---

## PROJ-014 — Safe PPT Engine Audit Repair 6 (Audit 6 findings F-05, F-06, R-02) — local only, not merged

**Decision:** An independent Codex re-audit of Audit Repair 5's local, uncommitted diff (Audit 6 - reviewed directly, without requiring a push or PR) found 3 findings, all concerning Repair 5's own F-05 fix and the governance record describing it. Per explicit task instruction, all 3 are fixed as **local, uncommitted working-tree changes only** - not committed, pushed, or opened as a PR, so `main` is unaffected by this decision and still reflects Audit Repair 2.

- **F-05 (remove the unsafe `fstat(fd)` + retry-`close(fd)` logic; after `os.close(fd)` reports an error, do not touch that fd number again; clean the temp pathname if possible; raise typed `TransactionIOError`; document that descriptor state after a failed close is not safely recoverable by retrying):** the `os.fstat(fd)`-then-retry block Repair 5 added to `create_working_copy`'s close-failure path is removed entirely. `create_working_copy` now does exactly what it did in Audit Repair 4 for the close-failure path: on `OSError` from `os.close(fd)`, it removes the temp pathname via `_remove_reporting_failure` and raises typed `TransactionIOError` - it never calls `os.fstat` or `os.close` on that fd again. The function's docstring and an inline comment on the `except` block now explain why: once `close()` has reported an error, the fd's state is ambiguous even where POSIX's "deallocated regardless of error" guarantee holds, because something else in the process (another thread, a signal handler, a library, the interpreter's own finalizers) may already have been handed that exact fd number back by the time control returns here - there is no way to distinguish "still ours" from "already reused" without racing that possibility, so the code deliberately does not try.
- **R-02 (replace the artificial retry test with a regression proving the engine never performs a second close on the same fd after a close error, including an fd-reuse adversarial case):** `test_f05_create_working_copy_os_close_failure_typed_and_no_leak` is replaced by two tests. `test_f05_create_working_copy_os_close_failure_typed_and_never_retouches_fd` spies on both `os.close` and `os.fstat`, fails the target fd's close exactly once (while genuinely releasing it, matching the code's own documented assumption), and asserts the engine calls `os.close`/`os.fstat` on that fd exactly once total - never again after the failure. `test_f05_create_working_copy_close_failure_does_not_touch_reused_fd` is the adversarial case the finding specifically named: inside the mocked close failure, it genuinely opens a second, unrelated real file, which reliably reuses the exact fd number just freed (the lowest-available-fd behavior of Linux/macOS); the test then asserts the engine never calls `os.close`/`os.fstat` on that number again, and separately proves the reused file is still open and writable afterward - i.e. the engine did not blindly act on a descriptor it no longer owns. Both tests were confirmed to fail against the pre-fix (retry) code: reinstating the `fstat`/retry block causes both to observe a second `close`/`fstat` call, which is exactly the defect Audit 6 flagged.
- **F-06 (correct the audit chronology and remove every claim that Audit 5 closed F-05/F-06/R-02, or that the retry strategy was proven safe; record Audit 6 = FAIL, Audit 7 = PASS WITH FINDINGS, Repair 7 local, Audit 8 pending):** `audit-report.md`'s Audit 5 row is corrected: it no longer characterizes Audit 5 as having "confirmed" Repair 4's F-06/R-02 fixes closed - Audit 5's own scope was Repair 4, and no later round has independently re-examined those two fixes on their own, so recording them as confirmed-closed was more than Audit 5 itself established. A new Audit 6 row records **FAIL** with this round's 3 findings, and the Audit 7 row records **PASS WITH FINDINGS** and the Audit 8 row is pending. `verification-report.md` and `spec.md` are updated the same way (round counts, file title, acceptance-criteria and audit-requirement prose). `docs/DECISIONS.md`'s PROJ-013 entry (above) gets an explicit correction note on its F-05 and R-02 bullets stating the "this retry is safe" reasoning and the retry-proving test were both wrong, rather than leaving them to stand as if still accurate history.

1 test removed, 2 added (net +1); 206 total in the suite. All other pre-existing tests pass unmodified.

**Reason:** A sixth independent audit round finding that the *fix* for the previous round's finding was itself a resource-safety defect - not a residual gap in an otherwise-sound fix, but a new risk introduced by the fix's own retry logic - is a more serious class of problem than any prior round in this history: it demonstrates that "reasoned as safe and documented as such" is not the same as "independently verified as safe," which is the entire premise for requiring audit rounds this far past the point where the code looked correct. Removing the retry entirely (rather than trying to patch it into being provably race-free, which would require synchronization primitives this single-threaded-caller function has no reason to carry) is the accuracy-over-cleverness choice: the engine now honestly reports "could not confirm this fd is closed" via a typed error, rather than attempting a recovery whose own correctness cannot be locally proven.

**What was deliberately not done:** no commit, push, PR, or merge - these changes exist only in the local working tree, per explicit instruction, so Codex can audit them directly, exactly as it did for Audits 4, 5, and 6; no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the canonical ZIP was not touched (its SHA-256 was re-verified); no founder approval or independent-audit-pass was fabricated - the founder-preview gate and the pending Audit 8 are both recorded as genuinely open; no attempt was made to build a "safer" retry (e.g. locking, PID/generation-tagging the fd) since that would add unverified complexity to work around a problem better solved by simply not retrying.

**Status:** Active, local only. Subsequently re-audited (Audit 7, directly against this local diff - no push required); found F-06 and R-02 remaining (F-05 confirmed closed). See PROJ-015.

---

## PROJ-015 — Safe PPT Engine Audit Repair 7 (Audit 7 findings F-06, R-02) — local only, not merged

**Decision:** An independent Codex re-audit of Audit Repair 6's local, uncommitted diff (Audit 7 - reviewed directly, without requiring a push or PR) confirmed F-05 (the removal of the unsafe `fstat`/retry logic) correctly closed, and found 2 findings remaining, both about this record's own accuracy rather than about `create_working_copy`'s behavior. Per explicit task instruction, both are fixed as **local, uncommitted working-tree changes only** - not committed, pushed, or opened as a PR, so `main` is unaffected by this decision and still reflects Audit Repair 2.

- **F-06 (correct the stale `docs/DECISIONS.md` heading so the chronology truthfully says Audit 6 failed and Audit 7 completed with findings / next re-audit pending as appropriate):** PROJ-014's own `**Status:**` line (above) still read "Independent re-audit (Audit 7) pending" even though, per this task, Audit 7 had already occurred and found these two findings - a stale-heading defect of the exact same shape this history has now corrected twice before (PROJ-012's and PROJ-013's status lines, corrected in earlier rounds). It now reads "Subsequently re-audited (Audit 7...); found F-06 and R-02 remaining (F-05 confirmed closed). See PROJ-015." `audit-report.md` already correctly recorded Audit 6 as **FAIL** and Audit 7 as **PASS WITH FINDINGS** after the prior round; this finding was specifically about `docs/DECISIONS.md`'s own status line lagging behind that, not about `audit-report.md`.
- **R-02 (strengthen the fd-reuse regression to explicitly assert that the unrelated file actually reused the exact failed descriptor number before testing that the engine leaves it untouched):** `test_f05_create_working_copy_close_failure_does_not_touch_reused_fd` (`tests/test_ppt_engine_audit_3.py`) relied on the OS's lowest-available-fd behavior to make the unrelated file reuse the exact number the engine's failed `close()` freed, but never asserted that this actually happened. If that assumption had silently failed to hold on some platform or under some allocator state, the test's main assertion (`create_working_copy` never touches the fd again) would have passed vacuously - proving only that the engine leaves some *other*, non-reused fd alone, which every other test already covers, not that it correctly avoids a genuinely reused one. The test now asserts `reused["fd"] == failing_fd["fd"]` immediately after the mocked operation, before the no-touch assertion, with a message explaining exactly what a failure of that assumption would mean. Confirmed stable across 5 consecutive runs on this platform (macOS/APFS) - the reuse assumption holds reliably here.

No engine behavior was changed: the strengthened test still passes against the current `create_working_copy`, confirming Repair 6's fix is correct and that the previous round's adversarial premise did in fact hold in practice - it just wasn't verified by the test itself. Per the task instruction, since the strengthened test did not expose a real defect, `lib/ppt_engine.py` is untouched this round.

0 tests added or removed; 1 existing test strengthened in place (an added assertion, not a rewrite of its scenario); 206 total in the suite, unchanged from Audit Repair 6. All other pre-existing tests pass unmodified.

**Reason:** A seventh independent audit round finding that a governance record's own status line had gone stale relative to the very event that triggered writing it, and that an adversarial regression's core adversarial premise was assumed rather than checked, are both instances of the same underlying discipline this whole audit history has been enforcing on the code: don't claim more than what was actually verified. Applying that same discipline to the paperwork and the test's own assumptions, not just to `create_working_copy`'s logic, keeps the full audit trail trustworthy going into an eighth round.

**What was deliberately not done:** no commit, push, PR, or merge - these changes exist only in the local working tree, per explicit instruction, so Codex can audit them directly, exactly as it did for Audits 4 through 7; no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the canonical ZIP was not touched (its SHA-256 was re-verified); no change to `create_working_copy`'s behavior, since the strengthened test did not expose a defect in it - only in the test's own unverified assumption; no founder approval or independent-audit-pass was fabricated - the founder-preview gate and the pending Audit 8 are both recorded as genuinely open.

**Status:** Active, local only. Independent re-audit (Audit 8) pending. It does not require these changes to be pushed first - Audits 4 through 7 already demonstrated Codex reviewing a local, uncommitted diff directly.

---

## PROJ-016 — Editorial Memory Slice 1

**Decision:** Editorial Memory is the next approved implementation milestone in locked Architecture v0.1, per the founder's lifecycle clarification (living/evolving/version-aware/provenance-preserving/history-preserving knowledge layer) and the subsequently accepted design audit recorded in `docs/EDITORIAL_MEMORY_IMPLEMENTATION_AUDIT.md` (committed here for the first time as part of this change - it had previously existed only as local, uncommitted design work, never merged). Slice 1 - the smallest real implementation proving the lifecycle end-to-end, per explicit task instruction not to build a broad knowledge platform - is implemented, tested, and integrated.

- **Three entities, no ontology:** `Evidence` (an artifact/observation, never itself knowledge), `KnowledgeItem` (the durable subject, identified by a stable natural key), `KnowledgeState` (one versioned claim about a `KnowledgeItem`, always grounded in evidence). No graph semantics, no universal semantic IDs beyond ordinary persistence identity, no vector or graph database.
- **Core invariant enforced in code:** Evidence never promotes itself into Knowledge. `propose_state` raises typed `MissingProvenanceError` if no evidence is cited and typed `UnknownEvidenceError` if a cited evidence id doesn't exist. The only way a state becomes `current` is an explicit `approve_state` call - no evidence type, including browser observation, auto-promotes.
- **Supersession vs. invalidation kept distinct:** approving a new state moves the prior current state to `superseded` (never deleted, always traceable via `superseded_by`); `invalidate_state` is a separate operation for knowledge that was never trustworthy, requiring no replacement to exist. Both statuses remain in `get_history` forever.
- **Unresolved conflict is a first-class, minimal concept:** a proposed state whose `relation_to_previous` is `contradicts` has a computed `review_required` property (never a stored field that could drift), surfaced via `has_pending_conflict`/`get_conflicts`. The existing current state is untouched until an explicit approval resolves it - a newer, contradicting proposal never silently wins by virtue of being newer.
- **No scoring anywhere:** `evidence_quality` (`high`/`medium`/`low`) is artifact-reliability metadata, not a truth score. `get_evidence_type_summary` preserves per-state source-category counts (e.g. distinguishing "browser + maintainer + screenshot" from "browser + browser + browser") purely as information, with no ranking or automatic weighting.
- **Persistence:** plain, atomically-written (temp file + rename) JSON files under a caller-chosen directory - one file per `Evidence` record, one per `KnowledgeItem` (embedding its full append-only state history). No new dependency (Python standard library only), no database, git-diffable like the rest of this repository. `EditorialMemory` reads fresh from disk on every call rather than caching, so reload-from-storage correctness holds by construction.
- **Feature-area naming convention** (`<platform>.<feature>`, e.g. `desktop.filters`) and **optional editorial `rationale`** on proposed states, per the founder-approved amendments, both implemented as plain fields - no registry, no taxonomy.

26 new tests (`editorial-memory/tests/test_editorial_memory.py`), including one deterministic 15-step end-to-end test proving the full evidence → item → state → approval → current-retrieval → new-unapproved-evidence → supersession → contradiction/review-required flow in a single run, matching the required Slice 1 lifecycle scenario exactly. `python scripts/check_pipeline.py` passes; the Safe PPT Engine (`baseline/B4PS-TUT-main/.b4ps-tools/`) was not touched.

**Reason:** Editorial Memory exists to hold claims about Bridge4PS without ever letting an artifact's mere existence be mistaken for approved current truth - exactly the guarantee the founder's lifecycle clarification and the "knowledge system, not a truth system" framing describe. Building the smallest version that proves this end-to-end, before any broader platform work, keeps the same safety-first discipline established for the Safe PPT Engine: a mechanical, testable foundation first, semantic/AI capability only after independent audit confirms the foundation is trustworthy.

**What was deliberately not done:** no Documentation Intelligence, no AI reasoning over memory, no affected-slide detection, no semantic red-box targeting, no browser automation or live `browser_observation` producer, no vector search, no embeddings, no graph database, no ontology, no generalized Bridge4PS Product Knowledge platform, no UI, no Freshdesk/AppSheet integration, no automatic ingestion from old PPTs, and no automatic promotion of evidence into truth; no CLI (nothing calls this API yet); no purge/hard-deletion tooling (deferred per the already-approved design - normal supersession/invalidation is the only lifecycle mechanism this slice implements); no modification to Safe PPT Engine behavior; no redesign of Architecture v0.1; no Slice 2 work, per explicit instruction to stop after this slice.

**Status:** Active, merged. Independent Codex audit of Editorial Memory Slice 1 (Audit 1) is the required next action before Slice 2 begins.
