# Bridge4PS Decision Log

Durable decisions for the Bridge4PS Documentation Engineer repository. This log records this project's own decisions. It is separate from the Project-Pipeline decision log (`capcutfor1month-oss/project-Pipline` → `docs/DECISIONS.md`), which governs the pipeline itself.

## PROJ-001 — Pipeline adoption

**Decision:** Bridge4PS adopts the Universal Agentic Project Pipeline from `capcutfor1month-oss/project-Pipline` at source commit `e755ece0caf62f8e7df1a6c168727049c362cd4c` to govern repository-development workflow only.

**Reason:** Give this repository a repeatable, founder-friendly process for clarifying, specifying, implementing, testing, auditing, and releasing changes without adopting any pipeline concept as Bridge4PS product or domain content.

**Status:** Active

---

## PROJ-002 — Inherited pipeline decision: DEC-014 — Collaborative founder decision rule

**Decision (inherited from Project-Pipeline):** ChatGPT is the founder's brainstorming, explanation, and strategy partner. The founder and ChatGPT discuss evidence, options, risks, and recommendations before deciding the next product move. Claude implements approved work, and Codex audits independently when the approved workflow reaches the audit step. When the founder shares agent output, the default response is explanation and discussion — not automatic generation of another agent prompt.

**Applies to Bridge4PS as:** every agent working in this repository follows this rule unchanged.

**Status:** Active

---

## PROJ-003 — Inherited pipeline decision: DEC-017 — Target-domain / development-governance separation

**Decision (inherited from Project-Pipeline):** Two authorities apply together and must never be merged. Bridge4PS's own canonical documents (`docs/PRODUCT.md`, `docs/ARCHITECTURE.md`) govern what the product is. Project-Pipeline governs only how repository changes are clarified, approved, specified, investigated, implemented, tested, audited, and released. Being given, pointed to, or reading the Project-Pipeline repository never authorizes adopting its concepts as Bridge4PS domain content.

**Applies to Bridge4PS as:** the binding authority-boundary rule recorded in `docs/INDEX.md`.

**Status:** Active

---

## PROJ-004 — Baseline import deferred

**Decision:** The canonical raw baseline `B4PS-TUT-main.zip` is designated as the source of the Bridge4PS implementation but is not imported during pipeline bootstrap.

**Reason:** Bootstrap scope is limited to adopting the pipeline. Importing and verifying the baseline is a separately approved task, recorded as the exact next action in `docs/CURRENT.md`.

**Status:** Superseded by PROJ-005.

---

## PROJ-005 — Baseline imported; canonical ZIP is fidelity authority, GitHub materialization is a 50/58 publication view

**Decision:** `B4PS-TUT-main.zip` (SHA-256 `00f5e41f475b8205535481da11f73c4e4f0bd614e0d3b1efff938e921b9eb6ee`, 58 files, embedded source commit `85af51a123e237c14f61f5bb43094292db664561`) was imported and is the founder-approved final publication model: the unmodified archive is committed at `baseline/source-artifacts/B4PS-TUT-main.zip` as the authority for exact raw-baseline fidelity. Its extraction is committed at `baseline/B4PS-TUT-main/` with its original `.gitattributes` unmodified, containing 50 of the 58 source files byte-for-byte. The remaining 8 files are pre-existing Git LFS pointer-text files whose real binaries were never available; GitHub's server-side Git LFS enforcement (`GH008`) rejects any push referencing those objects regardless of `.gitattributes` content (confirmed by two prior failed attempts, one editing `.gitattributes` and one additionally squashing history — both still rejected). The founder approved omitting those 8 paths from the extracted GitHub tree only, while keeping their exact pointer representations preserved inside the canonical ZIP. The full omission manifest (path, LFS OID, declared size) is recorded in `docs/CURRENT.md`.

**Reason:** The canonical ZIP alone cannot be the only published artifact without also giving GitHub a materialized, browsable tree; but GitHub's LFS enforcement makes publishing pointer-text files impossible without either fabricating binaries (forbidden) or altering their content (forbidden, would destroy evidence they originated as LFS pointers). Splitting the two artifacts — ZIP as immutable fidelity authority, extracted tree as a 50/58 publication view with a documented, non-fabricated gap — satisfies "preserve raw-baseline defects and limitations as baseline evidence" without either fabricating content or leaving the import unpublishable.

**Status:** Active. This decision does not authorize Repository Hardening Batch 1, resolving the 8 missing binaries, or any other baseline content change.

---

## PROJ-006 — Repository Hardening Batch 1 completed

**Decision:** Repository Hardening Batch 1 was implemented directly on the imported baseline tooling at `baseline/B4PS-TUT-main/.b4ps-tools/`, scoped to reproducibility, path/filesystem safety, failure behavior, configuration/state, tests, and operator usability — not to Safe PPT Engine feature work. Concrete fixes: (1) removed a hardcoded personal absolute path (`/Users/aamir/Documents/B4PS TUT`) from `scripts/capture_and_rename.sh`, replaced with a run-time-resolved project root and a `TMPDIR`-honoring temp file path; (2) added `requirements.txt` and `requirements-dev.txt` declaring the previously-undeclared `Pillow`/`numpy`/`opencv-python-headless`/`pytest` dependencies (already named in prose in the tool's own README but not machine-installable); (3) added `config.MissingDeckSourceError` and `config.require_pptx()`, wired into `DeckReader.__init__`, `deck.backup()`, and the CLI's top-level dispatch in `b4ps.py`, so a missing, empty, or corrupt deck `.pptx` now fails with one clear, actionable message and exit code 1 instead of a raw `FileNotFoundError`/`BadZipFile` traceback; (4) added 16 automated tests under `.b4ps-tools/tests/` (`pytest`) covering the fixed failure paths with small synthetic fixtures, plus static regression checks against the hardcoded-path defect and a CLI smoke test proving the tool runs clean from a fresh checkout; (5) added a Setup section, a Running-tests section, and a truthful known-limitation note to `.b4ps-tools/README.md`.

**Reason:** This repository's own baseline import (PROJ-005) made the missing-deck-source failure path immediately, concretely reproducible — running any deck-touching command in this checkout hits exactly the defect fixed in (3). Hardening this foundation before Safe PPT Engine work begins prevents that engine from being built against tooling that crashes opaquely on the exact condition this repository is currently in, and removes a machine-specific path that would silently break for any contributor other than the original author.

**What was deliberately not done:** the 8 missing LFS binaries were not obtained, fabricated, or otherwise resolved (out of scope, unchanged limitation); `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, `lib/geometry.py` were inspected for the same defect classes but not found to need a fix in this pass, and were not otherwise modified; no Safe PPT Engine feature, semantic-annotation, or workflow-intelligence work was started; the canonical ZIP `baseline/source-artifacts/B4PS-TUT-main.zip` was not touched.

**Status:** Active.

---

## PROJ-007 — Safe PPT Engine Build 1

**Decision:** Built the first Safe PPT Engine layer at `baseline/B4PS-TUT-main/.b4ps-tools/lib/ppt_engine.py`, using `python-pptx` (added to `requirements.txt`) rather than extending the baseline's existing hand-rolled XML-surgery approach in `lib/deck.py` — the two serve different purposes (`deck.py` is a purpose-built, high-performance surgical writer scoped to the two known production decks and their known shape conventions; the engine needs to safely load and describe *any* explicit `.pptx` path generically, which `python-pptx` already does safely and correctly). The engine provides: `load_deck` (typed, never-fallback loading), `inspect_deck` (deterministic structural JSON with no semantic interpretation), `create_working_copy` (mutation always staged off both the original and the final output path), and `set_shape_text` (the one controlled mutation primitive built this pass — explicit `(slide_index, shape_index)` targeting only, validated by reopening the saved result before it is placed at the caller's output path). Exposed via `engine-inspect` and `engine-set-text` CLI commands. Verified with 21 new tests (37 total) against synthetic fixtures built programmatically with `python-pptx`/`Pillow` — the 8 unavailable Git LFS assets were not touched.

**Reason:** The approved build order requires a safe, deterministic mechanical foundation (read/inspect/copy/mutate/save/validate/preserve) before any Documentation Intelligence or semantic capability can be layered on top. Establishing the pattern with one real, tested mutation primitive (text update) proves the safety invariants — source never modified, output path never collides, invalid input fails before mutation, output is reopened and content-verified after save — without yet committing to which richer primitives (move/resize/image-replace) Documentation Intelligence will actually need.

**What was deliberately not done:** no semantic shape identification, fuzzy matching, screenshot intelligence, or Documentation Intelligence; no additional mutation primitives beyond `set_shape_text`; no changes to `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py` (existing anchor/matching workflow is untouched and independent of the new engine layer); the 8 missing LFS binaries were not obtained or fabricated; the canonical ZIP was not touched.

**Status:** Active.

---

## PROJ-008 — Safe PPT Engine Build 2: visual mutation primitives

**Decision:** Added four visual mutation primitives to `lib/ppt_engine.py`: `move_shape` (left/top, EMU only), `resize_shape` (width/height, EMU only), `set_shape_geometry` (left+top+width+height as one atomic operation, for future red-box relocation), and `replace_picture`. All coordinates/dimensions are EMU — python-pptx's and OOXML's own native integer unit; no other unit is accepted, and callers are responsible for converting from inches/points/pixels if needed. `move_shape` and `resize_shape` are each verified not to touch the other's attributes (a shared regression test proves resize does not move the shape, matching PowerPoint's own semantics — no repositioning side effect was observed or needed handling). `set_shape_geometry` validates all four values *before* any working copy is staged, so an invalid value cannot leave a partially-applied geometry anywhere, in memory or on disk.

`replace_picture` rejects a non-picture target shape and a missing/empty/unreadable replacement image before staging anything. Its default (and only, for this build) behavior preserves the existing shape's left/top/width/height frame exactly, regardless of the replacement image's own pixel dimensions or aspect ratio — chosen as the safest deterministic behavior, since it requires no cropping/scaling decision and matches how PowerPoint's own `blipFill` model already renders an image into a fixed frame. Implementation touches OOXML directly in exactly one place — repointing the picture's `p:blipFill/a:blip/@r:embed` relationship to a newly-added image part via python-pptx's own `SlidePart.get_or_add_image_part()` plus the `CT_Blip.rEmbed` attribute descriptor (both existing, documented python-pptx internals, not raw string/XML surgery) — isolated entirely inside `replace_picture`; no other code touches OOXML for image handling. Any existing crop data (`a:srcRect`) is preserved by construction, since it is never touched.

Build 1's final-publish step (a bare `shutil.copy2(staged, output_path)`) was not fully atomic — an interruption mid-copy could leave a partially-written file at `output_path`. Hardened this pass with `_publish_atomically()` (copy to a same-directory temp file, then `os.replace()`, which is atomic on the same filesystem) and retrofitted it onto `set_shape_text` as well as all four new primitives, per the explicit instruction to harden Build 1's atomicity within this build if it did not already guarantee it.

**Reason:** These are the specific geometry/image operations future red-box relocation and screenshot-swap work will call. Building them as small, independently-tested, deterministic primitives — reusing Build 1's staged-copy/validate/publish pattern rather than inventing a parallel one — keeps the mechanical foundation trustworthy before any semantic targeting is layered on top of it.

**What was deliberately not done:** no semantic shape identification, fuzzy matching, screenshot intelligence, automatic affected-slide detection, Editorial Memory, or Documentation Intelligence; no rotation/flip or other geometry attributes beyond left/top/width/height; no shape creation/deletion; no automatic aspect-ratio cropping or aesthetic image adjustment; no changes to `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the 8 missing LFS binaries (including both production decks) were not obtained, fabricated, or needed by any test; the canonical ZIP was not touched.

**Status:** Active.

---

## PROJ-009 — Safe PPT Engine Audit Repair 1 (Codex findings F-01–F-08)

**Decision:** An independent Codex audit of Safe PPT Engine Builds 1–2 found 8 defects. All 8 are fixed in `lib/ppt_engine.py`/`b4ps.py`, per-finding:

- **F-01 (critical, source aliasing):** `_assert_safe_output_path` now compares `os.path.realpath` of both paths (resolves symlinks even when the output's final path component does not yet exist) and, when both paths currently exist, additionally checks `os.path.samefile` (catches hardlinks/bind-mount aliasing realpath alone can miss). Applies uniformly to `set_shape_text`, `move_shape`, `resize_shape`, `set_shape_geometry`, and `replace_picture` (all route through this one function). The pre-existing before/after source-hash check is kept as defense-in-depth, not the primary protection.
- **F-02 (high, no-overwrite publication race):** `_publish_atomically` now branches on `overwrite`. `overwrite=False` opens the destination itself with `os.O_CREAT | os.O_EXCL`, which atomically fails if anything exists there at that instant — no separate exists-check followed by a write. `overwrite=True` keeps the prior same-directory-temp-file + `os.replace` atomic-rename approach.
- **F-03 (high, raw filesystem errors leak):** every mutation primitive now calls `load_deck(input_path)` (the typed validation boundary) before `file_sha256`/staging, via a shared `_validate_and_hash_source` helper. A missing input now raises `DeckSourceError`, never a bare `FileNotFoundError`. Verified at both the engine API and CLI boundary (all 5 mutation CLI commands exit 1 with a clean `error:` message, no traceback, for missing/empty/corrupt input).
- **F-04 (medium, stale media left in package):** `replace_picture` captures the old relationship id before repointing the blip, then calls `_drop_relationship_if_unreferenced`, which scans the slide's shape tree for any remaining `a:blip` still using that id and only then drops the relationship. python-pptx's own package writer serializes only parts still reachable via some relationship, so an unreferenced image part is automatically absent from the next save with no separate manual part-deletion step. Verified against actual ZIP package contents (not just API-level state): single-picture replacement removes the old media file; two pictures on one slide sharing the same image (python-pptx assigns them the same relationship id automatically) — replacing one leaves the other's image completely intact.
- **F-05 (medium, staging/failure cleanup gaps):** `create_working_copy` now wraps `shutil.copy2` in try/except, removing the allocated temp path and raising `DeckSourceError` on any copy failure. Failure injection tests cover all four transaction points (staging copy, save, validation, publication) and confirm: source untouched, no partial/incorrect final destination, no leaked `b4ps_engine_*`/`.b4ps_publish_*` temp file, typed error where applicable.
- **F-06 (medium, missing verification records):** this decision entry plus `openspec/changes/safe-ppt-engine/verification-report.md`, recording Builds 1–2 and this repair's classification, rationale, applicable profiles, commands, results, limitations, and — critically — that Builds 1–2 were marked complete *before* independent audit and *failed* that audit; re-audit of this repair is still required and has not happened.
- **F-07 (low, non-picture target validated after staging):** `replace_picture` now calls `_preflight_resolve_shape` (a throwaway `load_deck` against the original input) and checks `shape_type` before `create_working_copy` is ever called, so an invalid target creates no temp file. The same preflight-resolve pattern was applied uniformly to `set_shape_text` and the shared geometry-mutation path too, as a direct, minimal implementation of the cross-cutting contract's stated order ("validate source + target + path safety" before "stage privately") rather than a change scoped to F-07 alone.
- **F-08 (low, canonical inspection includes absolute path):** `inspect_deck`'s return value no longer includes a `path` key, so two byte-identical `.pptx` files at different filesystem paths now produce an equal result. A new `describe_deck(path)` wraps it as `{"source_path": ..., "structure": inspect_deck(path)}` for callers that want provenance; `engine-inspect`'s CLI/JSON output now uses `describe_deck`, so its JSON shape changed from a flat dict to this nested form (documented, deliberate, and covered by updated CLI tests).

48 new adversarial/fault-injection tests added (`tests/test_ppt_engine_audit_repair.py`), 120 total in the suite. Two pre-existing tests were updated, each with an explicit reason: one `OutputPathError` message-wording assertion (F-01 improved the message text) and the CLI JSON-shape assertions in `test_engine_cli.py`/`test_engine_cli_geometry_image.py` (F-08's `structure`/`source_path` split). No test was weakened; all 72 pre-existing behavioral assertions still hold.

**Reason:** These are safety-critical defects in the exact guarantees the Safe PPT Engine exists to provide before any semantic/AI layer is built on top of it. Fixing them now, with adversarial regression coverage proving each fix, keeps the mechanical foundation trustworthy rather than deferring known gaps into Documentation Intelligence work that would inherit them.

**What was deliberately not done:** no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py` (unrelated baseline modules, untouched); the canonical ZIP was not touched. Independent Codex re-audit of this repair was **not** performed as part of this decision — that is the explicitly required next action, not something Claude can self-certify.

**Status:** Active. Re-audit pending.

---

## PROJ-010 — Safe PPT Engine Audit Repair 2 (re-audit findings F-02, F-04, F-05, F-06, R-01, R-02)

**Decision:** An independent Codex re-audit of Audit Repair 1 confirmed F-01, F-03, F-07, and F-08 correctly closed (no regression), and found 6 findings remaining. All 6 are fixed:

- **F-02 (high, no-overwrite publication not fully atomic):** `_publish_atomically` no longer opens the final `output_path` directly at all for `overwrite=False`. It now writes the complete staged content to a same-directory temp file, flushes and `os.fsync`s it, then publishes via `os.link(tmp, output_path)` - an atomic hard-link create that fails with `FileExistsError` (converted to `OutputPathError`) if the destination already exists, without the destination ever being observable as a partial file (unlike the prior `O_CREAT|O_EXCL` stream, which created the destination filename before any bytes were written into it). The temp file is removed in both the success and failure case. `overwrite=True` is unchanged (atomic `os.replace`).
- **R-01 (high, source-integrity check ran after publication):** a new shared `_finalize_transaction` helper is now the only path to `_publish_atomically` for every mutation primitive: it re-checks the source hash **before** calling `_publish_atomically` (the actual gate - if the source changed, publication is never attempted) and, only as defense-in-depth, once more after. A test asserts the literal call order (`_finalize_transaction` before `_publish_atomically`) for all four geometry/text mutation families, and separate tests prove a tampered source blocks publication entirely, leaving no output and no leaked temp files.
- **F-05 (medium, transaction I/O errors could leak as raw OSError):** a new `TransactionIOError` type normalizes filesystem/library failures during working-copy creation (`create_working_copy`), save (`_save_staged`, used by all three mutation code paths), temp-output preparation, and publication. Writing the adversarial test for this surfaced a genuine, previously-unwrapped bug: `_publish_atomically`'s own `tempfile.mkstemp` call was not inside any try/except, so a permission-denied output directory produced a raw traceback through the CLI - found via a real (unmocked) read-only-directory test, not a mock, and fixed by wrapping that call too.
- **F-04 (medium, relationship cleanup only scanned spTree):** `_drop_relationship_if_unreferenced` now scans `slide._element.iter(qn("a:blip"))` - the entire `<p:sld>` XML tree - instead of only `slide._element.spTree`. This correctly preserves a relationship still used by the slide background (`p:cSld/p:bg/p:bgPr/a:blipFill/a:blip`, a sibling of `p:spTree`, not a descendant of it) or any other non-shape slide element. Verified with a fixture where a foreground picture and the slide background share one relationship id (python-pptx's own default behavior when the same image content is related to the same slide part twice) - replacing the foreground picture leaves the background's relationship and media completely intact, which the prior spTree-only scan would have incorrectly dropped. A crop-preservation regression test (`crop_left`/`crop_top`/`crop_right`/`crop_bottom` before and after replacement) was added alongside it.
- **F-06 (medium, verification/governance evidence incomplete):** added `openspec/changes/safe-ppt-engine/spec.md` - the canonical classification record (base tier, rationale, applicable profiles, explicit audit requirement) `docs/TESTING.md` → "Canonical recording surface" requires live in the change's `spec.md`, not only `verification-report.md`, which was the actual gap. Expanded `verification-report.md` with builder review, founder-preview status (recorded truthfully as **pending** - no human has previewed a generated `.pptx` in real PowerPoint - not fabricated as complete), the Data-sensitive profile's specific gates (migration test and tenancy-isolation N/A with stated reasons; data-preservation and rollback-plan evidence stated), and full commands/results. Added `openspec/changes/safe-ppt-engine/audit-report.md`, the canonical location for independent-audit findings per `docs/PIPELINE.md`, recording the complete audit history (Audit 1 FAIL → Repair 1 → Audit 2 FAIL → Repair 2 → Audit 3 pending) truthfully, without claiming any audit passed that has not.
- **R-02 (low, adversarial tests proved end-state, not mechanism):** strengthened tests to prove the actual mechanism, not just outcomes: a call-count spy on `create_working_copy` proves a non-picture target results in *zero* staging calls (F-07), not merely no leftover temp file; call-order assertions prove the R-01 integrity gate runs strictly before publish for every mutation family; the F-03 missing/empty/corrupt/valid-ZIP-but-not-PPTX matrix now runs against every mutation family (`set_shape_text`, `move_shape`, `resize_shape`, `set_shape_geometry`, `replace_picture`) at both the engine-API and CLI boundary; image-replacement coverage was extended with crop-bearing, shared-picture, and background-sharing fixtures.

65 new tests added (`tests/test_ppt_engine_audit_repair_2.py`), 185 total in the suite. Two pre-existing tests updated with explicit justification (both now assert `TransactionIOError`, the more specific type F-05 introduces, in place of the broader error type they previously accepted) - no test was weakened; explicit regression tests confirm F-01/F-03/F-07/F-08 remain closed.

**Reason:** These were exactly the gaps a second independent audit round exists to catch - a mostly-correct first repair with residual atomicity, sequencing, error-typing, and cleanup-scope defects, plus genuinely incomplete governance recordkeeping. Fixing them, with tests that prove the mechanism rather than only the outcome, keeps the mechanical foundation trustworthy before any further capability is built on it.

**What was deliberately not done:** no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the canonical ZIP was not touched; no founder approval or independent-audit-pass was fabricated - both the founder-preview and the Audit 3 re-audit gates are recorded as genuinely pending.

**Status:** Active. Subsequently re-audited (Audit 3); see PROJ-011.

---

## PROJ-011 — Safe PPT Engine Audit Repair 3 (Audit 3 findings F-05, F-06, R-01, R-02) — local only, not merged

**Decision:** An independent Codex re-audit of Audit Repair 2 (Audit 3, against merged commit `69bb4761921ac7f7b843beb04ab255a2b272865e`) confirmed F-01, F-02, F-03, F-04, F-07, and F-08 correctly closed with no regression, and found 4 findings remaining. Per explicit task instruction, all 4 are fixed as **local, uncommitted working-tree changes only** - not committed, pushed, or opened as a PR, so `main` is unaffected by this decision and still reflects Audit Repair 2.

- **F-05 (wrap `create_working_copy`/`mkstemp` and cleanup failures; cleanup failure must not be silently swallowed):** `create_working_copy` now wraps its `tempfile.mkstemp` call (previously unwrapped) as well as the `shutil.copy2` call in typed-error handling. A new `_remove_reporting_failure(path)` helper replaces the previous `_safe_remove`, which discarded cleanup `OSError`s with a bare `pass`; the new helper never itself raises, but returns a description of any removal failure, which every caller folds into the error it is already raising (or, when nothing else is failing, raises on its own). A new `_staged_copy` context manager centralizes the create/cleanup pairing for every mutation primitive: if the wrapped mutation raised, a cleanup failure is reported as a stderr warning (visible, but not replacing the primary exception's type); if the wrapped mutation succeeded, a cleanup failure is itself raised as `TransactionIOError`, since a leaked temp file is then the only remaining problem and must not be silently accepted as success. The same reporting-not-swallowing treatment was applied to every cleanup call site inside `_publish_atomically`.
- **R-01 (source integrity must be checked immediately before publication; remove the post-publication check as a failure gate; source deletion/read errors during the pre-publish check must be typed; once atomic publication succeeds, the transaction is successful):** `_finalize_transaction` now calls a new `_verify_source_hash_or_raise(input_path, before_hash)` exactly once, immediately before `_publish_atomically`, and nothing after it. The previous second hash comparison that ran *after* `_publish_atomically` returned - the "defense-in-depth" check from Audit Repair 2 - has been removed entirely, per explicit instruction that it must not remain a failure gate: once publication succeeds, the transaction is complete, and re-checking afterward could only produce a confusing partial-success-that-raises state, never actually protect anything (the publish had already happened). `_verify_source_hash_or_raise` itself now distinguishes a source that is unreadable/deleted (typed `DeckSourceError`, since that is a read failure, not a content mismatch) from a source that is readable but has changed (typed `SafeDeckError`, the existing safety-invariant-violation case) - previously, `file_sha256` could raise a bare `OSError` at this exact point if the source vanished between the initial hash and this check.
- **R-02 (add regressions for the exact residual cases above):** 19 new local tests (`tests/test_ppt_engine_audit_3.py`) cover: `mkstemp` failure during both staging and publication; a cleanup failure that is itself induced (via mocking `os.remove`) on both the "mutation also failed" path (proving the secondary failure is reported, not substituted for the primary exception type) and the "mutation succeeded" path (proving the cleanup failure becomes the reported outcome, not silently accepted); source deletion and source tampering exactly at the pre-publish integrity boundary, parametrized across every mutation family (`set_shape_text`, `move_shape`, `resize_shape`, `set_shape_geometry`) plus a dedicated pair of tests proving `replace_picture` goes through the identical single integrity gate; and a call-order assertion proving the integrity check runs exactly once, immediately before (never after) `_publish_atomically`. All 185 pre-existing tests pass unmodified (204 total).

Governance recordkeeping (F-06) was also corrected this round, per explicit instruction: `spec.md` now states outright, under a "Process deviation" heading, that its classification was written retroactively (after Builds 1-2 were already implemented and merged) rather than presenting it as though it happened at the normal pre-implementation time; no approval is invented anywhere in `spec.md`'s "Approval" section (still truthfully "Not approved - no founder sign-off has occurred"); `verification-report.md` now states the *actual* recorded CI result for the last pushed round (Audit Repair 2 / PR #7: GitHub Actions `validate` passed, confirmed via `gh pr checks` and the merge-commit run) in place of the prior placeholder "expected to pass... updated once inspected" language that was never actually updated; `audit-report.md`'s verdict table now includes Audit 3's actual result and a fourth (not-yet-performed) row for Audit 4; and founder-preview status remains explicitly recorded as pending in both files, unchanged by this round.

**Reason:** A third independent audit round finding a narrower but still real set of defects (a silently-discarded cleanup failure that could mask data loss context, and an integrity check sequenced so a corrupted-transaction case could theoretically still report success after publication had already happened) is exactly the kind of residual risk that should not go unfixed just because two prior rounds already passed. Fixing it, and correcting the governance record to state plainly what has and has not been independently verified, keeps both the code and its paper trail honest before a fourth audit round and before any further capability is built on this engine.

**What was deliberately not done:** no commit, push, PR, or merge - these changes exist only in the local working tree, per explicit instruction, so Codex can audit them directly; no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the canonical ZIP was not touched (its SHA-256 was re-verified); no founder approval or independent-audit-pass was fabricated - the founder-preview gate and the pending Audit 4 are both recorded as genuinely open.

**Status:** Active, local only. Subsequently re-audited (Audit 4, directly against this local diff — no push required); see PROJ-012.

---

## PROJ-012 — Safe PPT Engine Audit Repair 4 (Audit 4 findings F-05, F-06, R-02) — local only, not merged

**Decision:** An independent Codex re-audit of Audit Repair 3's local, uncommitted diff (Audit 4 — reviewed directly, without requiring a push or PR) confirmed R-01 correctly closed, and found 3 findings remaining. Per explicit task instruction, all 3 are fixed as **local, uncommitted working-tree changes only** - not committed, pushed, or opened as a PR, so `main` is unaffected by this decision and still reflects Audit Repair 2.

- **F-05 (`create_working_copy` must type/clean up `os.close(fd)` failures too; no raw `OSError`, no leaked temp pathname):** `create_working_copy`'s `os.close(fd)` call - previously the one remaining unwrapped I/O call in the function, since only `tempfile.mkstemp` and `shutil.copy2` had been wrapped in Audit Repair 3 - is now wrapped in its own `try/except OSError`. On failure it calls `_remove_reporting_failure(staged)` to remove the temp file `mkstemp` had already created on disk (folding any secondary removal failure into the message), then raises typed `TransactionIOError`. Previously a close failure (e.g. `EBADF`/`EIO` on some filesystems) would surface as a raw, untyped `OSError` at the CLI and leak the already-created temp file, since nothing downstream would ever remove a path that `create_working_copy` never returned. **This round verified and fixed the temp-pathname leak only - it did not verify that the underlying file descriptor itself stayed closed after a reported close failure, and made no claim either way.** That narrower descriptor-leak question was raised by Audit 5 and is fixed in PROJ-013.
- **F-06 (correct governance records to reflect the actual workflow; remove statements saying Audit 4 requires a push; do not call N/A gates "approved" unless an approving authority is actually recorded):** every governance record's claim that "Audit 4... cannot occur until they are pushed" was factually contradicted by this very task - Audit 4 audited exactly such a local, uncommitted diff. `spec.md`, `verification-report.md`, `audit-report.md`, and `docs/CURRENT.md` were all corrected to state plainly that Codex reviewing a local diff directly (demonstrated first by Audit 4) is a normal, exercised part of this workflow, and that only the actual next step (Audit 5, not yet performed) remains open - with no claim that a push is a precondition for it. Separately, `verification-report.md`'s Data-sensitive profile table previously labeled its two not-applicable gates (migration test, tenancy-isolation check) as "N/A (approved)" - no approving authority reviewed or recorded approval of either determination, so "approved" was an invented status. Both rows now read plain "N/A" with the factual reason, explicitly noting no approving authority is recorded for the determination.
- **R-02 (add regressions for the `os.close` failure; make cleanup-warning tests assert the warning/output they claim to verify; correct any overstated coverage wording):** a new `test_f05_create_working_copy_os_close_failure_typed_and_no_leak` test selectively patches `os.close` to fail only for the fd `mkstemp` actually returned (so pytest's own file descriptors are unaffected), asserting both the typed `TransactionIOError` and that the temp file is not left behind. Two pre-existing Repair-3 tests - `test_f05_staged_copy_cleanup_failure_on_primary_failure_does_not_mask_it` and `test_f05_publish_temp_cleanup_failure_reported_on_link_exists_race` - had docstrings claiming a secondary cleanup failure "is reported (as a warning)" but only asserted the raised exception's type, never the warning text actually printed; both now use `capsys` to assert the literal stderr warning text, closing the gap between claimed and actual coverage. No existing test was weakened - these are additive assertions on already-passing tests.

1 new local test added (`tests/test_ppt_engine_audit_3.py`), 205 total in the suite (up from 204); the 2 strengthened tests are counted within that total, not as new tests. All 204 pre-existing tests still pass, with only those 2 gaining an additive assertion.

**Reason:** A fourth independent audit round finding a narrow but real residual error-typing gap (the exact same class of "one more unwrapped I/O call" defect the F-05 lineage keeps surfacing, this time at `os.close` rather than `mkstemp`/`copy2`) plus two governance-recordkeeping defects - one where the record's own language was already contradicted by the very audit round that flagged it, and one where "N/A" had drifted into implying an approval that never happened - is exactly the kind of residual risk that should not go unfixed just because three prior rounds already passed. Fixing it, and correcting the governance record to describe the workflow this project actually follows rather than an assumed and inaccurate one, keeps both the code and its paper trail honest before a fifth audit round.

**What was deliberately not done:** no commit, push, PR, or merge - these changes exist only in the local working tree, per explicit instruction, so Codex can audit them directly, exactly as it did for Audit 4; no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the canonical ZIP was not touched (its SHA-256 was re-verified); no founder approval or independent-audit-pass was fabricated - the founder-preview gate and the pending Audit 5 are both recorded as genuinely open, and the two Data-sensitive N/A gates no longer claim an approval that was never recorded.

**Status:** Active, local only. Subsequently re-audited (Audit 5, directly against this local diff — no push required); see PROJ-013.

---

## PROJ-013 — Safe PPT Engine Audit Repair 5 (Audit 5 findings F-05, F-06, R-02) — local only, not merged

**Decision:** An independent Codex re-audit of Audit Repair 4's local, uncommitted diff (Audit 5 - reviewed directly, without requiring a push or PR) found 3 findings remaining, all narrower than the round before. Per explicit task instruction, all 3 are fixed as **local, uncommitted working-tree changes only** - not committed, pushed, or opened as a PR, so `main` is unaffected by this decision and still reflects Audit Repair 2.

- **F-05 (when the staging descriptor close path fails, ensure the descriptor itself is not leaked; must still return typed `TransactionIOError`, remove the temp pathname, and leave the fd actually closed):** Repair 4 wrapped `create_working_copy`'s `os.close(fd)` call in a typed error and removed the temp pathname on failure, but never verified the fd itself stayed closed - POSIX `close(2)` deallocates a process's fd slot on return even when it reports an error (e.g. a delayed write-back failure surfacing as `EIO`), which is normally why retrying `close()` is unsafe (the fd number may already have been reused by something else). `create_working_copy` now handles the narrow case where that guarantee doesn't hold: on a close failure it calls `os.fstat(fd)` to check whether the descriptor is still genuinely open, and only if it is, retries `os.close(fd)` once (swallowing any further failure from that retry, since the original failure is already what's being reported). This retry is safe specifically because a `mkstemp`-returned fd is not yet shared with any other code at this point in the function, so `fstat` finding it "still open" cannot be a false positive caused by an unrelated file reusing the same number. The temp pathname is still removed via `_remove_reporting_failure` and folded into the raised `TransactionIOError`, exactly as before. **Correction (Audit 6): the "this retry is safe" reasoning in the paragraph above was wrong and is retracted, not merely qualified.** The fd being "not yet shared with any other code" only holds until the moment `close()` fails; nothing prevents another thread, signal handler, or library elsewhere in the process from opening a new file and being handed that exact fd number back before this function's `fstat`/retry runs. Audit 6 found this defect and PROJ-014 removes the retry entirely rather than trying to make it safer.
- **R-02 (strengthen the regression to verify descriptor closure, not just pathname removal; must fail if `os.fstat(fd)` still succeeds after the operation):** `test_f05_create_working_copy_os_close_failure_typed_and_no_leak` (`tests/test_ppt_engine_audit_3.py`) was rewritten from a mock that always fails `os.close` for the target fd (which could never actually prove closure, since it never let a close succeed) to a stateful mock that fails only the first call and lets a second, retried call actually close the fd - modeling the real "close() failed to report success once" scenario the fix targets. The test now asserts three things: the typed error is raised, the temp pathname was removed, and - the actual descriptor-leak regression - `create_working_copy` retried the close exactly once and `os.fstat(fd)` raises `OSError` afterward, proving the fd is genuinely closed. Confirmed this test fails against the pre-fix code (no retry attempted, so the mocked close call count stays at 1 and `os.fstat(fd)` still succeeds). **Correction (Audit 6): this test proved a retry happened and that the fd eventually became closed - it never proved the fd being retried was still safely the engine's own descriptor at the moment of the retry, which is exactly the property that mattered.** It was structurally incapable of catching the fd-reuse risk Audit 6 found. PROJ-014 replaces it.
- **F-06 (correct remaining governance overstatements - do not claim "no staging resource leaks" until this fix is verified; fix `spec.md` saying "two rounds" when the audit history now records four completed audits before this repair):** `spec.md`'s Classification → Rationale line, which still read "two rounds of independent audit" from before this engine's audit history grew past Audit 2, now reads "four completed rounds of independent audit as of this record, with a fifth pending." Separately, `docs/CURRENT.md`'s and `docs/DECISIONS.md`'s (PROJ-012) write-ups of Audit Repair 4's F-05 fix both said or implied "no leaked staging resource remains" - true only of the temp *pathname*, which was the one thing Repair 4 actually verified; whether the file descriptor itself could still leak was never checked, and Audit 5 found that it could. Both records were corrected to state plainly that Repair 4 closed the pathname leak only, and to point to this entry for the descriptor-leak fix - rather than let the overstatement stand uncorrected now that it's known to have been one.

1 test strengthened in place (no new test file); 205 total in the suite (unchanged from Audit Repair 4 - the strengthening added assertions to an existing test rather than adding a new one). All 204 other pre-existing tests pass unmodified.

**Reason:** A fifth independent audit round finding that the previous round's fix addressed the visible symptom (an untyped error, a leaked file on disk) but not the full claim implied by its own commit message (no staging resource leaked) is exactly the kind of gap that matters for infrastructure whose entire purpose is not corrupting or leaking resources tied to an irreplaceable source file. Fixing the descriptor-closure gap itself, strengthening the regression to actually detect it, and correcting the governance record to stop repeating a claim that was never verified, keeps both the code and its paper trail honest before a sixth audit round.

**What was deliberately not done:** no commit, push, PR, or merge - these changes exist only in the local working tree, per explicit instruction, so Codex can audit them directly, exactly as it did for Audits 4 and 5; no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the canonical ZIP was not touched (its SHA-256 was re-verified); no founder approval or independent-audit-pass was fabricated - the founder-preview gate and the pending Audit 6 are both recorded as genuinely open.

**Status:** Active, local only. Subsequently re-audited (Audit 6, directly against this local diff - no push required); found this repair's own F-05 fix unsafe. See PROJ-014.

---

## PROJ-014 — Safe PPT Engine Audit Repair 6 (Audit 6 findings F-05, F-06, R-02) — local only, not merged

**Decision:** An independent Codex re-audit of Audit Repair 5's local, uncommitted diff (Audit 6 - reviewed directly, without requiring a push or PR) found 3 findings, all concerning Repair 5's own F-05 fix and the governance record describing it. Per explicit task instruction, all 3 are fixed as **local, uncommitted working-tree changes only** - not committed, pushed, or opened as a PR, so `main` is unaffected by this decision and still reflects Audit Repair 2.

- **F-05 (remove the unsafe `fstat(fd)` + retry-`close(fd)` logic; after `os.close(fd)` reports an error, do not touch that fd number again; clean the temp pathname if possible; raise typed `TransactionIOError`; document that descriptor state after a failed close is not safely recoverable by retrying):** the `os.fstat(fd)`-then-retry block Repair 5 added to `create_working_copy`'s close-failure path is removed entirely. `create_working_copy` now does exactly what it did in Audit Repair 4 for the close-failure path: on `OSError` from `os.close(fd)`, it removes the temp pathname via `_remove_reporting_failure` and raises typed `TransactionIOError` - it never calls `os.fstat` or `os.close` on that fd again. The function's docstring and an inline comment on the `except` block now explain why: once `close()` has reported an error, the fd's state is ambiguous even where POSIX's "deallocated regardless of error" guarantee holds, because something else in the process (another thread, a signal handler, a library, the interpreter's own finalizers) may already have been handed that exact fd number back by the time control returns here - there is no way to distinguish "still ours" from "already reused" without racing that possibility, so the code deliberately does not try.
- **R-02 (replace the artificial retry test with a regression proving the engine never performs a second close on the same fd after a close error, including an fd-reuse adversarial case):** `test_f05_create_working_copy_os_close_failure_typed_and_no_leak` is replaced by two tests. `test_f05_create_working_copy_os_close_failure_typed_and_never_retouches_fd` spies on both `os.close` and `os.fstat`, fails the target fd's close exactly once (while genuinely releasing it, matching the code's own documented assumption), and asserts the engine calls `os.close`/`os.fstat` on that fd exactly once total - never again after the failure. `test_f05_create_working_copy_close_failure_does_not_touch_reused_fd` is the adversarial case the finding specifically named: inside the mocked close failure, it genuinely opens a second, unrelated real file, which reliably reuses the exact fd number just freed (the lowest-available-fd behavior of Linux/macOS); the test then asserts the engine never calls `os.close`/`os.fstat` on that number again, and separately proves the reused file is still open and writable afterward - i.e. the engine did not blindly act on a descriptor it no longer owns. Both tests were confirmed to fail against the pre-fix (retry) code: reinstating the `fstat`/retry block causes both to observe a second `close`/`fstat` call, which is exactly the defect Audit 6 flagged.
- **F-06 (correct the audit chronology and remove every claim that Audit 5 closed F-05/F-06/R-02, or that the retry strategy was proven safe; record Audit 6 = FAIL, Audit 7 = PASS WITH FINDINGS, Repair 7 local, Audit 8 pending):** `audit-report.md`'s Audit 5 row is corrected: it no longer characterizes Audit 5 as having "confirmed" Repair 4's F-06/R-02 fixes closed - Audit 5's own scope was Repair 4, and no later round has independently re-examined those two fixes on their own, so recording them as confirmed-closed was more than Audit 5 itself established. A new Audit 6 row records **FAIL** with this round's 3 findings, and the Audit 7 row records **PASS WITH FINDINGS** and the Audit 8 row is pending. `verification-report.md` and `spec.md` are updated the same way (round counts, file title, acceptance-criteria and audit-requirement prose). `docs/DECISIONS.md`'s PROJ-013 entry (above) gets an explicit correction note on its F-05 and R-02 bullets stating the "this retry is safe" reasoning and the retry-proving test were both wrong, rather than leaving them to stand as if still accurate history.

1 test removed, 2 added (net +1); 206 total in the suite. All other pre-existing tests pass unmodified.

**Reason:** A sixth independent audit round finding that the *fix* for the previous round's finding was itself a resource-safety defect - not a residual gap in an otherwise-sound fix, but a new risk introduced by the fix's own retry logic - is a more serious class of problem than any prior round in this history: it demonstrates that "reasoned as safe and documented as such" is not the same as "independently verified as safe," which is the entire premise for requiring audit rounds this far past the point where the code looked correct. Removing the retry entirely (rather than trying to patch it into being provably race-free, which would require synchronization primitives this single-threaded-caller function has no reason to carry) is the accuracy-over-cleverness choice: the engine now honestly reports "could not confirm this fd is closed" via a typed error, rather than attempting a recovery whose own correctness cannot be locally proven.

**What was deliberately not done:** no commit, push, PR, or merge - these changes exist only in the local working tree, per explicit instruction, so Codex can audit them directly, exactly as it did for Audits 4, 5, and 6; no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the canonical ZIP was not touched (its SHA-256 was re-verified); no founder approval or independent-audit-pass was fabricated - the founder-preview gate and the pending Audit 8 are both recorded as genuinely open; no attempt was made to build a "safer" retry (e.g. locking, PID/generation-tagging the fd) since that would add unverified complexity to work around a problem better solved by simply not retrying.

**Status:** Active, local only. Subsequently re-audited (Audit 7, directly against this local diff - no push required); found F-06 and R-02 remaining (F-05 confirmed closed). See PROJ-015.

---

## PROJ-015 — Safe PPT Engine Audit Repair 7 (Audit 7 findings F-06, R-02) — local only, not merged

**Decision:** An independent Codex re-audit of Audit Repair 6's local, uncommitted diff (Audit 7 - reviewed directly, without requiring a push or PR) confirmed F-05 (the removal of the unsafe `fstat`/retry logic) correctly closed, and found 2 findings remaining, both about this record's own accuracy rather than about `create_working_copy`'s behavior. Per explicit task instruction, both are fixed as **local, uncommitted working-tree changes only** - not committed, pushed, or opened as a PR, so `main` is unaffected by this decision and still reflects Audit Repair 2.

- **F-06 (correct the stale `docs/DECISIONS.md` heading so the chronology truthfully says Audit 6 failed and Audit 7 completed with findings / next re-audit pending as appropriate):** PROJ-014's own `**Status:**` line (above) still read "Independent re-audit (Audit 7) pending" even though, per this task, Audit 7 had already occurred and found these two findings - a stale-heading defect of the exact same shape this history has now corrected twice before (PROJ-012's and PROJ-013's status lines, corrected in earlier rounds). It now reads "Subsequently re-audited (Audit 7...); found F-06 and R-02 remaining (F-05 confirmed closed). See PROJ-015." `audit-report.md` already correctly recorded Audit 6 as **FAIL** and Audit 7 as **PASS WITH FINDINGS** after the prior round; this finding was specifically about `docs/DECISIONS.md`'s own status line lagging behind that, not about `audit-report.md`.
- **R-02 (strengthen the fd-reuse regression to explicitly assert that the unrelated file actually reused the exact failed descriptor number before testing that the engine leaves it untouched):** `test_f05_create_working_copy_close_failure_does_not_touch_reused_fd` (`tests/test_ppt_engine_audit_3.py`) relied on the OS's lowest-available-fd behavior to make the unrelated file reuse the exact number the engine's failed `close()` freed, but never asserted that this actually happened. If that assumption had silently failed to hold on some platform or under some allocator state, the test's main assertion (`create_working_copy` never touches the fd again) would have passed vacuously - proving only that the engine leaves some *other*, non-reused fd alone, which every other test already covers, not that it correctly avoids a genuinely reused one. The test now asserts `reused["fd"] == failing_fd["fd"]` immediately after the mocked operation, before the no-touch assertion, with a message explaining exactly what a failure of that assumption would mean. Confirmed stable across 5 consecutive runs on this platform (macOS/APFS) - the reuse assumption holds reliably here.

No engine behavior was changed: the strengthened test still passes against the current `create_working_copy`, confirming Repair 6's fix is correct and that the previous round's adversarial premise did in fact hold in practice - it just wasn't verified by the test itself. Per the task instruction, since the strengthened test did not expose a real defect, `lib/ppt_engine.py` is untouched this round.

0 tests added or removed; 1 existing test strengthened in place (an added assertion, not a rewrite of its scenario); 206 total in the suite, unchanged from Audit Repair 6. All other pre-existing tests pass unmodified.

**Reason:** A seventh independent audit round finding that a governance record's own status line had gone stale relative to the very event that triggered writing it, and that an adversarial regression's core adversarial premise was assumed rather than checked, are both instances of the same underlying discipline this whole audit history has been enforcing on the code: don't claim more than what was actually verified. Applying that same discipline to the paperwork and the test's own assumptions, not just to `create_working_copy`'s logic, keeps the full audit trail trustworthy going into an eighth round.

**What was deliberately not done:** no commit, push, PR, or merge - these changes exist only in the local working tree, per explicit instruction, so Codex can audit them directly, exactly as it did for Audits 4 through 7; no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory or Documentation Intelligence work; no attempt to obtain or fabricate the 8 missing production LFS binaries; no refactor of `lib/deck.py`, `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, or `lib/geometry.py`; the canonical ZIP was not touched (its SHA-256 was re-verified); no change to `create_working_copy`'s behavior, since the strengthened test did not expose a defect in it - only in the test's own unverified assumption; no founder approval or independent-audit-pass was fabricated - the founder-preview gate and the pending Audit 8 are both recorded as genuinely open.

**Status:** Active, local only. Independent re-audit (Audit 8) pending. It does not require these changes to be pushed first - Audits 4 through 7 already demonstrated Codex reviewing a local, uncommitted diff directly.

---

## PROJ-016 — Editorial Memory Slice 1

**Decision:** Editorial Memory is the next approved implementation milestone in locked Architecture v0.1, per the founder's lifecycle clarification (living/evolving/version-aware/provenance-preserving/history-preserving knowledge layer) and the subsequently accepted design audit recorded in `docs/EDITORIAL_MEMORY_IMPLEMENTATION_AUDIT.md` (committed here for the first time as part of this change - it had previously existed only as local, uncommitted design work, never merged). Slice 1 - the smallest real implementation proving the lifecycle end-to-end, per explicit task instruction not to build a broad knowledge platform - is implemented, tested, and integrated.

- **Three entities, no ontology:** `Evidence` (an artifact/observation, never itself knowledge), `KnowledgeItem` (the durable subject, identified by a stable natural key), `KnowledgeState` (one versioned claim about a `KnowledgeItem`, always grounded in evidence). No graph semantics, no universal semantic IDs beyond ordinary persistence identity, no vector or graph database.
- **Core invariant enforced in code:** Evidence never promotes itself into Knowledge. `propose_state` raises typed `MissingProvenanceError` if no evidence is cited and typed `UnknownEvidenceError` if a cited evidence id doesn't exist. The only way a state becomes `current` is an explicit `approve_state` call - no evidence type, including browser observation, auto-promotes.
- **Supersession vs. invalidation kept distinct:** approving a new state moves the prior current state to `superseded` (never deleted, always traceable via `superseded_by`); `invalidate_state` is a separate operation for knowledge that was never trustworthy, requiring no replacement to exist. Both statuses remain in `get_history` forever.
- **Unresolved conflict is a first-class, minimal concept:** a proposed state whose `relation_to_previous` is `contradicts` has a computed `review_required` property (never a stored field that could drift), surfaced via `has_pending_conflict`/`get_conflicts`. The existing current state is untouched until an explicit approval resolves it - a newer, contradicting proposal never silently wins by virtue of being newer.
- **No scoring anywhere:** `evidence_quality` (`high`/`medium`/`low`) is artifact-reliability metadata, not a truth score. `get_evidence_type_summary` preserves per-state source-category counts (e.g. distinguishing "browser + maintainer + screenshot" from "browser + browser + browser") purely as information, with no ranking or automatic weighting.
- **Persistence:** plain, atomically-written (temp file + rename) JSON files under a caller-chosen directory - one file per `Evidence` record, one per `KnowledgeItem` (embedding its full append-only state history). No new dependency (Python standard library only), no database, git-diffable like the rest of this repository. `EditorialMemory` reads fresh from disk on every call rather than caching, so reload-from-storage correctness holds by construction.
- **Feature-area naming convention** (`<platform>.<feature>`, e.g. `desktop.filters`) and **optional editorial `rationale`** on proposed states, per the founder-approved amendments, both implemented as plain fields - no registry, no taxonomy.

26 new tests (`editorial-memory/tests/test_editorial_memory.py`), including one deterministic 15-step end-to-end test proving the full evidence → item → state → approval → current-retrieval → new-unapproved-evidence → supersession → contradiction/review-required flow in a single run, matching the required Slice 1 lifecycle scenario exactly. `python scripts/check_pipeline.py` passes; the Safe PPT Engine (`baseline/B4PS-TUT-main/.b4ps-tools/`) was not touched.

**Reason:** Editorial Memory exists to hold claims about Bridge4PS without ever letting an artifact's mere existence be mistaken for approved current truth - exactly the guarantee the founder's lifecycle clarification and the "knowledge system, not a truth system" framing describe. Building the smallest version that proves this end-to-end, before any broader platform work, keeps the same safety-first discipline established for the Safe PPT Engine: a mechanical, testable foundation first, semantic/AI capability only after independent audit confirms the foundation is trustworthy.

**What was deliberately not done:** no Documentation Intelligence, no AI reasoning over memory, no affected-slide detection, no semantic red-box targeting, no browser automation or live `browser_observation` producer, no vector search, no embeddings, no graph database, no ontology, no generalized Bridge4PS Product Knowledge platform, no UI, no Freshdesk/AppSheet integration, no automatic ingestion from old PPTs, and no automatic promotion of evidence into truth; no CLI (nothing calls this API yet); no purge/hard-deletion tooling (deferred per the already-approved design - normal supersession/invalidation is the only lifecycle mechanism this slice implements); no modification to Safe PPT Engine behavior; no redesign of Architecture v0.1; no Slice 2 work, per explicit instruction to stop after this slice.

**Status:** Active, merged. Subsequently audited (Audit 1); found EM-01, EM-02, EM-03 (plus a related JSON-corruption-normalization gap). See PROJ-017.

---

## PROJ-017 — Editorial Memory Repair 1 (Audit 1 findings EM-01, EM-02, EM-03) — local only, not merged

**Decision:** An independent Codex audit of merged Editorial Memory Slice 1 (Audit 1, against `93e87239ea9b55185f374f1a3aa2a390be03c24f`) found 3 findings, plus a related JSON-corruption-normalization gap noted in the same pass. Per explicit task instruction, all are fixed as **local, uncommitted working-tree changes only** - not committed, pushed, or opened as a PR, so `main` is unaffected by this decision and still reflects merged Slice 1.

- **EM-01 (`get_current()` must not return a state whose Evidence provenance is missing/corrupt; raise a typed error):** `get_current` now re-resolves every evidence id a current state cites, on every call, via the same `get_evidence` path used elsewhere - no separate, divergent check. A missing or corrupt reference raises typed `CorruptProvenanceError`, chaining the underlying `UnknownEvidenceError`/`StorageCorruptionError` via `__cause__` so the specific problem stays discoverable. A superseded or invalidated state's evidence going missing does not affect `get_current` at all - only the currently-`current` state's provenance is checked, since that is the only state this call claims is trustworthy.
- **EM-02 (prevent natural-key slug collisions such as `a.b` vs `a-b`; keep deterministic stable identity):** `slugify` remains lossy by design (rewriting it losslessly would be a bigger encoding change than this finding calls for) - instead, `get_or_create_knowledge_item` now compares the stored `key` against the requested `key` whenever an item already exists at the computed slug. A mismatch means two distinct natural keys collided into one id; the call now raises typed `KeyCollisionError` rather than silently returning the existing item under a different logical identity. The same key reused remains fully idempotent (unchanged from Slice 1) - only a *different* key landing on the same slug is rejected.
- **EM-03 (prevent `feature_area` path traversal/escape; stored paths must remain inside the configured memory root):** `feature_area` is now validated against a safe `<platform>.<feature>` pattern (`store.validate_feature_area`) before it is ever joined into a filesystem path - rejecting `/`, `\`, `..`, empty segments, and leading/trailing dots outright. A second, defense-in-depth check in `_knowledge_path` resolves the final path and confirms it still lands inside the configured `knowledge/` directory, catching the narrower case where a symlink already present under the root could otherwise let an already-safe-looking value resolve outside it.
- **JSON-corruption normalization (narrow, related finding from the same audit pass):** `load_evidence`/`load_knowledge_item` now catch `json.JSONDecodeError` and re-raise as typed `StorageCorruptionError`, so a corrupted or truncated record file on disk surfaces as a typed Editorial Memory error rather than a raw `json.JSONDecodeError` escaping the module's own error taxonomy.

23 new adversarial tests added (`editorial-memory/tests/test_editorial_memory.py`; 49 total in the suite, all 26 pre-existing Slice 1 tests passing unmodified): 4 for EM-01 (deleted evidence, corrupt evidence, a healthy-state regression guard, and a guard proving only the *current* state's provenance is checked, not history), 4 for EM-02 (both collision orderings, idempotent reuse still working, and collision detection surviving a simulated process reload), 11 for EM-03 (a 10-value parametrized path-traversal/escape matrix plus a check that a rejected call touches no file anywhere, and a regression guard for legitimate multi-segment feature areas), and 3 for JSON-corruption normalization (corrupt `KnowledgeItem`, corrupt `Evidence`, and a truncated file). Two of the EM-01 tests were confirmed to fail against `get_current`'s pre-repair logic specifically (with the new error types otherwise available) - not merely against missing symbols - establishing them as genuine regressions rather than tautologies; the same holds more broadly, since the entire new test file fails to even import against the unmodified pre-repair `lib/` files (the new exception types don't exist yet).

Separately, per explicit instruction, `docs/CURRENT.md` wording claiming Safe PPT Engine Audit Repair 7 remained uncommitted/unintegrated was corrected: Git history (`40f3a1083bf8556830abf9552fb186f629412c5c`, merged as `be6df7fdfe8f6ab4cad24f4c3c3a7408945cb715`, PR #8) shows Repairs 3 through 7 were already committed and integrated together as a single change. This is a documentation correction unrelated to Editorial Memory's own code, flagged in the same audit pass and fixed here since it was explicitly named in this task's scope.

`python scripts/check_pipeline.py` still passes; `git diff --check` passes; the canonical ZIP was not touched; Safe PPT Engine code (`baseline/B4PS-TUT-main/.b4ps-tools/`) was not touched.

**Reason:** Audit 1 finding three real defects on Editorial Memory's very first independent pass - including one (EM-03) with genuine security relevance, not just a lifecycle-logic gap - is exactly the kind of result that justifies requiring independent audit before trusting a new data layer's safety claims. Fixing all three narrowly, adding adversarial coverage confirmed to fail against the pre-repair code, and correcting an unrelated-but-noticed stale governance claim in the same pass, keeps both the code and its paper trail honest before a second audit round.

**What was deliberately not done:** no commit, push, PR, or merge - these changes exist only in the local working tree, per explicit instruction, so Codex can audit them directly; no redesign of Architecture v0.1; no product-scope expansion; no Editorial Memory Slice 2 work; no modification to Safe PPT Engine code; no fix to the analogous, narrower `evidence_id` path-construction pattern in `store.py` (not part of Audit 1's EM-03 finding, which named `feature_area` specifically - evidence ids are always internally generated, never caller-supplied, so the exposure is narrower; noted as a limitation for a future audit pass rather than silently fixed or silently ignored); no attempt to make `slugify` itself collision-proof by construction (a bigger encoding change than this finding calls for) - detecting and rejecting a collision was the smallest sufficient fix; no fabricated audit-pass claim - Audit 2 is recorded as genuinely not yet performed.

**Status:** Active, local only. Subsequently re-audited (Audit 2): **PASS WITH FINDINGS** - EM-01, EM-02, and EM-03 are confirmed closed; a new, narrower finding (EM-04) was raised. See PROJ-018.

---

## PROJ-018 — Editorial Memory Repair 2 (Audit 2 finding EM-04) — local only, not merged

**Decision:** Independent Codex re-audit of Repair 1's local diff (Audit 2, against the uncommitted working tree still based on `93e87239ea9b55185f374f1a3aa2a390be03c24f`) returned **PASS WITH FINDINGS**: EM-01, EM-02, and EM-03 are confirmed closed by Repair 1. One new finding, EM-04, was raised - the analogous unvalidated-path pattern in `store._evidence_path()` that Repair 1's own verification report had already flagged as a known, deliberately-unfixed limitation (narrower exposure, not named in Audit 1). Fixed here, per explicit task instruction, as a further **local, uncommitted working-tree change only** - not committed, pushed, or opened as a PR; `main` is unaffected and still reflects merged Slice 1.

- **EM-04 (`evidence_id` must not let a caller-controlled or persisted value escape the configured memory root):** `store.py` gains `validate_evidence_id()`, the same shape of allowlist check as EM-03's `validate_feature_area()` - a plain lowercase-alphanumerics/hyphens token, matching exactly what `record_evidence()` itself generates (`"ev-" + uuid hex`). `_evidence_path()` now calls it before any path is built, raising typed `InvalidEvidenceIdError` on rejection, plus the same defense-in-depth resolved-path containment check already used by `_knowledge_path()`. Because `get_current()`'s existing EM-01 provenance re-check already wraps each evidence lookup in a generic `except EditorialMemoryError`, a persisted-but-tampered `evidence_refs` entry naturally surfaces as `CorruptProvenanceError` (chaining `InvalidEvidenceIdError` via `__cause__`) with no change to `memory.py` required.
- Direct calls to `get_evidence()` with a malicious id (not just ones reached indirectly through a current state) are rejected the same way, since the validation lives in the shared `_evidence_path()` every read/write already goes through.

10 new adversarial tests added (`editorial-memory/tests/test_editorial_memory.py`; 59 total in the suite, all 49 prior tests passing unmodified): a 6-value parametrized path-traversal/escape matrix (`../../escape`, an absolute path, forward- and backslash-traversal, a bare `..`, a nested `a/b`), a check that a rejected call touches no file outside the root, a regression guard proving a real generated evidence id keeps working, a test proving a persisted malicious `evidence_refs` entry is caught via `get_current` as `CorruptProvenanceError` chaining `InvalidEvidenceIdError`, and a reload test proving the validation survives a simulated fresh process. All 9 negative-path/traversal tests were confirmed to fail against the pre-repair `_evidence_path()` specifically (not merely against a missing symbol) before being accepted.

`python scripts/check_pipeline.py` still passes; `py_compile` is clean; `git diff --check` passes; Safe PPT Engine code (`baseline/B4PS-TUT-main/.b4ps-tools/`) was not touched; no Slice 2 work was started.

**Reason:** EM-04 closes the one gap Repair 1 had already named and consciously left open - the same class of path-injection risk EM-03 addressed for `feature_area`, now closed for `evidence_id` too, so both caller-facing path-building surfaces in `store.py` share the same allowlist-plus-containment defense.

**What was deliberately not done:** no commit, push, PR, or merge - local working tree only, per explicit instruction; no redesign of the id-generation scheme itself (`record_evidence()`'s `"ev-" + hex` format already satisfies the new allowlist unchanged); no Slice 2 work; no Safe PPT Engine changes; no change to `memory.py`, since the existing generic `except EditorialMemoryError` in `get_current()` already routes the new error type through the EM-01 path without modification.

**Status:** Active, merged. Independent re-audit (Audit 3) returned **PASS** - all four findings (EM-01/EM-02/EM-03/EM-04) confirmed closed. This diff was then committed and merged to `main` (commit `65c46bebc1954c343f964d59835bf2ecf1b94926`, merge commit `19946ebc2d80e0d494e451e03750fa1b6f77dddb`, PR #10). See PROJ-019 for the next slice (Slice 2, maintainer-decision bootstrap), implemented locally on top of this merged state.

---

## PROJ-019 — Editorial Memory Slice 2 (maintainer-decision bootstrap) — local only, not merged

**Decision:** With Slice 1 closed (PROJ-016/017/018, all four Codex findings resolved and merged via PR #10), the next approved unit of work is Slice 2: the smallest maintainer-decision bootstrap path, proving that an already-approved maintainer editorial decision can become durable current Editorial Memory with complete provenance, using the existing Slice 1 storage/lifecycle layer unmodified. Per explicit task instruction, implemented as a **local, uncommitted working-tree change only** - not committed, pushed, or opened as a PR.

- **Bootstrap privilege, scoped to one evidence type:** new `EditorialMemory.record_maintainer_decision()` in `editorial-memory/lib/memory.py` accepts no `evidence_type` parameter at all - `maintainer_decision` is the only value it can ever produce, so no other evidence type gains this privilege by construction, not by convention.
- **Reuses, never bypasses, the existing approval mechanism:** internally this is exactly `propose_state()` followed by `approve_state()` - the same two Slice 1 methods every other caller already uses. No direct write of `status=current`, no second approval mechanism. A call-spy test (`test_s2_10_bootstrap_reuses_existing_approval_logic`) proves both are actually invoked, and a companion test proves a state produced this way is still subject to ordinary lifecycle rules - re-approving it through the plain API still raises `InvalidLifecycleTransitionError`.
- **KnowledgeItem reuse, not duplication:** `get_or_create_knowledge_item` is called unmodified, so the existing EM-02 slug-collision protection (`KeyCollisionError`) and idempotent same-key lookup apply automatically; a second decision under the same key never creates a second `KnowledgeItem`. (A mismatched `feature_area` on that second call was originally handled by silently keeping the first-set value - Audit 1 found this insufficiently explicit; see PROJ-020, S2-01, for the corrected reject-outright behavior.)
- **`feature_area` validation reused, not reimplemented:** `store.validate_feature_area()` (EM-03) is called explicitly up front, before any record is created, so an invalid `feature_area` is rejected before any Evidence or KnowledgeItem write happens - verified by a test asserting zero Evidence records exist after a rejected call.
- **Idempotency without new schema:** exact-repeat input is detected by comparing the incoming content/rationale/Evidence fields against what a `KnowledgeItem`'s existing states and their cited Evidence already have on disk - no new persisted field, no separate dedup index. An exact repeat returns the existing state unchanged; a materially different decision (any field differing) creates a new `KnowledgeState` through the ordinary lifecycle, superseding the prior current state exactly as any other supersession does.
- **Default `relation_to_previous`:** `new` for a brand-new `KnowledgeItem`, `refines` for a subsequent decision on an existing one (an implementer judgment call, not independently reviewed yet); callers may override it explicitly, e.g. to record a `contradicts` decision.

17 new tests added (`editorial-memory/tests/test_editorial_memory.py`; 76 total in the suite, all 59 prior tests passing unmodified), covering: creation of exactly one Evidence + one current state; the state referencing that Evidence; `approved_by`/`approved_at` presence, including the case where the approver differs from the capturer; rationale and `verification_scope` survival; invalid `feature_area` rejection with zero side effects; non-overwrite of an existing item's `feature_area`; the bootstrap-privilege scoping via signature inspection; round-trip reload through existing Slice 1 loading; the approval-reuse call-spy and lifecycle-rule proofs above; exact-repeat idempotency (by both return-value and record-count assertions); materially-different-decision supersession; KnowledgeItem reuse across decisions; and the default-relation behavior. All 17 were confirmed to fail with `record_maintainer_decision` removed from `memory.py` (AttributeError), establishing they exercise the new logic specifically.

`python scripts/check_pipeline.py` still passes; `py_compile` is clean; `git diff --check` passes; Safe PPT Engine code (`baseline/B4PS-TUT-main/.b4ps-tools/`) was not touched; only `editorial-memory/lib/memory.py` and the test file were modified - `errors.py`, `store.py`, `models.py`, and `__init__.py` are all untouched, since no new capability required a new error type, storage primitive, or model field.

**Reason:** A maintainer has already supplied the approved reasoning for these decisions outside the system; requiring them to be independently re-proposed-then-approved through two separate manual calls would add friction without adding safety, since the two-step Slice 1 lifecycle is still fully exercised underneath - just invoked together as one workflow, per explicit task instruction, rather than skipped.

**What was deliberately not done:** no commit, push, PR, or merge - local working tree only, per explicit instruction; no PPT/script/screenshot/recording ingestion; no browser-observation producer; no CLI or UI; no review queue beyond what Slice 1 already exposes (`get_pending`/`get_conflicts`); no new retrieval architecture; no Documentation Intelligence; no redesign of the three-entity model, lifecycle, invalidation/purge design, or any other Slice 1 architecture decision.

**Status:** Active, local only. Independent Codex audit (Audit 1) found 2 findings - see PROJ-020.

---

## PROJ-020 — Editorial Memory Slice 2 Repair 1 (Audit 1 findings S2-01, S2-02) — local only, not merged

**Decision:** Independent Codex audit of Slice 2's original local implementation (Audit 1) returned **FAIL**: 2 findings. Per explicit task instruction, both are fixed as **local, uncommitted working-tree changes only** - not committed, pushed, or opened as a PR; `main` is unaffected and still reflects merged Slice 1 through Repair 2 (PR #10).

- **S2-01 (`feature_area` mismatch on an existing `KnowledgeItem` must be rejected, not silently accepted):** before any duplicate-detection or mutation, `record_maintainer_decision` now loads the existing `KnowledgeItem` (if any) for the requested `key` and compares its stored `feature_area` against the requested one. A mismatch raises new typed `FeatureAreaMismatchError` (`editorial-memory/lib/errors.py`) immediately - no Evidence is recorded, no `KnowledgeState` is created, and the mismatched request is never treated as an idempotent repeat of an existing decision, closing the gap PROJ-019's own "KnowledgeItem reuse" bullet had described as silent-keep behavior.
- **S2-02 (`relation_to_previous` must be part of exact-repeat identity):** `_find_matching_maintainer_state` now also compares the effective `relation_to_previous` (after the existing default-computation logic, or the caller's explicit override) against each candidate state's stored relation. The same content/rationale/Evidence fields resubmitted with an explicitly different relation (e.g. `NEW` then `CONTRADICTS`) no longer match as a duplicate and instead create a new `KnowledgeState` through the ordinary `propose_state`/`approve_state` lifecycle. The existing default-relation behavior (`NEW` for a brand-new item, `REFINES` otherwise) is unchanged.

9 new adversarial tests added (`editorial-memory/tests/test_editorial_memory.py`; 85 total in the suite, all 76 prior tests passing, 2 of the original 17 Slice 2 tests updated in place with explicit justification rather than weakened - `test_s2_07` now asserts the corrected reject-outright behavior, `test_s2_11`/`test_s2_12` now pass an explicit, matching `relation_to_previous` on repeated calls since the pre-existing default-relation logic legitimately differs between "first state on a new item" and "later decision on an existing item"): 4 for S2-01 (mismatched feature_area with different content, mismatched feature_area with an otherwise-exact repeat, mismatch surviving a simulated process reload, record-count-unchanged on rejection) and 5 for S2-02 (differing explicit relation creates a new state, same explicit relation remains idempotent, a three-call REFINES-after-CONTRADICTS chain proving it is not idempotent with either prior state, default-relation behavior unaffected, history/supersession correctness across relation changes). 7 of the 9 new tests were confirmed to fail against the pre-repair logic specifically; the other 2 pass against pre-repair code coincidentally, since they exercise default-relation behavior neither fix touches - reported honestly rather than dropped.

`python scripts/check_pipeline.py` still passes; `py_compile` is clean; `git diff --check` passes; Safe PPT Engine code (`baseline/B4PS-TUT-main/.b4ps-tools/`) was not touched; `editorial-memory/lib/store.py` and `models.py` remain untouched by this repair; only `memory.py`, `errors.py` (new error type), and `__init__.py` (its export) were modified in `lib/`.

**Reason:** Both findings are the same class of defect the founder's Editorial Memory lifecycle clarification exists to prevent: knowledge silently drifting or silently collapsing distinct decisions together. S2-01 is the feature_area analogue of Slice 1's own EM-02 (identity drift instead of an explicit rejection); S2-02 is a correctness gap in the bootstrap's own idempotency contract, since an explicit `CONTRADICTS` resubmission is precisely the signal this system exists to preserve, not discard.

**What was deliberately not done:** no commit, push, PR, or merge - local working tree only, per explicit instruction; no redesign of the default-relation heuristic (`NEW`/`REFINES`), which was out of scope for these two findings; no Slice 3 work; no Safe PPT Engine changes; no change to `store.py` or `models.py`, since both findings were resolvable entirely within `memory.py`'s own bootstrap logic and a single new error type.

**Status:** Active, merged. Independent re-audit (Audit 2) returned **PASS** — S2-01 and S2-02 confirmed closed. The exact audited local tree was then committed and merged to `main` (commit `9a5614276f75db37944ad09337635fe7a971c271`, merge commit `9b2cb21d8b627cc2fe871446747584e2cc06939b`, PR #11). Slice 2 is complete and merged; `main` at `9b2cb21` is the current base for Slice 3.

---

## PROJ-021 — Editorial Memory Slice 3 (retrieval functions) — local only, not merged

**Decision:** With Slice 1 complete and merged, and Slice 2 (including its Repair 1, PROJ-020) complete, independently re-audited **PASS**, and merged to `main` via PR #11 (commit `9b2cb21d8b627cc2fe871446747584e2cc06939b`), the next approved unit of work is Slice 3: the smallest deterministic retrieval layer, proving stored Editorial Memory can be consumed by key/feature_area/knowledge_type without bypassing the existing approval/lifecycle rules. Per explicit task instruction, implemented as a **local, uncommitted working-tree change only** — not committed, pushed, or opened as a PR.

- **New module, no changes to Slice 1/2 code:** `editorial-memory/lib/retrieval.py` is additive only. `models.py`, `store.py`, `errors.py`, and `memory.py` are all untouched — every retrieval function is a thin composition of `EditorialMemory.get_current`, `get_history`, and `list_knowledge_items`, all unchanged, so every lifecycle rule they already enforce (approval-gated `current`, corrupt-provenance detection via `CorruptProvenanceError`, contradictions never promoted) applies to retrieval unchanged, not reimplemented.
- **Three entry points, one shared implementation:** `get_current_by_key(memory, key)`, `list_current_by_feature_area(memory, feature_area)`, and `list_current_by_knowledge_type(memory, knowledge_type)` — the last two share one internal `_list_current` helper rather than duplicating the current-state filter twice.
- **Key lookup is read-only:** a new `_resolve_key` helper slugifies the key and loads the item directly from `JSONStore`, without the create-if-missing side effect `get_or_create_knowledge_item` has — retrieval must never create a `KnowledgeItem` as a side effect of being asked about one. A key that doesn't exist, and a key that collides at the slug level with a *different* stored key, both resolve to "no such item" (`None`) rather than raising or creating anything; this mirrors the existing `KeyCollisionError` check's reasoning without invoking a mutation-path error class from a read path.
- **"No current state yet" and "unknown key" both return `None`, not an error:** an item that exists but has only `proposed`/`contradicted`/`invalidated`/`superseded` states (no state has ever been approved) is indistinguishable, from a caller's perspective, from a key nobody has recorded anything about yet — both mean "nothing is currently known," so both return `None` from `get_current_by_key` and are simply absent from the `list_current_by_*` results, never a null entry.
- **`CurrentKnowledge` read model bundles identity + lifecycle, never duplicates Evidence content:** the dataclass returned by all three lookups carries `KnowledgeItem` identity (`id`, `key`, `knowledge_type`, `feature_area`) alongside the current `KnowledgeState`'s `content`, `status`, `relation_to_previous`, `evidence_refs` (ids only — the raw Evidence records are a separate, unchanged `get_provenance` call), `approved_by`/`approved_at`, `created_at`, and `rationale` — matching the founder brief's "returned information" list without inlining raw Evidence.
- **History retrieval reuses Slice 1's `get_history` verbatim:** `get_history_by_key(memory, key)` only resolves the key to an item id and delegates; Slice 1's own history semantics (including what it does or doesn't exclude) are unchanged, since this slice does not redesign Slice 1.
- **Ordering is deterministic by construction, not by a new sort:** `JSONStore.list_knowledge_item_ids()` already returns a `sorted()` listing, and `list_knowledge_items`/`_list_current` iterate it unmodified — no new sort key was introduced, so two fresh `EditorialMemory` instances reading the same on-disk store return results in the same order, proven by a reload test (`test_deterministic_ordering_across_reload`).

17 new tests added (`editorial-memory/tests/test_retrieval.py`; 101 total in the suite, all 84 prior Slice 1/2 tests passing unmodified), covering: retrieval by key returning the current approved state; feature_area and knowledge_type filtering; superseded/proposed/contradicted/invalidated states all correctly excluded from "current" (four separate tests, one per status); evidence_refs, rationale, and approval metadata (`approved_by`/`approved_at`/`created_at`) surviving retrieval; ordered, complete history retrieval; deterministic ordering across a simulated fresh-process reload; a corrupt on-disk record raising the existing typed `StorageCorruptionError` rather than crashing unsafely or returning bad data; unknown-key and no-current-state-yet both returning `None`/empty; and an explicit no-mutation check comparing `get_history` before and after exercising every retrieval function.

`python scripts/check_pipeline.py` passes; `py_compile` is clean on all changed/added files; `editorial-memory/tests/` (101 tests) passes in full; Safe PPT Engine code (`baseline/B4PS-TUT-main/.b4ps-tools/`) was not touched; only `editorial-memory/lib/retrieval.py` (new), `editorial-memory/lib/__init__.py` (new exports only), and `editorial-memory/tests/test_retrieval.py` (new) were added/modified — `models.py`, `store.py`, `errors.py`, and `memory.py` are all untouched.

**Reason:** Documentation Intelligence (a later milestone, not part of this slice) needs a stable, lifecycle-safe surface to query Editorial Memory against. Building it as pure composition over Slice 1/2's existing accessors — rather than new storage access, a new index, or a parallel current-state computation — means retrieval cannot drift from the lifecycle rules Slice 1/2 already enforce, by construction rather than by separately re-verifying them here.

**What was deliberately not done:** no commit, push, PR, or merge — local working tree only, per explicit instruction; no fuzzy/ranked/semantic search or embeddings; no database or new index file (retrieval still reads through `JSONStore` on every call, per Slice 1's stateless-between-calls design); no redesign of Slice 1's `get_history` inclusion/exclusion behavior; no CLI or UI; no PPT/script/screenshot/recording ingestion; no Browser Verification; no Documentation Intelligence; no Slice 4+.

**Status:** Implemented locally. Independent Codex audit (Audit 1) returned **PASS WITH FINDINGS**: 2 findings, S3-01 (invalidated states exposed by `get_history_by_key`) and S3-02 (stale governance chronology in this log). No integration completed — `main` still reflects merged Slice 1 and Slice 2 (PR #11, commit `9b2cb21d8b627cc2fe871446747584e2cc06939b`); this slice exists only in this local working tree. See PROJ-022 for Repair 1.

---

## PROJ-022 — Editorial Memory Slice 3 Repair 1 (Audit 1 findings S3-01, S3-02) — local only, not merged

**Decision:** Independent Codex audit of Slice 3's original local implementation (Audit 1, PROJ-021) returned **PASS WITH FINDINGS**: 2 findings. Per explicit task instruction, both are fixed as **local, uncommitted working-tree changes only** — not committed, pushed, or opened as a PR; `main` is unaffected and still reflects merged Slice 1 and Slice 2 (PR #11, commit `9b2cb21d8b627cc2fe871446747584e2cc06939b`).

- **S3-01 (invalidated states exposed by `get_history_by_key`):** the canonical retrieval contract excludes `invalidated` states from every normal retrieval path, including history — only a dedicated invalidated-state query may expose them, and this slice does not add one (not yet required). `get_history_by_key` in `editorial-memory/lib/retrieval.py` now filters the list `EditorialMemory.get_history` (unchanged) returns, dropping any state with `status == StateStatus.INVALIDATED`, via a single list comprehension — the smallest change consistent with the contract. `EditorialMemory.get_history` itself is untouched, so its own semantics (and any other caller relying on them) are unaffected; only the Slice 3 retrieval-layer wrapper changed. `current`, `superseded`, `proposed`, and `contradicted` states are still returned, in the same order `get_history` already produces them (oldest → newest) — the filter does not reorder or reconstruct the list, so deterministic ordering of the remaining history is preserved by construction, not by a new sort.
- **S3-02 (stale governance chronology):** this log's own status lines had fallen behind actual events — PROJ-020's status still read "Independent re-audit (Audit 2) pending" after Audit 2 had already returned PASS and Slice 2 Repair 1 had already been merged to `main` via PR #11 (commit `9a5614276f75db37944ad09337635fe7a971c271`, merge commit `9b2cb21d8b627cc2fe871446747584e2cc06939b`), and PROJ-021's decision line still described Slice 2 Repair 1 as "pending its own re-audit" for the same reason. Both are corrected to state plainly that Slice 2 Repair 1 passed Audit 2 and was merged, that `main` at `9b2cb21` is Slice 3's actual base, and that Slice 3's own Audit 1 returned PASS WITH FINDINGS (this repair's two findings) with re-audit still pending. No historical audit result was rewritten — Audit 1 (Slice 1), Audit 2 (Slice 1 Repair 1), Audit 3 (Slice 1 Repair 2), and Slice 2's own Audit 1 finding-set (S2-01/S2-02) all remain exactly as previously recorded; only the *status lines* describing what happened *after* those audits were brought current.

4 new adversarial tests added (`editorial-memory/tests/test_retrieval.py`; 105 total in the suite, all 101 prior Slice 1/2/3 tests passing unmodified): an approved-then-invalidated state absent from `get_history_by_key` while still present in the raw, unfiltered `EditorialMemory.get_history` (proving this is a retrieval-layer filter, not a Slice 1 change); a mixed current/superseded/invalidated history where only the invalidated state is excluded; ordering staying deterministic (two consecutive calls, plus a version-gap check) after an interior, non-terminal state is invalidated; and the exclusion surviving a simulated fresh-process reload. All 4 were confirmed to fail against the pre-repair `get_history_by_key` (which returned `EditorialMemory.get_history`'s result unfiltered) before the fix was restored — not merely against a missing symbol, since the function already existed and only its filtering behavior was wrong.

`python scripts/check_pipeline.py` passes; `py_compile` is clean on all changed files; `git diff --check` passes; `editorial-memory/tests/` (105 tests) passes in full; Safe PPT Engine code (`baseline/B4PS-TUT-main/.b4ps-tools/`) was not touched; only `editorial-memory/lib/retrieval.py`, `editorial-memory/tests/test_retrieval.py`, and this log were modified — `models.py`, `store.py`, `errors.py`, and `memory.py` remain untouched, since S3-01 was resolvable entirely as a filter in the Slice 3 retrieval wrapper and required no change to Slice 1/2's own lifecycle code.

**Reason:** S3-01 is the same class of defect this system exists to prevent applied to history instead of current-state retrieval: content that was never trustworthy (`invalidated`) must never be presented to a consumer as valid history, even via an explicit history query — the canonical contract reserves that to a dedicated query this slice does not build. S3-02 is a chronology-accuracy defect of the same shape this log has corrected in earlier rounds (PROJ-012, PROJ-013, PROJ-015): a status line lagging behind events that had already happened by the time it was written.

**What was deliberately not done:** no commit, push, PR, or merge — local working tree only, per explicit instruction; no new invalidated-state query (`get_invalidated`-style API) — not required by the current contract for this slice; no redesign of `get_history`'s ordering or inclusion logic in Slice 1 itself; no change to `get_current_by_key`, `list_current_by_feature_area`, or `list_current_by_knowledge_type` (none exposed invalidated states — Audit 1 confirmed this, only `get_history_by_key` had the gap); no rewrite of any historical audit result in this log; no Obsidian changes; no Safe PPT Engine changes; no Slice 4 work.

**Status:** Active, local only. Independent re-audit (Audit 2) returned **PASS WITH FINDINGS** — S3-01 confirmed closed; S3-02 confirmed only partial, since `docs/DECISIONS.md` was already correct but `docs/CURRENT.md` still described Slice 2 as un-merged/pending re-audit and Slice 3 as not begun. That remaining gap was fixed as a further local repair, scoped to `docs/CURRENT.md` only (no code or test change). A subsequent independent re-audit (Audit 3) returned **PASS WITH FINDINGS** once more — this same status line, in this entry, was the one remaining stale artifact (still reading "Audit 2 pending" after Audit 2 had already happened), now corrected here.

**Update:** A final comprehensive Codex verdict subsequently returned **PASS** (S3-01 and S3-02 both CLOSED). The exact audited Slice 3 tree was committed and merged to `main` (commit `5038d50e7588d34eef777da5d9ebd8ae71510ace`, merge commit `98c55f3839c66221ae3b8389b15c627d74b4be03`, PR #12, branch `editorial-memory-slice3` deleted after merge). Slice 3 is closed; `main` at `98c55f3` is the current base for the Slices 4–7 completion batch — see PROJ-023.

---

## PROJ-023 — Editorial Memory completion batch: Slices 4–7 — local only, not merged

**Decision:** With Slices 1–3 closed and merged (`main` at `98c55f3839c66221ae3b8389b15c627d74b4be03`), the next approved unit of work is the remaining Editorial Memory completion batch — Slice 4 (existing-documentation ingestion), Slice 5 (review queue), Slice 6 (knowledge evolution), and Slice 7 (invalidation + bounded purge) — implemented sequentially, each with its own dedicated tests and a full-suite green run before starting the next, per explicit task instruction. Implemented as a **local, uncommitted working-tree change only** — not committed, pushed, or opened as a PR. No three-entity model, lifecycle design, approval model, or provenance model changes were made; every addition composes unchanged Slice 1 primitives (`propose_state`, `approve_state`, `invalidate_state`, `get_current`, `get_history`, `get_pending`, `list_knowledge_items`) or adds narrowly-scoped new methods following the exact pattern Slice 2 already established.

- **Slice 4 — existing-documentation ingestion:** new `EditorialMemory.ingest_existing_documentation()` in `editorial-memory/lib/memory.py`, structurally identical to Slice 2's `record_maintainer_decision()` except it calls `propose_state()` alone — never `approve_state()` — since existing documentation describes what was *written*, not what's true today (per the approved evidence hierarchy), and must never auto-promote. Reuses the same `feature_area`-mismatch rejection (S2-01-style, raising the existing `FeatureAreaMismatchError`) and the same exact-repeat idempotency comparison, generalized from `_find_matching_maintainer_state` into `_find_matching_evidence_backed_state` (now parameterized by `evidence_type`, so a maintainer decision and a documentation ingestion never dedupe against each other even with identical text) — a small internal refactor of Slice 2's own private helper, not a change to any public Slice 1/2 behavior. `knowledge_type` is always `documentation`, never caller-supplied. 15 new tests (`test_slice4_documentation_ingestion.py`).
- **Slice 5 — review queue:** new `editorial-memory/lib/review.py` (`list_pending_review`, `list_conflicts`, `PendingReview`), composing unchanged `EditorialMemory.list_knowledge_items`/`get_pending`/`review_required` into a deterministic, filterable (by `feature_area`/`knowledge_type`) queue view that carries KnowledgeItem identity — something `get_pending`/`get_conflicts` don't provide on their own when called across the whole store (they return bare `KnowledgeState` objects with no back-reference to their parent item, so a reviewer wouldn't know which item/version to act on). No new lifecycle, no new status, no new mutation: approve/reject/resolve are documented as exactly `approve_state`/`invalidate_state`, unchanged. 14 new tests (`test_slice5_review_queue.py`).
- **Slice 6 — knowledge evolution:** no production code changes — `propose_state`/`approve_state` and the `Relation`/`StateStatus` enums already fully implement confirm/refine/contradict/supersede (Slice 1). The gap was proof, not mechanism: `Relation.CONFIRMS` had never been exercised anywhere in the existing suite. 8 new tests (`test_slice6_knowledge_evolution.py`) drive a full confirm→refine→contradict→supersede chain against a real (non-bootstrap) item using ordinary `propose_state`/`approve_state` calls, plus a test proving current truth is determined by explicit approval rather than "newest version number."
- **Slice 7 — invalidation + bounded purge:** new `editorial-memory/lib/purge.py` with two independent halves. Invalidated-knowledge audit access (`get_invalidated_by_key`, `list_invalidated_by_feature_area`, `list_invalidated_by_knowledge_type`, `InvalidatedKnowledge`) is the dedicated query the design always reserved for `invalidated` states, deliberately deferred by Slice 3/5 since their normal retrieval paths must exclude it — pure composition, no mutation. Bounded purge (`purge_evidence`, `purge_knowledge_state`, `Tombstone`) is a direct storage-layer operation, outside the normal evolution flow, human-authorized (`purged_by` always required) and targeted at exactly one record: `purge_evidence` maps cleanly onto Evidence's one-file-per-record storage (tombstone-by-default overwrite, or full removal via a new `JSONStore.delete_evidence`); `purge_knowledge_state` redacts (`content`→`"[purged]"`, `rationale`→`None`) or removes one entry from its KnowledgeItem's embedded `states` list — safe because `version` is an explicit stored field, never implied by array position, so sibling states are never affected. Purging Evidence still cited by a `current` state is not specially blocked — it surfaces loudly via the existing, unchanged `CorruptProvenanceError` (EM-01), matching the design's own accepted "evidence artifact goes missing" failure mode rather than inventing a new guard. 18 new tests (`test_slice7_invalidation_purge.py`).
- **Cross-slice end-to-end proof:** `test_e2e_slices_4_7.py` — one full walkthrough (existing-documentation Evidence → proposed Knowledge → review → approval/current → new Evidence → refinement → review → contradiction → rejected-via-invalidation → genuine supersession/update → invalidation of earlier history → correct current retrieval → history/provenance preserved, invalidated states excluded → fresh reload → identical results), plus 10 adversarial tests specifically combining two or more slices: duplicate ingestion mid-lifecycle, conflicting evidence (two contradictions against one current state), an invalid transition reached via ingest-then-invalidate-then-approve, corrupt Evidence isolated to its own item (an unrelated item's review queue stays correct), malformed KnowledgeItem JSON failing safely across every Slice 3/5/7 retrieval path that touches it, path-traversal rejection with zero side effects, all three non-current statuses (stale/superseded/invalidated) retrieved correctly together, end-to-end idempotency across a repeated maintainer-decision + documentation-ingestion run, purge unable to cross item boundaries, and purge's tombstone redaction never being silent. 12 new tests total in this file (the walkthrough, the 10 adversarial cross-slice tests, and one import-sanity check).

120 tests passed after Slice 4, 134 after Slice 5, 142 after Slice 6, 160 after Slice 7; 172 tests total pass after the cross-slice E2E file — 105 prior Slice 1/2/3 tests plus 67 new Slice 4–7/E2E tests, all passing, zero regressions found during the build (one test-authoring mistake was caught and fixed before moving on: an early idempotency test used the default `relation_to_previous`, which legitimately differs between a brand-new item and a later resubmission — the same S2-02 distinction Slice 2 already documents — corrected to pass an explicit matching relation, mirroring `test_s2_11`/`test_s2_12`; a second mistake asserted the wrong exception type for corrupt-evidence-behind-a-current-state, corrected to expect `CorruptProvenanceError` chaining `StorageCorruptionError`, matching EM-01's actual behavior).

`python scripts/check_pipeline.py` passes; `py_compile` is clean on every changed/added `lib/`/`tests/` file; `git diff --check` passes; Safe PPT Engine code (`baseline/B4PS-TUT-main/.b4ps-tools/`) was not touched. Files changed: `editorial-memory/lib/memory.py` (Slice 4 method + generalized dedup helper), `editorial-memory/lib/store.py` (`delete_evidence` for Slice 7 full-removal purge), `editorial-memory/lib/__init__.py` (new exports); files added: `editorial-memory/lib/review.py`, `editorial-memory/lib/purge.py`, and five new test files. `models.py` and `errors.py` are untouched — no new status, no new relation, no new error type was needed anywhere in this batch.

**Reason:** These four slices were the remaining approved scope from the original Editorial Memory implementation plan (`docs/EDITORIAL_MEMORY_IMPLEMENTATION_AUDIT.md` §18) — completing them closes the gap between "the lifecycle mechanism exists" (Slices 1–3) and "the lifecycle can actually be driven end-to-end by realistic ingestion, review, evolution, and cleanup workflows" (Slices 4–7), without which Documentation Intelligence would have nothing real to consume.

**What was deliberately not done:** no commit, push, PR, or merge — local working tree only, per explicit instruction; no redesign of the three-entity model, lifecycle, approval model, or provenance model; no new relation or status types; no Browser Verification; no Documentation Intelligence; no Slice 8; no Safe PPT Engine changes; no Obsidian changes. Editorial Memory is **not** being declared complete or closed by this entry — that requires comprehensive independent Codex audit, integration, an Obsidian checkpoint, and founder/experienced-maintainer workflow validation, none of which have happened yet.

**Correction (GOV-02, EM47 repair round — see PROJ-024):** this entry originally stated that purge's inability to persist `purged_by`/reason on a `KnowledgeState` tombstone "was accepted rather than 'fixed' by adding a field" — that framing is corrected here, since no approving authority reviewed or signed off on treating that gap as acceptable, and the underlying limitation is no longer present: EM47-04 (PROJ-024) resolved it by persisting purge attribution/reason through the existing `invalidated_by`/`invalidated_at`/`invalidation_reason` fields, without adding a new schema field. The claim is removed rather than re-justified, since it is now moot.

**Status:** Implemented locally. Independent Codex audit found 5 code findings (EM47-01–05) and 2 governance findings (GOV-01, GOV-02) — all fixed as local Repair 1, see PROJ-024. No integration completed. Editorial Memory phase NOT yet closed.

---

## PROJ-024 — Editorial Memory Slices 4–7 Repair 1 (EM47 audit findings) — local only, not merged

**Decision:** Independent Codex audit of the Slices 4–7 completion batch (PROJ-023) found 5 code findings (EM47-01 through EM47-05) and 2 governance findings (GOV-01, GOV-02). Per explicit task instruction, all 7 are fixed as **local, uncommitted working-tree changes only** — not committed, pushed, or opened as a PR; `main` is unaffected and still reflects merged Slices 1–3 (`98c55f3839c66221ae3b8389b15c627d74b4be03`, PR #12).

- **EM47-01 (omitted-relation exact repeats not idempotent, Slice 2 + 4):** `_find_matching_evidence_backed_state`'s duplicate check compared each candidate state against a *single* default relation (`NEW`/`REFINES`) recomputed at call time from `item.states`'s current emptiness — which changes after the first successful call, so a second, otherwise-identical omitted-relation call could recompute `REFINES` where the original state actually stored `NEW`, fail to match, and create a spurious duplicate. Fixed: when the caller's `relation_to_previous` was omitted, each candidate state is now compared against the default *that would have applied when that specific state was created* (`NEW` if it was the item's first state, `REFINES` otherwise) — duplicate detection uses the original submission's identity, not a default recomputed against how the item has grown since. An explicitly-supplied relation (S2-02) is unaffected — it must still match exactly.
- **EM47-02 (purge `item_id` not validated as a literal, glob/wildcard reaches `Path.glob()`):** `purge_knowledge_state` passed a caller-supplied `item_id` straight into `JSONStore.load_knowledge_item`, whose id-only lookup branch resolves via `self.knowledge_dir.glob(f"*/{item_id}.json")` — a glob-metacharacter `item_id` (`*`, `?`, `[...]`) would be interpreted as a pattern, not a literal id. Fixed: new `store.validate_knowledge_item_id()` (same safe-token charset as `validate_evidence_id`) rejects anything that isn't a literal `slugify()`-shaped id before purge ever touches disk, raising new typed `InvalidKnowledgeItemIdError`. Scoped to the purge path specifically (per the finding), not retrofitted onto every Slice 1 id-based read, which is out of this finding's scope and would be a wider change than asked for.
- **EM47-03 (new state version not guaranteed unique after purge):** `propose_state`'s version was `len(item.states) + 1` — safe under Slice 1/2/3/4/5/6 alone (states are only ever appended, never removed), but no longer safe once Slice 7's full-removal purge can shrink `states` without renumbering what remains (version is an explicit stored field, never implied by array position, precisely so purge could do this safely — but `propose_state` itself hadn't been updated to match). Fixed: `version = max((s.version for s in item.states), default=0) + 1` — correct whether or not any prior version was ever removed, and correctly reuses a version number that a full-removal purge freed rather than skipping past it.
- **EM47-04 (tombstoned KnowledgeState could remain `current`; blank `purged_by` accepted; attribution not preserved):** `purge_knowledge_state`'s tombstone mode redacted `content`/`rationale` but left `status` untouched, so a purged `current` state remained returned by `get_current`/`get_current_by_key` — content gone, but still presented as valid current truth. Fixed: tombstone mode now unconditionally forces `status = invalidated`. Both `purge_evidence` and `purge_knowledge_state` now reject a missing/`None`/blank-or-whitespace-only `purged_by` via new typed `MissingPurgeAuthorizationError`, checked before either function touches disk. Purge attribution and reason are now preserved in a contract-safe form by reusing the `invalidated_by`/`invalidated_at`/`invalidation_reason` fields the approved Slice 1 schema already defines for exactly this purpose — no new `KnowledgeState` field was added, so this is not a schema change (see the GOV-02 correction above, which removes the prior entry's now-inaccurate claim that the missing-attribution gap was "accepted").
- **EM47-05 (tombstoned Evidence produced a raw `KeyError`, not a typed error):** `Evidence.from_dict` does an unguarded `data["evidence_type"]` lookup; loading a purge tombstone stub (`{id, purged_at, purged_by, reason}`, which has no `evidence_type` key) raised a raw `KeyError` out of `EditorialMemory.get_evidence()` — not caught by `get_current`'s `except EditorialMemoryError` wrapping, since `KeyError` isn't an `EditorialMemoryError` subclass. (The original Slice 7 test suite had, by coincidence, encoded this bug as expected behavior — `test_s7_09` asserted `pytest.raises(KeyError)`.) Fixed: `get_evidence()` now catches `KeyError`/`TypeError`/`ValueError` from `Evidence.from_dict` and re-raises new typed `CorruptEvidenceRecordError`, chaining the original exception via `__cause__` — `get_current`'s existing EM-01 wrapping now catches this too, producing a full typed chain (`CorruptProvenanceError` ← `CorruptEvidenceRecordError` ← `KeyError`). `test_s7_09` corrected to assert the fixed behavior, not weakened.
- **GOV-01 (no governance records existed for Slices 4–7):** added `openspec/changes/editorial-memory-slices-4-7/{spec.md, verification-report.md, audit-report.md}`, mirroring the existing `editorial-memory`/`editorial-memory-slice2` pattern.
- **GOV-02 (unsupported "accepted" claim in this log):** corrected above, in this entry's parent (PROJ-023) — the claim is removed, not re-justified, since EM47-04 resolved the underlying limitation rather than merely accepting it.

21 new adversarial regression tests added (`editorial-memory/tests/test_em47_audit_repair.py`; 193 total in the suite, all 172 prior Slice 1–7 tests passing unmodified except one intentional, justified update): at least one dedicated test per finding, including omitted-relation idempotency surviving an approval in between (EM47-01), glob/wildcard/path-traversal `item_id` rejection with zero side effects including against a real co-existing item (EM47-02), version uniqueness after both interior and highest-version full-removal purge plus survival across reload (EM47-03), a purged `current` state disappearing from `get_current`/`get_current_by_key`, a purged `proposed` state disappearing from the review queue, blank-`purged_by` rejection for both purge functions with zero side effects, and attribution/reason surviving via the reused fields (EM47-04), and the full typed error chain plus an explicit "no raw KeyError reaches the caller" proof (EM47-05). One pre-existing test (`test_s7_09`) was updated with explicit justification — not weakened, corrected because the behavior it asserted no longer occurs once EM47-05 is fixed.

`python scripts/check_pipeline.py` passes; `py_compile` is clean on all changed/added files; `git diff --check` passes; `editorial-memory/tests/` (193 tests) passes in full; Safe PPT Engine code was not touched. Files changed: `editorial-memory/lib/memory.py`, `editorial-memory/lib/store.py`, `editorial-memory/lib/purge.py`, `editorial-memory/lib/errors.py`, `editorial-memory/lib/__init__.py`, `editorial-memory/tests/test_em47_audit_repair.py` (new), `editorial-memory/tests/test_slice7_invalidation_purge.py` (one test updated); `openspec/changes/editorial-memory-slices-4-7/` (new); this log; `docs/CURRENT.md`. `models.py` is untouched — no new relation or status type was needed; EM47-04's attribution requirement was met by reusing existing fields.

**Reason:** EM47-04 and EM47-05 are direct instances of the exact failure mode this whole system exists to prevent (content that shouldn't be trusted remaining silently retrievable as truth; a storage-layer failure surfacing as an untyped crash instead of a typed, catchable one) — both already solved for the ordinary path by Slice 1's EM-01, now closed for the purge path too. EM47-01/02/03 are correctness/hardening gaps in the newly-added Slice 4/7 surfaces specifically, not defects in Slice 1's own unmodified lifecycle. GOV-01/02 restore the governance-record accuracy this log has consistently maintained across every prior repair round.

**Contract ambiguity discovered, not resolved by this repair (flagged for founder/Codex judgment, not silently decided):** `purge_knowledge_state`'s tombstone mode always overwrites `invalidated_by`/`invalidated_at`/`invalidation_reason` with the purge's own attribution/reason. If a state was already `invalidated` for an unrelated reason before being purged, that original invalidation's attribution/reason is silently replaced by the purge's, with no separate trace of the original invalidation surviving anywhere. Whether this is acceptable (purge is understood to be a strictly more severe operation that supersedes any prior invalidation record) or requires preserving both are not something this repair round decided unilaterally — flagged here per instruction rather than resolved by implementer judgment.

**What was deliberately not done:** no commit, push, PR, or merge — local working tree only, per explicit instruction; no architecture redesign; no change to Slice 5/6 behavior, EM-01–EM-04, Slice 2 bootstrap, or Slice 3 retrieval (all confirmed unaffected by the full suite passing unmodified); no new relation or status type; no Browser Verification; no Documentation Intelligence; no Slice 8; no Safe PPT Engine changes; no Obsidian changes; the EM47-04-adjacent contract ambiguity above was flagged, not resolved.

**Status:** Active, local only. Independent re-audit found 5 further code findings (numbered 1–5) plus GOV-01/GOV-02 again — all fixed as local Repair 2, see PROJ-025.

---

## PROJ-025 — Editorial Memory Slices 4–7 Repair 2 (findings 1–5, GOV-01/GOV-02 again) — local only, not merged

**Decision:** Independent Codex audit of Repair 1 (PROJ-024) found 5 further code findings (numbered 1–5, deeper gaps Repair 1's own fixes did not fully close) and 2 governance findings (GOV-01, GOV-02, again). Per explicit task instruction, all 7 are fixed as **local, uncommitted working-tree changes only** — not committed, pushed, or opened as a PR; `main` is unaffected and still reflects merged Slices 1–3 (PR #12, commit `98c55f3839c66221ae3b8389b15c627d74b4be03`).

- **Finding 1 (omitted-relation dedup used mutable list position, not stable identity):** Repair 1's EM47-01 fix compared each dedup candidate's expected default relation using `enumerate(item.states)`'s array index — which shifts the moment an earlier state is fully removed by purge, breaking dedup for a still-surviving later state. `_find_matching_evidence_backed_state` now compares using `state.version == 1` instead: a version number, once assigned to a surviving state, is never reassigned or renumbered, so it stays a correct identity signal regardless of what else in the item's history has since been purged. If the item's own true first state was itself the one purged, nothing carries that identity anymore and a resubmission of exactly that original claim is correctly treated as fresh — matching purge's own "no trace" semantics.
- **Finding 2 (version allocation ignored dangling `superseded_by` references):** Repair 1's EM47-03 fix (`max(existing versions) + 1`) only considered versions currently present as their own state, not a surviving state's `superseded_by` field still pointing at a version that was later fully removed by purge — risking that number being silently reassigned to an unrelated new state, repointing the old reference at the wrong thing. New `_next_version(states)` helper in `memory.py` folds every surviving `superseded_by` value into the same `max()`, so a version number stays permanently unique for the item once it has ever meant something, present or only referenced.
- **Finding 3 (dedup's own Evidence lookup bypassed the typed loader):** `_find_matching_evidence_backed_state` called `Evidence.from_dict` directly against `store.load_evidence()`'s raw result — Repair 1's EM47-05 fix lives in `EditorialMemory.get_evidence()`, a different call site the dedup helper never used. A tombstoned/corrupt candidate encountered mid-dedup-scan still raised a raw `KeyError`. Fixed: the dedup loop now calls `self.get_evidence()`, catching `EditorialMemoryError` and skipping (not crashing on) that one candidate — a damaged historical record for an unrelated past submission must not block an entirely new, unrelated ingestion call.
- **Finding 4 (no `knowledge_type` isolation across the two bootstrap paths):** neither `record_maintainer_decision` nor `ingest_existing_documentation` checked the existing item's stored `knowledge_type` before reusing it — a maintainer-decision call and a documentation-ingestion call sharing one natural key could silently blend `editorial`/`documentation`/`product` knowledge under one `KnowledgeItem`, violating the founder's explicit "knowledge categories must remain distinct" amendment (Amendment 1, §14). Fixed: both paths now check `knowledge_type` before any dedup/mutation, raising new typed `KnowledgeTypeMismatchError` (`errors.py`) on mismatch — mirrors the existing `feature_area`-mismatch check exactly, including a zero-side-effects guarantee on rejection.
- **Finding 5 (KnowledgeState tombstone didn't match the approved contract):** Repair 1's EM47-04 fix redacted `content`/`rationale` in place, forced `status=invalidated`, and reused `invalidated_by`/`invalidated_at`/`invalidation_reason` — safe in effect, but not the approved non-claim tombstone shape (`{id, purged_at, purged_by, reason}`), and left an entry inside `states` that was neither a real claim nor a real tombstone. Fixed, superseding Repair 1's approach entirely: `purge_knowledge_state` now always fully removes the target from `states` (never left there in any form), and — tombstone mode, the default — writes the exact same `Tombstone` dataclass shape `purge_evidence` already produces to a new dedicated location, `JSONStore.save_knowledge_state_tombstone`/`load_knowledge_state_tombstone` (`<item_id>--v<version>.json` under a new `tombstones/` directory, mirroring Evidence's one-record-one-location pattern). No new `KnowledgeState` schema field was added. This also retroactively resolves the "purge overwrites a prior invalidation's own attribution" contract ambiguity Repair 1 flagged — there is no longer a surviving state for that ambiguity to apply to, since the state is simply gone.
- **GOV-01 (governance records had drifted and lacked required disclosures):** `openspec/changes/editorial-memory-slices-4-7/spec.md` gained an explicit "Process deviation" section (this record was authored retroactively, after Slices 4–7 were already implemented and once-audited — matching the same disclosure this repository's Safe PPT Engine history already established for itself, e.g. Audit Repair 3's own "Process deviation" heading) and a "Round 2" findings section; `verification-report.md`'s "Rollback plan" gate — which had described only pre-purge *validation* (non-blank `purged_by`, a validated `item_id`) while marking itself "Complete," conflating prevention of an invalid call with rollback of a completed one — now states plainly that no rollback exists for a completed purge, by design; its "Founder-preview requirement" gate — marked "Not applicable" by analogy to Slices 1–3's pure retrieval API — is corrected to **Pending**, since Slice 7 introduces a genuinely new, irreversible, maintainer-facing operational capability no maintainer has yet exercised against real data, the same category of overstatement this repository's Safe PPT Engine governance has previously caught for itself (PROJ-009, PROJ-011) rather than silently marked N/A; `audit-report.md`'s verdict/findings tables extended with Round 2's own rows, cross-referencing which Round 1 fixes each Round 2 finding supersedes or extends.
- **GOV-02 (`docs/CURRENT.md`'s active-change pointer was stale):** the file's top-level Editorial Memory summary line still pointed at Slice 1 as the active change; corrected to point at the Slices 4–7 completion batch, the change actually in progress.

17 new adversarial regression tests added (`editorial-memory/tests/test_repair2_purge_and_dedup.py`; 210 total in the suite, all 193 prior tests passing, with 7 pre-existing tests updated with explicit justification for finding 5's superseding tombstone contract — see `audit-report.md` → "Test-quality findings" for the full list). Findings 1 and 2's fixes were each individually reverted in isolation and their new tests re-run, reproducing clean failures (2 tests failed for finding 1, 3 for finding 2) before being restored — confirmed as genuine regressions the tests actually exercise.

`python scripts/check_pipeline.py` passes; `py_compile` is clean on all changed/added files; `git diff --check` passes; `editorial-memory/tests/` (210 tests) passes in full; Safe PPT Engine code was not touched. Files changed: `editorial-memory/lib/{memory,store,purge,errors,__init__}.py` (further changes on top of Repair 1); `editorial-memory/tests/test_repair2_purge_and_dedup.py` (new); `test_slice7_invalidation_purge.py`, `test_em47_audit_repair.py`, `test_e2e_slices_4_7.py` (tests updated); `openspec/changes/editorial-memory-slices-4-7/{spec.md, verification-report.md, audit-report.md}`; this log; `docs/CURRENT.md`. `models.py` remains untouched — finding 4's isolation check and finding 5's tombstone both work within the existing schema.

**Reason:** Round 2's findings are largely the same underlying lesson repeated across three call sites: Repair 1 fixed the *first* place a defect was visible (a single default-relation computation, a single version-assignment expression, a single `get_evidence` wrapper) without re-deriving whether the same assumption was relied on anywhere else — it was, in the dedup helper's own separate array-index logic, its own separate version-allocation blind spot to `superseded_by`, and its own separate direct Evidence-loading call. Finding 4 is the founder's own explicit "knowledge categories must remain distinct" requirement (Amendment 1), never previously enforced at the one place two independently-implemented bootstrap paths could collide on a shared key. Finding 5 is a contract-conformance defect: Repair 1's fix was *safe* but not *the approved shape*, and per explicit instruction the approved contract governs, not an implementer's safe-but-divergent alternative.

**What was deliberately not done:** no commit, push, PR, or merge — local working tree only, per explicit instruction; no architecture redesign; no change to Slice 5/6 behavior, EM-01–EM-04, Slice 2 bootstrap, or Slice 3 retrieval (all confirmed unaffected by the full suite passing unmodified); no new relation or status type; no Browser Verification; no Documentation Intelligence; no Slice 8; no Safe PPT Engine changes; no Obsidian changes.

**Unresolved contract ambiguity, flagged not decided:** finding 3's fix chooses to *skip* a dedup candidate whose Evidence is tombstoned/corrupt rather than raising a typed error to the caller — a deliberate implementer judgment call (a damaged historical record for an unrelated past submission should not block a new, unrelated call), not independently reviewed. A founder/Codex reviewer may reasonably prefer the opposite (raise, forcing the caller to address the corruption first). See `verification-report.md` → "Remaining uncertainty" for the same note.

**Status:** Active, local only. Independent re-audit found 3 further code findings (tombstone version reuse, unapproved storage-architecture expansion, purge write atomicity) plus GOV-01/GOV-02 a third time — all fixed as local Repair 3, see PROJ-026.

---

## PROJ-026 — Editorial Memory Slices 4–7 Repair 3 (tombstone version reuse, unapproved storage expansion, write atomicity, GOV-01/GOV-02 again) — local only, not merged

**Decision:** Independent Codex audit of Repair 2 (PROJ-025) found 3 further code findings and 2 governance findings (GOV-01, GOV-02, a third time). Per explicit task instruction, all 5 are fixed as **local, uncommitted working-tree changes only** — not committed, pushed, or opened as a PR; `main` is unaffected and still reflects merged Slices 1–3 (PR #12, commit `98c55f3839c66221ae3b8389b15c627d74b4be03`).

- **Tombstone version reuse:** Repair 2's `_next_version()` accounted for surviving states' `superseded_by` references but not for a version whose *tombstone* still exists after the tombstoned state itself was removed from `states` — that version could be silently reassigned to a new, unrelated state, colliding with its own retained tombstone's claimed identity. Fixed: `_next_version()` now also folds in new `JSONStore.tombstoned_versions(item_id)`, which reads tombstone identities directly from the raw persisted record.
- **Unapproved storage-architecture expansion:** the audit rejected Repair 2's `tombstones/` top-level directory outright — not as buggy, but as exceeding the approved storage contract, which defines no location for a KnowledgeState tombstone other than the per-item file a real state already occupies (there is no one-file-per-state location the way Evidence has one-file-per-record). Fixed, removing the directory entirely: a tombstoned KnowledgeState now overwrites its own existing array slot, in place, inside the same `knowledge/<feature_area>/<item_id>.json` file it always lived in — "the same canonical record location," per the finding's own wording. New `store.TOMBSTONE_MARKER_KEY` (`"purged_at"`, present on a tombstone, never on a real `KnowledgeState`) is the discriminator `JSONStore.load_knowledge_item` now uses to filter tombstone-shaped entries back out of `states` before any ordinary caller — `get_current`, `get_history`, the Slice 5 review queue, `get_invalidated_by_key` — ever sees them; none of those functions changed at all. New `load_knowledge_item_raw` (unfiltered) and `tombstoned_versions` support purge and version-allocation's need for tombstone visibility specifically. **Fixing this exposed a second, previously-latent defect this audit round did not itself name:** `EditorialMemory._save_item` — the one choke point `propose_state`/`approve_state`/`invalidate_state` all use to persist a KnowledgeItem — wrote `item.to_dict()` verbatim, and since `item.states` is always the *filtered* (tombstone-free) view, this would have silently dropped any tombstone currently on disk the next time anything else about that item was saved. Caught by three of this round's own new tests failing unexpectedly during implementation (not anticipated by the audit finding, not a deliberate revert-and-check), root-caused, and fixed: `_save_item` now re-reads the raw record and merges retained tombstones back into what it writes, so no mutation path can ever erase one as a side effect.
- **Purge write not atomic:** Repair 2's `purge_knowledge_state` removed the target state and published its tombstone as two separate `save_knowledge_item` calls — a failure of the second write after the first succeeded would have left a state permanently gone with no tombstone at all, a worse outcome than either purge mode is meant to produce. Resolved as a direct consequence of the storage-location fix above, not a separate change: since the target's array slot is now replaced or removed and the *entire* item record written in exactly one `save_knowledge_item` call, both purge modes are atomic by construction, via `JSONStore`'s existing temp-file-then-rename discipline (`_atomic_write_json`) — a write failure at any point leaves the original file, and therefore the original state, completely untouched.
- **GOV-01 (again):** `spec.md`, `verification-report.md`, and `audit-report.md` all said "five pre-existing tests updated" while each enumerating seven test names (`test_s7_14`, `test_s7_15`, `test_s7_18`, three `test_em47_04_*` tests, one E2E test) — a plain arithmetic inconsistency, corrected to "seven" throughout. Repair 2's process-deviation disclosure, Founder-preview-Pending correction, and Rollback-plan correction were all re-checked and confirmed still accurate — not altered by this fix.
- **GOV-02 (again):** `docs/CURRENT.md`'s wording was tightened so it cannot be read as implying Slices 4–7 have not yet been independently audited — it now states plainly that they have been audited three times, with comprehensive re-audit and integration the items still pending, not the audit itself.

11 new adversarial regression tests added (`editorial-memory/tests/test_repair3_tombstone_contract.py`; 221 total in the suite, all 210 prior tests passing, with 6 tests updated again — `test_s7_14`, `test_s7_15`, `test_s7_18` (`test_slice7_invalidation_purge.py`), `test_finding5_tombstone_is_not_a_knowledge_state_shape`, `test_finding5_reload_confirms_invisibility_and_no_trace_mode` (`test_repair2_purge_and_dedup.py`), and `test_adversarial_purge_tombstone_is_never_silent` (`test_e2e_slices_4_7.py`) — mechanically, to read the raw record directly instead of calling the now-removed `load_knowledge_state_tombstone` accessor, not a behavioral change. This is a *different* 6-test set from Repair 2's own 7-test set (which included the three `test_em47_04_*` tests, none of which called `load_knowledge_state_tombstone` and so needed no further change here) — the two counts were previously and incorrectly conflated as "the same seven-test set," corrected here to state the actual, distinct count: tombstone/live version collision (a fresh state after a purge never reuses the purged version), the same proven across two and then three sequential purges, reload preservation of both the tombstone and the version-uniqueness guarantee, re-purge rejection of an already-tombstoned identity (both tombstone and full-removal modes), three distinct write-failure fault-injection tests (`_atomic_write_json` failing outright, `Path.replace` failing specifically during the atomic rename, and full-removal mode's own write failing) each proving the original state survives completely untouched, and two tests confirming no `tombstones/` directory or matching store attribute exists anywhere.

`python scripts/check_pipeline.py` passes; `py_compile` is clean on all changed files; `git diff --check` passes; `editorial-memory/tests/` (221 tests) passes in full; Safe PPT Engine code was not touched. Files changed: `editorial-memory/lib/{memory,store,purge}.py` (no `errors.py`/`__init__.py` change — no new error type needed); `editorial-memory/tests/test_repair3_tombstone_contract.py` (new); `test_slice7_invalidation_purge.py`, `test_repair2_purge_and_dedup.py`, `test_e2e_slices_4_7.py` (tombstone-accessor references updated); `openspec/changes/editorial-memory-slices-4-7/{spec.md, verification-report.md, audit-report.md}`; this log; `docs/CURRENT.md`. `models.py` remains untouched — the in-place tombstone and its filtering both live entirely in `store.py`, keeping Slice 1's own model/parsing code exactly as it was.

**Reason:** The storage-architecture finding is the most consequential of this round: it establishes that a fix can be internally correct and thoroughly tested (as Repair 2's tombstone approach was) and still fail an audit for exceeding what was actually approved — "no storage-architecture expansion" is a real constraint, not just a style preference, and a new top-level directory is exactly that kind of expansion regardless of how safely it behaves. The `_save_item` defect this fix's implementation surfaced is a reminder that redesigning *how* something is stored can silently break *other* code that assumes the old storage shape, even when that other code is never touched directly — worth re-deriving from scratch after any storage-layer change, not just from the audit's own stated finding list.

**What was deliberately not done:** no commit, push, PR, or merge — local working tree only, per explicit instruction; no architecture redesign beyond what findings 1-3 require; no change to Slice 5/6 behavior, EM-01–EM-04, Slice 2 bootstrap, or Slice 3 retrieval (all confirmed unaffected by the full suite passing unmodified); no new relation, status, or error type; no Browser Verification; no Documentation Intelligence; no Slice 8; no Safe PPT Engine changes; no Obsidian changes; Round 2's finding-3 contract ambiguity (skip vs. raise on corrupt dedup evidence) remains flagged, not resolved, since this round's findings did not touch it.

**Status:** Active, local only. Independent re-audit found 2 further code findings (tombstone validation strictness, array slot/order preservation) plus GOV-01/GOV-02 a fourth time — all fixed as local Repair 4, see PROJ-027.

---

## PROJ-027 — Editorial Memory Slices 4–7 Repair 4 (tombstone validation strictness, slot/order preservation, GOV-01/GOV-02 a fourth time) — local only, not merged

**Decision:** Independent Codex audit of Repair 3 (PROJ-026) found 2 further code findings and 2 governance findings (GOV-01, GOV-02, a fourth time). Per explicit task instruction, all 4 are fixed as **local, uncommitted working-tree changes only** — not committed, pushed, or opened as a PR; `main` is unaffected and still reflects merged Slices 1–3 (PR #12, commit `98c55f3839c66221ae3b8389b15c627d74b4be03`).

- **Tombstone validation not strict:** Repair 3's tombstone recognition treated *any* dict containing a `purged_at` key as a tombstone, with no check of its remaining shape or identity. A real `KnowledgeState` that somehow acquired a stray `purged_at` field (corruption, a bug, tampering) would have been silently filtered out of `states` as if it were a legitimate tombstone — silently hiding current/history truth, the exact failure mode this whole system exists to prevent, now recurring one layer deeper (in tombstone *recognition* rather than the storage location Repair 3 fixed). Fixed: new `store.is_tombstone_entry(entry, item_id)` requires the entry's keys to match the canonical contract *exactly* — `{id, purged_at, purged_by, reason}`, nothing more or less — and its `id` to parse as `"<item_id>#<version>"` for the containing item specifically. Any `purged_at`-bearing dict that doesn't match either check raises typed `StorageCorruptionError` — never silently resolved as a tombstone, never silently resolved as a real state, never a raw `KeyError`/`IndexError`. This single function is now the one place every entry-classification decision goes through: `load_knowledge_item`'s filter, `tombstoned_versions`, `_save_item`'s merge, and `purge_knowledge_state`'s target lookup — replacing four separate ad-hoc `"purged_at" in entry` checks that had each independently made the same too-loose assumption.
- **Tombstone slot/order not preserved:** Repair 3's `_save_item` fix correctly stopped tombstones from being silently dropped, but merged them back by *prepending* them ahead of the real states being written — satisfying "not lost" while violating "same slot," and not preserving the relative order of real states around them either. Fixed: `_save_item` now walks the *existing raw array*, position by position — a tombstone entry is carried through completely unchanged at its own index; a real-state entry at a version this call already knew about is replaced in place with its updated dict (content/status/etc. may have changed via `approve_state`/`invalidate_state`); only a version that didn't exist in the raw record at all yet (a brand-new state `propose_state` just appended) is added at the end. A tombstone now survives any number of subsequent, unrelated mutations — and a fresh reload — in the exact array slot it was created in.
- **GOV-01 (again):** two stale spots in Repair 3's own governance wording were found and corrected: "both rounds" (a phrase left over from before Repair 3 itself existed, describing only Repairs 1–2), and a reference to `load_knowledge_state_tombstone` — the accessor Repair 3 itself removed — surviving in a Data-preservation gate row. A separate arithmetic error was also found: Repair 3's own report conflated two *different* test sets — Repair 2's 7-test tombstone-*contract* set (which included three `test_em47_04_*` tests) and Repair 3's own, smaller 6-test accessor-*removal* set (`test_s7_14`, `test_s7_15`, `test_s7_18`, two `test_finding5_*` tests, one E2E test — the `test_em47_04_*` tests never called that accessor, so none needed touching in Repair 3) — as if they were identical. Both sets are now described separately with their actual, correct membership.
- **GOV-02 (again):** `docs/CURRENT.md` still described only 3 completed audit/repair rounds. Corrected to state that Slices 4–7 have now been independently audited **four** times, with comprehensive re-audit (covering all four rounds) and integration remaining the pending next steps — not the audits themselves.

10 new adversarial regression tests added (`editorial-memory/tests/test_repair4_tombstone_validation.py`; 231 total in the suite, all 221 prior tests passing, no existing test required updating this round): a real state with a stray `purged_at` field raising `StorageCorruptionError` from `get_knowledge_item`/`get_current_by_key`/`get_history_by_key` alike (never silently hidden); a malformed tombstone missing a required field; a tombstone with an extra, unapproved field; a tombstone whose `id` names the wrong item; a tombstone whose `id` has a non-numeric version suffix; an explicit proof that no raw `KeyError`/`IndexError`/`TypeError` ever escapes the validation boundary; a direct unit test of `is_tombstone_entry` covering all four outcomes (ordinary state, two ambiguous-shape rejections, one valid tombstone); a tombstone surviving in its original array slot across a later, unrelated `propose_state` call; the same proven across multiple further mutations (`invalidate_state`, another `propose_state`) and a fresh reload; and multiple tombstones at different original positions each independently staying put while new states are appended only at the end.

`python scripts/check_pipeline.py` passes; `py_compile` is clean on all changed files; `git diff --check` passes; `editorial-memory/tests/` (231 tests) passes in full; Safe PPT Engine code was not touched. Files changed: `editorial-memory/lib/{memory,store,purge}.py` (no `errors.py`/`__init__.py` change — no new error type needed, `is_tombstone_entry` raises the existing `StorageCorruptionError`); `editorial-memory/tests/test_repair4_tombstone_validation.py` (new); `openspec/changes/editorial-memory-slices-4-7/{spec.md, verification-report.md, audit-report.md}`; this log; `docs/CURRENT.md`. `models.py` remains untouched across all four repair rounds.

**Reason:** Both code findings are instances of the same lesson this whole repair sequence keeps re-teaching: a mechanism can be *safe in the cases it was designed for* and still be *unsafe on inputs it didn't anticipate* (finding 1: a corrupted real state that happens to look tombstone-adjacent) or *incomplete in a property nobody separately checked* (finding 2: "not lost" quietly standing in for "not moved"). Neither gap was hypothetical — finding 1 directly threatens the same "silently hide current/history truth" failure this project treats as its most serious defect class; finding 2, left unfixed, would have made `states`' on-disk ordering drift from its append-only design intent the first time any tombstone coexisted with a later mutation.

**What was deliberately not done:** no commit, push, PR, or merge — local working tree only, per explicit instruction; no architecture redesign; no change to Slice 5/6 behavior, EM-01–EM-04, Slice 2 bootstrap, or Slice 3 retrieval (all confirmed unaffected by the full suite passing unmodified); no new relation, status, or error type; no Browser Verification; no Documentation Intelligence; no Slice 8; no Safe PPT Engine changes; no Obsidian changes; Round 2's finding-3 contract ambiguity (skip vs. raise on corrupt dedup evidence) remains flagged, not resolved, since this round's findings did not touch it.

**Status:** Active, local only. Comprehensive independent re-audit of Slices 1–7 together, covering all four repair rounds, pending.

## PROJ-028 — Browser Verification Pilot, Engineering Slice 1 (+ Repair Round 1) — local only, not merged

**Decision:** Implemented `editorial-memory/lib/browser_evidence.py`, the smallest safe bridge from a bounded, structured browser observation to Editorial Memory Evidence, per the founder-locked [[Browser Verification Pilot Requirement]] (Obsidian `BDE-DEC-0004`) — Evidence only, never automatic Knowledge creation/approval, reusing the existing, unmodified `EditorialMemory.record_evidence`/`models.Evidence`. Independent **Audit 1 returned FAIL**, finding 4 findings (2 code-strictness, 1 coverage gap, 1 governance-completeness gap), all fixed as a **local, uncommitted working-tree repair (Repair Round 1), in this same round** — not committed, pushed, or opened as a PR; `main` is unaffected and still reflects merged Slices 1–7 (PR #13, commit `31a5ae1e725c3377f2510dc38c8d85c883286224`). **Closure Audit 2 also returned FAIL** with 3 further findings against this round's own fix — see `PROJ-029` for that repair.

- **Finding 1 (strict persisted notes parsing)**: `BrowserObservation.from_notes()` used a subset key check (silently accepted extra keys), compared `schema` with `!=` (a boolean `schema` would have silently passed, since `True == 1` in Python), and called `tuple(...)` on the three list fields without confirming they were JSON arrays first (a persisted string would have been silently character-split). Fixed: exact key-set equality; `type(schema) is not int` (excludes `bool` by type, not just value); new `_require_json_str_list` helper enforces list-type/non-empty/non-blank-string-entries before any coercion; `workflow`/`uncertainty` now explicitly type-checked.
- **Finding 2 (strict write boundary)**: `record_browser_observation`'s helper called `tuple(value)` directly on caller input — a bare string was silently character-split, a non-iterable value raised a raw untyped `TypeError`, a `set`/`frozenset` produced nondeterministic serialization order; `verification_scope` and `evidence_quality` received no validation of their own. Fixed: new `_require_ordered_str_sequence` helper explicitly rejects `str`/`bytes`/`set`/`frozenset`/non-iterable/non-string-entries, applied to `direct_observations`/`inferences`/`unknowns`/`verification_scope`; `evidence_quality` now checked via `isinstance(..., EvidenceQuality)`.
- **Finding 3 (regression coverage)**: 41 new tests added (`test_browser_evidence.py`, 22 → 63) covering every malformed shape in findings 1–2 on both boundaries, an explicit `captured_at` round-trip (present and omitted), and a deterministic-ordering proof.
- **Finding 4 (canonical governance records)**: this change had no `openspec/changes/` classification/verification/audit record set at all. Added: `openspec/changes/browser-verification-pilot-engineering-slice-1/{spec.md, verification-report.md, audit-report.md}`. `docs/CURRENT.md`'s one stale line claiming no `browser_observation` producer exists yet is corrected; no other pre-existing governance drift (e.g. the separately-flagged, larger `docs/CURRENT.md` staleness about Editorial Memory Slices 4–7's actual merged state) is touched by this round — out of scope.

330 tests total in the `editorial-memory` suite (267 prior + 63 in `test_browser_evidence.py`: 22 from the initial implementation, 41 new this round), zero pre-existing tests modified or weakened. `python scripts/check_pipeline.py` passes; `py_compile` is clean; `git diff --check` passes. Files changed: `editorial-memory/lib/browser_evidence.py` (new), `editorial-memory/lib/errors.py` (2 new error classes), `editorial-memory/lib/__init__.py` (exports), `editorial-memory/tests/test_browser_evidence.py` (new); `openspec/changes/browser-verification-pilot-engineering-slice-1/{spec.md, verification-report.md, audit-report.md}` (new); this log; `docs/CURRENT.md` (one line). `models.py`, `store.py`, `memory.py`'s `record_evidence`, `purge.py`, `review.py`, `retrieval.py` remain unmodified.

**Reason:** Both code findings are the same class of gap this repository's Editorial Memory history keeps re-finding: a boundary that correctly handles the inputs it was designed for, but silently mishandles (rather than typed-rejects) an input shape nobody explicitly tested — character-splitting a string mistaken for a collection, or a boolean silently satisfying an integer equality check. Neither is hypothetical: both are exactly the kind of hand-edited-or-malformed persisted data / caller-error input this repository's typed-error convention exists to catch before it becomes silent data corruption or an unhandled crash.

**What was deliberately not done:** no commit, push, PR, or merge — local working tree only, per explicit instruction; no architecture/lifecycle/model/storage redesign; no change to `models.py`/`store.py`/`memory.py`'s `record_evidence`/`purge.py`/`review.py`/`retrieval.py`; no automatic Knowledge promotion; no generalized/automated browser-driving framework; no Documentation Intelligence; no declaration that the Browser Verification Pilot adoption gate (which also requires the AI-understanding dimension and independent audit) is cleared; the pre-existing, separately-flagged `docs/CURRENT.md` drift regarding Editorial Memory Slices 4–7's actual integration state is left untouched, out of this round's scope.

**Status:** Superseded by `PROJ-029` (Closure Audit 2 found this round's own fix still insufficiently strict). Active, local only, not merged.

## PROJ-029 — Browser Verification Pilot, Engineering Slice 1, Repair Round 2 (duplicate JSON keys, oversized JSON integer, governance chronology) — local only, not merged

**Decision:** Closure Audit 2 of Repair Round 1's diff (`PROJ-028`) returned **FAIL**, finding 3 further findings — 2 remaining code-strictness gaps in `BrowserObservation.from_notes()`'s JSON-decode boundary, and 1 governance-chronology misstatement in this change's own `audit-report.md`. Per explicit task instruction, all 3 are fixed as **local, uncommitted working-tree changes only, in this same round** — not committed, pushed, or opened as a PR; `main` is unaffected and still reflects merged Slices 1–7 (PR #13, commit `31a5ae1e725c3377f2510dc38c8d85c883286224`).

- **Finding 1 (duplicate JSON keys)**: `BrowserObservation.from_notes()` called plain `json.loads(notes)`, which silently accepts a JSON object containing a duplicate member name and keeps only the last-seen value (confirmed: `json.loads('{"workflow":"a","workflow":"b"}')` returns `{'workflow': 'b'}`, never rejected). Fixed: `json.loads` is now called with a custom `object_pairs_hook` (`_strict_object_pairs_hook`) that raises typed `MalformedBrowserObservationError` the instant a duplicate key is found, at any nesting level.
- **Finding 2 (oversized JSON integer raw `ValueError`)**: a JSON integer literal with an extreme digit count anywhere in the document (e.g. a 5000-digit `schema` value) reached Python's own int-string-conversion guard *inside* `json.loads` itself, raising a raw `ValueError` (confirmed: `json.loads` on such a literal raises `ValueError: Exceeds the limit (4300 digits) for integer string conversion...` directly from the decoder) before this module's own `try`/`except` around `json.loads` could distinguish it from a genuine parse failure — and that guard's threshold varies by Python version and is process-configurable (including disable-able via `sys.set_int_max_str_digits(0)`). Fixed: `json.loads` is now also called with a custom `parse_int` hook (`_strict_parse_int`) enforcing a fixed, version-independent digit-count bound (`_MAX_JSON_INT_DIGITS = 18`) before ever calling `int()` on the literal's text, raising typed `MalformedBrowserObservationError` deterministically regardless of Python version/configuration. Both fixes cover `get_browser_observation()` automatically, since it calls `from_notes()` internally.
- **Finding 3 (governance chronology)**: `audit-report.md`'s own prior verdict misstated Audit 1 as "PASS WITH FINDINGS" rather than **FAIL** — a materially different verdict. Fixed: `audit-report.md` rewritten to state the accurate chronology (Audit 1 FAIL → Repair Round 1 → Closure Audit 2 FAIL → Repair Round 2 → next closure audit pending); `spec.md` and `verification-report.md` updated where test counts/round numbering had gone stale as a direct consequence of this round (neither had itself misstated a verdict, since neither carries one); `docs/CURRENT.md` and this log corrected to match.

11 new tests added (`test_browser_evidence.py`, 63 → 74): duplicate `workflow` key specifically; duplicate keys at three other field positions; a 5000-digit `schema` integer through both `from_notes()` and `get_browser_observation()`; combined no-raw-exception proofs for both bug classes; a valid-payload-still-parses sanity check. 341 tests total in the `editorial-memory` suite (330 prior + 11 new), zero pre-existing tests modified or weakened. `python scripts/check_pipeline.py` passes; `py_compile` is clean; `git diff --check` passes. Files changed: `editorial-memory/lib/browser_evidence.py`; `editorial-memory/tests/test_browser_evidence.py`; `openspec/changes/browser-verification-pilot-engineering-slice-1/{spec.md, verification-report.md, audit-report.md}`; this log; `docs/CURRENT.md` (no content change beyond what Repair Round 1 already made — reviewed, found not further stale). `models.py`, `store.py`, `memory.py`'s `record_evidence`, `purge.py`, `review.py`, `retrieval.py`, `errors.py`, `__init__.py` remain unmodified by this round.

**Reason:** Both code findings are the same class of gap Repair Round 1 itself was created to close, recurring one layer deeper: Round 1 hardened this module's *own* validation logic against malformed input, but the JSON *decoder itself* (`json.loads`, called before any of that logic runs) had two of the identical failure modes - silent last-value-wins on an ambiguous input (duplicate keys, echoing Round 1's "don't silently coerce" principle) and an unbounded, environment-dependent conversion guard (oversized integers, echoing this repository's own established `_TOMBSTONE_VERSION_MAX_DIGITS` precedent in `store.py` for the identical class of bug). Neither is hypothetical - both were independently reproduced as real, current-Python behavior before being fixed.

**What was deliberately not done:** no commit, push, PR, or merge — local working tree only, per explicit instruction; no architecture/lifecycle/model/storage redesign; no change to `models.py`/`store.py`/`memory.py`'s `record_evidence`/`purge.py`/`review.py`/`retrieval.py`/`errors.py`/`__init__.py`; no automatic Knowledge promotion; no generalized/automated browser-driving framework; no Documentation Intelligence; no declaration that this change PASSed an audit or that the Browser Verification Pilot adoption gate is cleared; the pre-existing, separately-flagged `docs/CURRENT.md` drift regarding Editorial Memory Slices 4–7's actual integration state remains untouched, out of this round's scope; `parse_float` was left unmodified (out of scope - only integer literals were named in the finding, and floats do not have Python's int-string-conversion-guard failure mode).

**Status:** Active, local only. A further independent closure audit of this local diff (initial implementation + Repair Round 1 + Repair Round 2 together) is the required next action before any commit/push/PR/merge. This change has not passed an audit and is not being declared complete.

## PROJ-030 — Documentation Intelligence Slice 1 (Build 1 + Audit 1 FAIL + Repair Round 1), and governance reconciliation — local only, not merged

**Decision:** Per the founder-confirmed live Obsidian checkpoint (`Implementation/Editorial Memory Real-World Workflow Validation Closure Checkpoint.md`, 2026-08-22 — implementation validation PASS, real-world workflow validation PASS, Editorial Memory phase CLOSED), Documentation Intelligence was authorized to begin. Slice 1 — a read-only Editorial Memory delta-comparison layer, `documentation-intelligence/documentation_intelligence/compare.py` — was built, then independently audited. **Audit 1 returned FAIL**, finding 4 code defects plus 3 governance defects, all fixed as a **local, uncommitted working-tree repair (Repair Round 1), in this same round** — not committed, pushed, or opened as a PR; `main` is unaffected.

- **Finding 1 (caller-supplied relation treated as authoritative)**: Build 1 accepted `asserted_relation`/`asserted_documentation_vs_product` parameters and reported them essentially as-is, contrary to the founder's post-audit clarification that Slice 1 must derive relation itself from bounded deterministic rules, defaulting to unresolved otherwise. Fixed: both parameters removed. `relation_candidate` is now derived internally — `NEW` (no current product knowledge + relevant Evidence) or `CONFIRMS` (incoming claim text exactly equal, whitespace-normalized only, to current content) are the only two reachable non-null outcomes; everything else, including anything that would eventually be `REFINES`/`CONTRADICTS`/`SUPERSEDES`, is `unresolved` with a stated reason. Distinguishing those three relations from arbitrary differing text requires semantic judgment (LLM/embeddings/fuzzy matching) explicitly barred from this slice — a deliberate scope boundary, not an oversight.
- **Finding 2 (Evidence sufficiency = mere existence)**: any resolvable Evidence id previously counted as sufficient regardless of relevance. Fixed: a cited Evidence id now counts toward `evidence_sufficiency` only if it already appears in the retrieved current product state's own `evidence_refs`, or its `verification_scope` intersects a caller-supplied `expected_verification_scope` — unrelated, adjacent, mismatched-scope, and absence-only Evidence no longer qualify.
- **Finding 3 (no Knowledge-type enforcement)**: `product_key`/`documentation_key` resolution now enforces `KnowledgeType.PRODUCT`/`KnowledgeType.DOCUMENTATION` respectively via new `_resolve_typed_current`, raising the existing, reused `KnowledgeTypeMismatchError` on a role/type mismatch rather than silently accepting it.
- **Finding 4 (import-collision defect: duplicate class/Enum/exception identities depending on import order)**: Build 1's bridge module loaded `editorial-memory/lib` under a fixed private alias from inside a sibling package that was itself also named `lib` (`documentation-intelligence/lib`), colliding with `editorial-memory/tests/test_editorial_memory.py`'s own established bare-`lib` import convention — whichever side imported first in a process determined which got a duplicate, non-`is`-identical copy of `Relation`/`KnowledgeType`/every exception class. Fixed with a permanent structural change, not a patch: the package was renamed `documentation-intelligence/lib/` → `documentation-intelligence/documentation_intelligence/`, removing its own contention for the name `lib` entirely; the bridge now claims the canonical name `lib` for `editorial-memory/lib` when free (private-alias fallback only if `lib` is bound to something unrelated) and separately scans `sys.modules` by resolved file path to reuse an already-loaded copy under any name. Verified against the exact adversarial condition: `editorial-memory`'s and `documentation-intelligence`'s full test suites run together in one `pytest` process, both collection orders — 371/371 passing, with `documentation-intelligence/tests/test_import_order.py` asserting `is`-identity explicitly.
- **Finding 5 (test coverage)**: adversarial tests added for unrelated Evidence, mismatched verification scope, matching verification scope, absence-only evidence, both swapped-Knowledge-type directions, both import orders, byte-level (sha256, not id-list) real-store immutability on every real-data test, and relation reasoning as Slice 1's own derivation rather than a caller assertion (including an explicit "obviously-contradicting text still stays unresolved" case).
- **Finding 6 (missing governance records)**: `openspec/changes/documentation-intelligence-slice-1/{spec.md, verification-report.md, audit-report.md}` created, with an explicit process-deviation disclosure (implementation and Audit 1 both preceded these records' creation).
- **Finding 7 (stale repository governance/status documentation)**: `docs/CURRENT.md` reconciled — the phase-status paragraph, the "Active change" pointer, the "Not included/not started" Documentation Intelligence bullet, the "Current blocker" item (5), and the "Exact next action" section all previously stated or implied Editorial Memory Slices 4–7 were local-only/unmerged and Editorial Memory was not closed. Per git history (`31a5ae1e725c3377f2510dc38c8d85c883286224`, PR #13) and the live Obsidian checkpoint, both were stale. Each corrected section is left with the original text intact underneath an explicit "Reconciliation correction (`PROJ-030`)" note — no sentence was deleted or silently rewritten, and no architecture was changed.

28 Documentation Intelligence tests (`documentation-intelligence/tests/test_compare.py`, `test_import_order.py`); combined with the unmodified `editorial-memory` suite, 371 tests total pass in one process. `python scripts/check_pipeline.py` still passes; `py_compile` and `git diff --check` are clean; the real Editorial Memory store (`documentation-artifacts/bridge-ui-evidence/store`) is confirmed byte-for-byte (sha256) unchanged by the full test run. `editorial-memory/` is completely unmodified by this change, in Build 1 and in this repair round alike. See `docs/DECISIONS.md` history above for the Editorial Memory/Safe PPT Engine/Browser Verification work this phase builds on; see `openspec/changes/documentation-intelligence-slice-1/` for the full per-finding record.

**Reason:** Codex's findings were all instances of the same underlying risk: a bounded, read-only reasoning layer whose safety guarantees (never assert unverified product truth, never silently accept a malformed reference, never create duplicate object identity depending on incidental import order) were stated as intentions in the specification but not yet fully enforced in the code itself. Each fix closes the gap between the approved specification's stated invariant and what the implementation actually guaranteed structurally.

**What was deliberately not done:** no commit, push, PR, or merge — local working tree only, per explicit instruction; no change to any file under `editorial-memory/`; no PPT editing; no claim extraction; no live Bridge exploration; no new Evidence collection; no Knowledge approval or proposal; no Obsidian modification; no architecture redesign or reopening of Architecture v0.1; no declaration that this change PASSed an audit or that Documentation Intelligence Slice 1 is complete/closed/integrated.

**Status:** Active, local only, not merged. A further independent Codex re-audit of this repaired local diff is the required next action before this change is committed, pushed, merged, or used as a foundation for further Documentation Intelligence work.

## PROJ-031 — Documentation Intelligence Slice 1, Repair Round 2 (Re-audit 1 findings DI-S1-01/02/04, TEST-DI-01, GOV-DI-01, GOV-DI-03) — local only, not merged

**Decision:** Independent Codex Re-audit 1 of `documentation-intelligence-slice-1`'s Repair Round 1 diff (`PROJ-030`) returned **FAIL**, finding 3 code defects and 3 governance/test defects — each a case where Repair Round 1's own fix for an Audit 1 finding was itself insufficiently strict. Per explicit founder instruction, the deterministic relation model itself (NEW/CONFIRMS/unresolved, REFINES/CONTRADICTS/SUPERSEDES unreachable) is accepted and was not redesigned; only the following are fixed, as a local, uncommitted working-tree repair (Repair Round 2), in this same round — not committed, pushed, or opened as a PR; `main` is unaffected.

- **DI-S1-01 (Evidence relevance still exploitable)**: Repair Round 1's relevance check accepted a caller-supplied `expected_verification_scope` intersecting an Evidence's own `verification_scope` as an alternative to the already-linked-evidence test — both are caller/Evidence-supplied inputs to a single `compare()` call, so a caller could fabricate an apparent match for genuinely unrelated Evidence, producing a positive `NEW`/`CONFIRMS` result with no real backing. Fixed: `expected_verification_scope` removed from `compare()`'s signature entirely. Relevance is now exactly one test — the Evidence id already appears in `evidence_refs` of *any* state (any lifecycle status, not only current) already recorded on the `product_key` KnowledgeItem — an independently persisted relationship established through Editorial Memory's own normal lifecycle at some point in the past, never through anything the `compare()` call's own arguments assert. If `product_key` resolves to no KnowledgeItem at all, the comparison fails conservatively (`insufficient`/`unresolved`) rather than substituting any other signal.
- **DI-S1-02 (KnowledgeType enforcement escapable via non-current items)**: Repair Round 1's `_resolve_typed_current` only type-checked the result of `get_current_by_key`, so an item with no current state (only proposed, or only invalidated/superseded) skipped the check and silently resolved as if nothing existed. Fixed: new `_resolve_typed_item` resolves and type-checks the exact referenced KnowledgeItem unconditionally, regardless of lifecycle status, before any current-state lookup — applied identically to `product_key` and `documentation_key`, including when `include_proposed_documentation=False`.
- **DI-S1-04 (alias-first import identity gap)**: Repair Round 1's bridge correctly reused an already-loaded copy of `editorial-memory/lib` found under a foreign alias, but never additionally registered it under the canonical name `lib` — a later, unrelated plain `import lib` elsewhere in the same process would find `lib` unclaimed and freshly re-import from disk, creating a second, non-`is`-identical identity. Fixed: new `_canonicalize_under_lib` registers an alias-discovered module (and its already-imported submodules) under `lib` too; a genuine name conflict (an unrelated module already bound to `lib`) now raises new `EditorialMemoryImportConflictError` explicitly rather than silently proceeding under a private alias that could diverge. Verified via a new, fully-isolated `subprocess`-based regression (`test_import_alias_first.py`) that reproduces the exact ordering Codex found, independent of this suite's own file-collection order.
- **TEST-DI-01 (missing byte snapshot)**: one real-data test lacked the before/after whole-store byte-hash bookend every other real-data test already had. Fixed: added; every `TestRealRegressionCases` test now has it uniformly, with no documentation-claim weakening.
- **GOV-DI-01 (OpenSpec completeness)**: `spec.md` lacked the `docs/TESTING.md`-required Classification section. Fixed: added (Standard base tier; rationale; both specialized evidence profiles explicitly marked not-applicable with reasoning; independent-audit requirement stated); the audit record (`audit-report.md`) rewritten to accurately represent both audit rounds' full finding sets, including this finding.
- **GOV-DI-03 (stale `docs/INDEX.md`)**: `docs/INDEX.md` described `documentation-artifacts/bridge-ui-evidence/` as "empty until Browser Verification work resumes" — false, and had been false since well before this slice began (38 Evidence records, 2 KnowledgeItems already present). Fixed: `docs/INDEX.md` corrected to describe the real, populated store, with new bullets explicitly delimiting Documentation Intelligence Slice 1's own integration scope (reads already-persisted Editorial Memory records only, via the unmodified `editorial-memory/lib` API) from the unrelated raw `masterslide/` decks, `Expert resources/` videos, and build caches — none of those are silently folded into this or any other change's scope merely by being present in the working tree.

32 Documentation Intelligence tests (up from 28 after Repair Round 1: `test_compare.py` plus new `test_import_alias_first.py`); combined with the unmodified `editorial-memory` suite, 375 tests total pass in one process, confirmed in both directory orders. `python scripts/check_pipeline.py` still passes; `py_compile` and `git diff --check` are clean; the real Editorial Memory store is confirmed byte-for-byte (sha256, unchanged hash from the Repair Round 1 baseline) unchanged by the full test run. `editorial-memory/` remains completely unmodified. See `openspec/changes/documentation-intelligence-slice-1/` for the full per-finding record.

**Reason:** every Re-audit 1 finding was the same underlying shape of gap: a safety property Repair Round 1 correctly identified and partially addressed, but did not close for every reachable code path (an alternate relevance test, a lifecycle-status branch, a second import ordering). Each fix closes the specific remaining path rather than broadening the mechanism's scope beyond what was found.

**What was deliberately not done:** no commit, push, PR, or merge — local working tree only; no change to any file under `editorial-memory/`; no change to the accepted deterministic relation model (NEW/CONFIRMS/unresolved unchanged, REFINES/CONTRADICTS/SUPERSEDES still unreachable, not added); no LLM, embeddings, vector search, fuzzy semantic matching, or confidence scoring introduced; no Editorial Memory writes added; no PPT editing; no claim extraction; no live Bridge exploration; no new Evidence collection; no Knowledge approval or proposal; no Obsidian modification; no declaration that this change PASSed an audit or is complete/closed/integrated.

**Status:** Active, local only, not merged. A further independent Codex re-audit of this repaired local diff is the required next action before this change is committed, pushed, merged, or used as a foundation for further Documentation Intelligence work.

## PROJ-032 — Presentation Exploration Layers 1–4: founder approval of the Current Editorial Style candidate

**Decision:** The founder reviewed `documentation-artifacts/masterslide/exploration/layer4_founder_validation_packet.md` (the compact founder-facing review packet from the `presentation-exploration-layers-1-4` research change) and returned **APPROVE**, with an explicit relationship-aware exception rule attached. The approval covers exactly two, inseparable parts:

1. **Approved default Bridge4PS editorial grammar** — clean numbered instructions at the top; concise instructional text with clear hierarchy; large contextual screenshots below; red highlights around only the exact actionable UI target; good whitespace/visual hierarchy; restrained use of arrows, connectors, pointers, and similar annotation objects. This corresponds to the *Procedural Hero Screenshot* family (B1/B2/B3) already described as the Current Editorial Style candidate in `candidate_presentation_memory.md` §3.
2. **Relationship-aware exception rule** — "restrained use of arrows/connectors" does not mean removed whenever possible. Gray pointers, arrows, connectors, dividers, callouts, and highlight relationships remain part of the approved visual grammar whenever they perform a real instructional function (exception conditions, dependencies, source→target relationships, before/after progression, explanatory callouts, text-to-screenshot-region associations, cross-UI-area relationships). These must be preserved and intelligently maintained during future editing — repositioned/reflowed if what they relate to moves, never crumpled, misdirected, stranded, or deleted merely because the default style is cleaner. Founder principle, verbatim: *"Make the slide clean while preserving purposeful visual relationships that explain the content."*

Per this repository's evidence-preservation convention (the same one this file's own history follows for supersession vs. invalidation), the approval is recorded as a **new, distinct artifact** — `documentation-artifacts/masterslide/exploration/founder_approved_presentation_style.md` — rather than by mutating the candidate research in place. The original six Layers 1–4 exploration deliverables (`layer1_range_evidence.md`, `layer1b_anomaly_audit.md`, `layer2_specialist_synthesis.md`, `layer3_reconciliation.md`, `candidate_presentation_memory.md`, `layer4_founder_validation_packet.md`) are unmodified in substance; `candidate_presentation_memory.md` and `layer4_founder_validation_packet.md` each gained only a short status-pointer note toward the new artifact, with their original evidence, provenance, and unresolved ambiguities left exactly as written. `openspec/changes/presentation-exploration-layers-1-4/verification-report.md` and `docs/CURRENT.md` were updated to reflect founder-validation-complete status for this one finding.

**What this decision does NOT do:** it does not resolve any other candidate finding from this research (the exact PDF p.131→132/133 and p.133→134/135 occurrence ambiguities, slide 141's UNRESOLVED anomaly status, the duplicate-topic divergence pairs' historical cause, full PPT↔PDF correspondence, or the large Baseline structural cluster's internal visual homogeneity — all remain exactly as unresolved as before); it does not separately rule on whether the Relationship-Heavy/Dense family (B4, cluster 2) itself counts as part of the approved default grammar for complex multi-target actions; it does not constitute or substitute for independent Codex audit — the earlier Presentation Exploration micro-closure audit (Fix 1/2/3) had already PASSED with PE-01 through PE-08 CLOSED, and founder approval does not waive the separate founder-approval-delta audit/integration gate, which remains pending in its own right; it does not authorize commit, push, or merge of any Presentation Exploration file; and it does not authorize starting Presentation Editing Intelligence / Scene-Aware Editing, which requires its own separate founder/orchestrator authorization. It does establish that phase's key implementation requirement: preserving semantic visual relationships (belongs-to, moves-with, points-to, highlights, separates, anchored-to, ordered-before/after, aligned-with, repeated-with, contained-by, depends-on) during future edits — not implemented by this decision or by any code in this repository.

**Reason:** This mirrors the same pattern this repository's governance has applied consistently elsewhere (Editorial Memory's `propose_state`/`approve_state` separation, this file's own supersession-not-deletion convention): a founder decision on one specific, scoped question is recorded precisely, without either overstating its reach into adjacent unresolved questions or requiring the underlying evidence record to be rewritten to reflect it.

**What was deliberately not done:** no commit, push, PR, or merge; no change to Layer 0 or the canonical PPTX (SHA-256 unchanged: `e461613baae2874eaeede3268fff1aee081c33a04a6bcbb0c7769b94bab16834`); no rewriting of any of the six original exploration deliverables' evidence content; no resolution of any of the explicitly-listed still-unresolved research questions; no Presentation Editing Intelligence implementation or design; no scope broadening beyond recording this one founder decision.

**Status:** Local only, not committed, pushed, or merged. **Correction (recorded in `PROJ-033`, not a new event):** the separate mapping/governance micro-repair pass in `openspec/changes/presentation-exploration-layers-1-4/` (Fix 1/2/3) had, in fact, already received its own Codex closure audit — **PASS**, PE-01 through PE-08 CLOSED — before this entry was originally written; this entry's original text describing that audit as "still-pending" was stale, not a description of a later event. Codex Audit 1 of this founder-approval delta itself then returned **FAIL**, for two governance-only findings that included this exact staleness (GOV-STYLE-01: stale audit-chronology claims in `docs/CURRENT.md`/`docs/DECISIONS.md`/the verification report; GOV-STYLE-02: stale scope/count accounting in the verification report) — the substantive approved style, the relationship-aware exception rule, candidate-history preservation, and all technical regression checks PASSED. Both findings are repaired in `PROJ-033` below; the founder-approval delta as a whole is not yet recorded as PASS.

## PROJ-033 — Presentation Exploration founder-approval delta: governance-only repair (GOV-STYLE-01, GOV-STYLE-02)

**Decision:** Codex founder-approval delta Audit 1 (`PROJ-032`) returned **FAIL** for two governance-only findings, with the substantive approval itself, the relationship-aware exception rule, candidate-history preservation, and all technical regression checks confirmed **PASSED**. Both findings are repaired as local, uncommitted working-tree changes in this same round — not committed, pushed, or opened as a PR.

- **GOV-STYLE-01 (stale audit chronology)**: `docs/CURRENT.md`, `docs/DECISIONS.md` (this file), and `openspec/changes/presentation-exploration-layers-1-4/verification-report.md` each contained language describing the Fix 1/2/3 micro-closure audit as still pending and PE-01–PE-08 as still awaiting closure. Both had already resolved (the micro-closure audit returned PASS; PE-01–PE-08 are CLOSED) before the founder-approval delta was recorded, but the governance text had not been updated to say so. Fixed: all three files now state the truthful chronology — Codex Audit 1 FAIL → bounded repair rounds → Fix 1/2/3 micro-closure audit PASS → PE-01–PE-08 CLOSED → founder validation complete → founder-approved style delta created → founder-approval delta Audit 1 FAIL (GOV-STYLE-01/02 only) → this repair → next gate is a narrow governance-only closure re-audit. The founder-approval delta itself is explicitly **not** recorded as PASS by this repair — that verdict is reserved for the next audit round.
- **GOV-STYLE-02 (stale scope/count accounting)**: `openspec/changes/presentation-exploration-layers-1-4/verification-report.md` stated the working tree as `59 total entries: 51 + 8` (stale — the actual count after the founder-approval delta is `61 = 51 + 10`); stated the founder-approval delta touched "exactly 5 paths" while its own prose then listed 6; and stated "not one of the 8 paths above" in the Scope section's Obsidian-exclusion note, when the Scope list itself had already grown to 10 entries. Fixed: all three numbers corrected to the true accounting (61 = 51 + 10; founder-approval delta = 6 paths; "10 paths above").

**What was deliberately not done:** no change to `founder_approved_presentation_style.md`'s substantive content, the approved style, the relationship-aware exception rule, Presentation Memory evidence, Layer 1–4 research, the PPT, Layer 0, or Safe PPT Engine; no commit, push, PR, or merge; no Presentation Editing Intelligence work.

**Status:** Local only, not committed, pushed, or merged. A narrow founder-approval governance closure re-audit, scoped to GOV-STYLE-01 and GOV-STYLE-02 only, is the required next action.
