# Current State

## Repository purpose

Bridge4PS Documentation Engineer product repository.

## Current phase

Repository Hardening Batch 1 completed. Safe PPT Engine Build 1 and Build 2 completed, then through seven independent-Codex-audit rounds: Audit 1 found F-01–F-08 (fixed and merged as Audit Repair 1); Audit 2 (re-audit of Repair 1, commit `13b3b532528b52a808030840734da9e58d92cfeb`) found F-02/F-04/F-05/F-06/R-01/R-02 remaining, F-01/F-03/F-07/F-08 confirmed closed (fixed and merged as Audit Repair 2); Audit 3 (re-audit of Repair 2, against merged commit `69bb4761921ac7f7b843beb04ab255a2b272865e`) found F-05/F-06/R-01/R-02 remaining, F-01/F-02/F-03/F-04/F-07/F-08 confirmed closed (fixed locally as Audit Repair 3); Audit 4 (re-audit of Repair 3's local diff — the first round of this history where Codex reviewed a diff before it was pushed or merged) confirmed R-01 closed and found F-05/F-06/R-02 remaining (fixed locally as Audit Repair 4); Audit 5 (re-audit of Repair 4's local diff) found Repair 4's F-05 fix had closed the temp-pathname leak but not verified the underlying file descriptor stayed closed, plus 2 governance-wording gaps (fixed locally as Audit Repair 5, which added an `os.fstat`-then-retry recovery for the close-failure path); Audit 6 (re-audit of Repair 5's local diff) found that retry logic itself unsafe — retrying or probing a fd after a failed close risks acting on a descriptor already reused by unrelated code — plus 2 further governance corrections, including that this record had previously overstated Audit 5 as having "confirmed" some of Repair 4's fixes closed and had described the unsafe retry as proven safe. Audit 7 (re-audit of Repair 6's local diff) completed with PASS WITH FINDINGS: F-05 is closed, while F-06 and R-02 remain; those were fixed as local Audit Repair 7. **Audit Repairs 3 through 7, together with Repair 7's own governance/test findings, were subsequently committed and integrated into GitHub as a single change (commit `40f3a1083bf8556830abf9552fb186f629412c5c`, merged to `main` as `be6df7fdfe8f6ab4cad24f4c3c3a7408945cb715` via PR #8)** — `main` reflects the full Repair 7 state, not only Audit Repair 2. Independent Codex audit of that integrated state (Audit 8) is the required next action before further Safe PPT Engine work; it does not require anything further to be pushed first, since the integration has already happened. Documentation Intelligence and semantic/AI capability on top of the engine have not started.

## Active change

`safe-ppt-engine` (verification recordkeeping only — `openspec/changes/safe-ppt-engine/spec.md` (classification), `verification-report.md` (evidence), and `audit-report.md` (independent-audit history) record Builds 1–2 and both audit repairs' test evidence and audit status per `docs/TESTING.md`; no product/architecture scope was opened or changed by creating them).

`editorial-memory` (new implementation change, Slice 1 — see `openspec/changes/editorial-memory/spec.md`, `verification-report.md`, `audit-report.md`).

## Completed

- Research: completed and closed.
- Architecture v0.1: founder-approved and locked.
- Universal Agentic Project Pipeline adopted from `capcutfor1month-oss/project-Pipline` at source commit `e755ece0caf62f8e7df1a6c168727049c362cd4c`.
- Pipeline adoption is complete: `scripts/check_pipeline.py` was repaired to validate this target repository truthfully (the inherited README assertion "This is a development-governance repository" was replaced with a target-repository-accurate assertion), and `python scripts/check_pipeline.py` passes with the result `Bridge4PS target-repository pipeline-adoption checks passed.`
- Canonical baseline imported and verified. The canonical ZIP is the complete immutable raw baseline: `baseline/source-artifacts/B4PS-TUT-main.zip` (SHA-256 `00f5e41f475b8205535481da11f73c4e4f0bd614e0d3b1efff938e921b9eb6ee`, unmodified, 58 files, embedded source commit `85af51a123e237c14f61f5bb43094292db664561`) contains every original file. The GitHub materialization at `baseline/B4PS-TUT-main/` contains 50 of those 58 source files, byte-for-byte identical to the ZIP, plus the baseline's own original `.gitattributes` restored unmodified. Eight unresolved Git LFS pointer entries are intentionally omitted from the extracted tree only, because their referenced binary objects are unavailable and GitHub rejects unresolved LFS references (`GH008`). Their exact source representations remain preserved inside the canonical ZIP. This omission is a repository-publication constraint, not a baseline repair — see the manifest below. `python scripts/check_pipeline.py` still passes.

### Omitted Git LFS pointer entries (GitHub materialization only — present in the canonical ZIP)

| Baseline-relative path | LFS OID | Declared size (bytes) |
|---|---|---|
| `.b4ps-tools/wip/new-filters-sidebar-2026-08-01/slides_171_173.pptx` | `sha256:a11552b742cfebe12b5445a399e04d2115949a2be6860e078632ba813c60372e` | 1946671 |
| `.b4ps-tools/wip/new-filters-sidebar-2026-08-01/staged_desktop.pptx` | `sha256:4dd652abbc81b097d8cf117fce311f5cdd290c4f78907bd92f39958e9db6f536` | 149116945 |
| `B4PS_Pipeline_Guide.pdf` | `sha256:3ad794a07ec8d76af8ebfa0ca17eb56b0e37907b7243c9cd6f79ffc712001f00` | 15909 |
| `Current update/Desktop/MASTER Complete Bridge4PS Desktop-Browser Feature Tutorials.pptx` | `sha256:47c12e3fd04ce950ed27d540e2fddc0ff9ef12250ee7a80fa263a5f862d470f4` | 142506574 |
| `Current update/Mobile/Copy of MASTER Complete Bridge4PS Mobile Feature Tutorials.pptx` | `sha256:85f8d522500af5de19226e3146d0d565927478ccb98225ff40ca9d3c624ad5b9` | 183065770 |
| `Source/Design/Slide Sample.pdf` | `sha256:592008b8f42b06520b61b1490836707bdcb02a2c534382007b573d91b6b7b53d` | 396583 |
| `Source/Desktop/Creating slides/pdf/New Filters and secondary sidebar (1).pdf` | `sha256:86311bd2578e961bb4792fecfd4e8bd401b65e7b1d425300b53f36e8541a767f` | 3503140 |
| `Source/Desktop/Screen recording/Notification_threedots.mp4` | `sha256:38565efb71bef3421cf719e15262d0cf722ccbe1266a6c8bf5c78d36d4d3124a` | 9369549 |

Each entry was present in the source ZIP only as Git LFS pointer text (not a real binary); their actual binary objects were never available to this import. GitHub rejected publishing those unresolved references with `GH008`. Their exact pointer representations remain preserved, unmodified, inside the canonical ZIP.

- Repository Hardening Batch 1 completed on the imported baseline tooling at `baseline/B4PS-TUT-main/.b4ps-tools/`. Hardened categories: reproducibility (removed a hardcoded personal absolute path from `scripts/capture_and_rename.sh`; added `requirements.txt`/`requirements-dev.txt` for previously-undeclared `Pillow`/`numpy`/`opencv-python-headless` dependencies), failure behavior (missing/empty/corrupt deck `.pptx` sources now raise a clear `MissingDeckSourceError` with an actionable message instead of crashing with a raw traceback, wired into `DeckReader`, `backup()`, and the CLI's top-level error handling), and operator usability (`.b4ps-tools/README.md` gained a Setup section, a Running-tests section, and an explicit note that the 2 production decks are currently unavailable in this checkout). 16 new automated tests were added under `.b4ps-tools/tests/` (`pytest`), all passing, covering the fixed failure paths (not just happy paths) using small synthetic fixtures — no production PPTX content was fabricated. `python scripts/check_pipeline.py` still passes; the canonical ZIP was not touched. See `docs/DECISIONS.md` for the full record and `docs/TESTING.md` for what remains deliberately unhardened.
- Safe PPT Engine Build 1 completed: `baseline/B4PS-TUT-main/.b4ps-tools/lib/ppt_engine.py`, built on `python-pptx` (added to `requirements.txt`), provides deck-agnostic, non-destructive primitives — `load_deck` (typed `DeckSourceError` on missing/empty/corrupt input, never falls back to another file), `inspect_deck` (deterministic structural JSON: slide count, dimensions, per-shape id/name/type/position/text/image-presence/z-order — no semantic interpretation), `create_working_copy` (mutation always stages on a private temp copy, never the original or the final output path directly), and `set_shape_text` (the one controlled mutation primitive built this pass: sets one explicitly-targeted shape's text, validated by reopening the saved output and reconfirming slide count and the new text, before being placed at the caller's output path). Exposed via two new CLI commands, `engine-inspect` and `engine-set-text`, operating on any explicit `--input` path. 21 new tests (37 total in the suite), all passing, using only synthetic fixtures built with `python-pptx`/`Pillow` — the 8 unavailable Git LFS assets were not touched, fabricated, or worked around. `python scripts/check_pipeline.py` still passes. This is the mechanical engine only: no semantic red-box detection, fuzzy shape matching, or documentation-intelligence work exists yet. See `docs/DECISIONS.md` (PROJ-007) for the full record.
- Safe PPT Engine Build 2 completed: four new visual mutation primitives in `lib/ppt_engine.py` — `move_shape` (left/top only, EMU), `resize_shape` (width/height only, EMU; a dedicated test confirms position is not touched), `set_shape_geometry` (left+top+width+height as one atomic operation for future red-box relocation — all four values validated before anything is staged, so an invalid value writes nothing and leaves no partial output anywhere), and `replace_picture` (rejects a non-picture target and a missing/empty/corrupt replacement image; preserves the existing shape's left/top/width/height frame by default regardless of the replacement image's own aspect ratio, and leaves any existing OOXML crop data untouched since only the picture's image relationship is repointed — the one OOXML-level operation used, isolated entirely inside this function). All four are validated by save→reopen→content-check before being placed at the caller's output path via a newly-hardened atomic-publish step (`_publish_atomically`, using a same-directory temp file + `os.replace`, retrofitted onto Build 1's `set_shape_text` too, since a bare `shutil.copy2` directly to the final output path was not itself atomic). Exposed via 4 new CLI commands (`engine-move-shape`, `engine-resize-shape`, `engine-set-geometry`, `engine-replace-image`). 35 new tests (72 total in the suite, all 37 Build 1 tests still passing unmodified), all using synthetic PPTX/PNG fixtures built with `python-pptx`/`Pillow` — the 8 unavailable Git LFS assets (including both production decks) were not touched. `python scripts/check_pipeline.py` still passes. This remains mechanical infrastructure only: no semantic shape targeting, fuzzy matching, or documentation-intelligence work exists yet. See `docs/DECISIONS.md` (PROJ-008) for the full record.
- Safe PPT Engine Audit Repair 1 completed: an independent Codex audit of Builds 1–2 found 8 defects (F-01–F-08; 1 critical, 2 high, 3 medium, 2 low). **Builds 1–2 must not be described as having passed independent safety audit** — they had not been audited when marked complete, and failed once audited. All 8 findings are fixed: F-01 (source-aliasing protection strengthened to `os.path.realpath` + `os.path.samefile`, applied to all 5 mutation primitives, no longer solely reliant on the post-publication hash check); F-02 (`overwrite=False` publication now uses `os.O_CREAT|os.O_EXCL` atomic exclusive creation, not a check-then-replace race; `overwrite=True` still uses atomic `os.replace`); F-03 (source is now validated through the typed `load_deck` boundary *before* hashing/staging in every mutation primitive, so a missing/empty/corrupt input raises `DeckSourceError`, not a bare `FileNotFoundError`; all 5 CLI mutation commands verified to exit 1 with a clean message and no traceback); F-04 (`replace_picture` now drops the old image relationship when nothing else on the slide still references it, verified against actual ZIP package contents — a shared image relationship, e.g. two pictures on one slide, is correctly left untouched); F-05 (`create_working_copy` is now exception-safe, and staging/save/validation/publication failure injection tests confirm no leaked `b4ps_engine_*`/`.b4ps_publish_*` temp files and no source/output corruption on any of those four failure points); F-06 (this recordkeeping — `openspec/changes/safe-ppt-engine/verification-report.md`); F-07 (`replace_picture`'s non-picture-target rejection now happens via a preflight resolve against the original input, before any working copy is staged); F-08 (`inspect_deck`'s canonical structural result no longer embeds the source path — two byte-identical `.pptx` files at different paths now inspect identically; a new `describe_deck` wraps the canonical structure with `source_path` as separate provenance metadata, used by `engine-inspect`'s CLI/JSON output). 48 new adversarial/fault-injection tests added (120 total in the suite); 2 pre-existing tests were updated with an explicit justification each (one error-message wording change from the F-01 fix, and CLI JSON-shape assertions updated for the F-08 `structure`/`source_path` split) — no test was weakened. `python scripts/check_pipeline.py` still passes; the canonical ZIP was not touched. Independent Codex re-audit of this repair was subsequently performed (Audit 2) and found 6 findings remaining — see the Audit Repair 2 entry below. See `docs/DECISIONS.md` (PROJ-009) for the full per-finding record.
- Safe PPT Engine Audit Repair 2 completed: an independent Codex re-audit of Audit Repair 1 (Audit 2) confirmed F-01, F-03, F-07, and F-08 correctly closed, and found 6 remaining findings — F-02, F-04, F-05, F-06, R-01, R-02 — all now fixed. F-02 (`overwrite=False` publication now writes a complete, fsynced same-directory temp file first, then publishes via an atomic `os.link` — never opens/streams into the final path directly, so the final path can never be observed holding a partial file; `overwrite=True` still uses atomic `os.replace`). R-01 (the source-integrity hash check now runs immediately before publication — the actual gate that prevents an unsafe publish — rather than only after; a defense-in-depth second check remains after publication). F-05 (a new `TransactionIOError` type normalizes filesystem/library I/O failures during working-copy creation, save, temp-output preparation, and publication into a typed error rather than a raw `OSError`; a genuine, previously-unwrapped `tempfile.mkstemp` failure inside `_publish_atomically` was found and fixed by a real, unmocked read-only-directory test). F-04 (`replace_picture`'s stale-relationship cleanup now scans the *entire* slide XML tree, not only `p:spTree`, so a relationship still used by the slide background — or any other non-shape slide element — is correctly preserved; verified with a background-sharing fixture that would have broken under the old spTree-only scan, plus a crop-preservation regression test). F-06 (added `openspec/changes/safe-ppt-engine/spec.md`, the canonical classification record `docs/TESTING.md` requires that was missing, and expanded `verification-report.md`/`audit-report.md` with builder review, founder-preview status — recorded truthfully as still pending, not fabricated as complete — profile-specific gates, and full audit history). R-02 (adversarial tests strengthened to prove mechanisms, not just end states: a call-count spy proves zero staging calls for a non-picture target; call-order assertions prove the integrity gate runs before publish for every mutation family; the F-03 missing/empty/corrupt/valid-ZIP-non-PPTX matrix now covers every mutation family including image replacement, at both the engine and CLI boundary). 65 new tests added (185 total in the suite); 2 pre-existing tests updated with explicit justification (both now assert the more specific `TransactionIOError` F-05 introduces, in place of the broader error type they previously accepted) — no test was weakened. `python scripts/check_pipeline.py` still passes; the canonical ZIP was not touched. Independent Codex re-audit of this repair was subsequently performed (Audit 3) and found 4 findings remaining — see the local Audit Repair 3 entry below. See `docs/DECISIONS.md` (PROJ-010) for the full per-finding record.
- Safe PPT Engine Audit Repair 3 fixed **locally only** (not committed, pushed, or opened as a PR — per explicit task instruction, left as working-tree changes for Codex to audit directly): an independent Codex re-audit of Audit Repair 2 (Audit 3, against merged commit `69bb4761921ac7f7b843beb04ab255a2b272865e`) confirmed F-01, F-02, F-03, F-04, F-07, F-08 correctly closed, and found 4 findings remaining — F-05, F-06, R-01, R-02 — all now fixed. F-05 (`create_working_copy` now wraps its `tempfile.mkstemp` call, not only the copy step, in typed-error handling; a new `_remove_reporting_failure` helper and `_staged_copy` context manager ensure a cleanup failure is never silently discarded — it is folded into the raised error's message when a primary failure is already in flight, printed as a stderr warning when it happens alongside another already-reported failure, or raised as a `TransactionIOError` in its own right when the operation otherwise succeeded and the leaked temp file is the only remaining problem). R-01 (the source-integrity check is now the single gate, run immediately before `_publish_atomically`; the previous defense-in-depth post-publication re-check was removed entirely, per explicit instruction — once publication succeeds, the transaction is successful; a source-deletion or unreadable-source error at the pre-publish check now raises a typed `DeckSourceError` distinct from the content-mismatch `SafeDeckError`). 19 new local regression tests (`tests/test_ppt_engine_audit_3.py`; 204 total in the suite, all 185 pre-existing tests passing unmodified) cover `mkstemp` failure (staging and publish), cleanup failure on both the primary-failure and operation-succeeded paths, source deletion/change exactly at the integrity boundary across every mutation family including image replacement, and call-order proof that the integrity check runs immediately before (and only before) publication. `python scripts/check_pipeline.py` still passes; the canonical ZIP was not touched. Governance records (`spec.md`, `verification-report.md`, `audit-report.md`) were also corrected this round: the retroactive classification is now explicitly flagged as a process deviation rather than presented as normal-order process; no approval is invented; the actual recorded CI result for the last pushed round (Audit Repair 2, PR #7 — `validate` passed) is now stated instead of the prior placeholder "expected to pass" language; the audit chronology now includes Audit 3; founder preview remains recorded as genuinely pending. Independent Codex re-audit of this local diff (Audit 4) was subsequently performed and found 3 findings remaining — see the local Audit Repair 4 entry below. See `docs/DECISIONS.md` (PROJ-011) for the full per-finding record.
- Safe PPT Engine Audit Repair 4 fixed **locally only** (not committed, pushed, or opened as a PR — per explicit task instruction, left as working-tree changes for Codex to audit directly): an independent Codex re-audit of Audit Repair 3's local, uncommitted diff (Audit 4 — the first round in this history to review a diff before it was pushed or merged) confirmed R-01 correctly closed, and found 3 findings remaining — F-05, F-06, R-02 — all now fixed. F-05 (`create_working_copy`'s bare `os.close(fd)` call is now wrapped: a close failure raises typed `TransactionIOError` instead of a raw `OSError`, and the temp file `mkstemp` had already created on disk is removed — via `_remove_reporting_failure` — before the error is raised. **This closed the temp-pathname leak only; whether the underlying file descriptor itself could still be left open after a close failure was not verified by this round** — that gap was found by Audit 5 and closed by Audit Repair 5, below). F-06 (governance records corrected to reflect the actual, observed workflow: Codex audits a local, uncommitted diff directly, as Audit 4 itself just did — every "cannot occur until pushed" statement about Audit 4/5 was removed and replaced with an accurate description; the Data-sensitive profile's two N/A gates in `verification-report.md` no longer say "approved," since no approving authority reviewed or signed off on those determinations — they are factual scope statements, not approvals). R-02 (`test_f05_staged_copy_cleanup_failure_on_primary_failure_does_not_mask_it` and `test_f05_publish_temp_cleanup_failure_reported_on_link_exists_race` now assert the actual stderr warning text via `capsys`, not just the raised exception's type, closing the gap between what their docstrings claimed was verified and what was actually asserted; a new `test_f05_create_working_copy_os_close_failure_typed_and_no_leak` regression covers the `os.close` failure itself, using a selective `os.close` patch that only fails for the specific fd `mkstemp` returned, confirming both the typed error and that no temp file is leaked). 1 new local regression test added (`tests/test_ppt_engine_audit_3.py`; 205 total in the suite, all 204 pre-existing tests still passing, with 2 of them gaining an additive `capsys` assertion rather than being weakened or replaced). `python scripts/check_pipeline.py` still passes; the canonical ZIP was not touched. Independent Codex re-audit of this local diff (Audit 5) was subsequently performed and found 3 findings remaining — see the local Audit Repair 5 entry below. See `docs/DECISIONS.md` (PROJ-012) for the full per-finding record.
- Safe PPT Engine Audit Repair 5 fixed **locally only** (not committed, pushed, or opened as a PR — per explicit task instruction, left as working-tree changes for Codex to audit directly): an independent Codex re-audit of Audit Repair 4's local, uncommitted diff (Audit 5) found 3 findings remaining, narrower than the round before — F-05, F-06, R-02 — all "fixed" at the time, though **Repair 5's own F-05 fix was subsequently found unsafe by Audit 6 and has since been replaced — see the Audit Repair 6 entry below; this bullet is kept as an accurate record of what Repair 5 actually did, not a claim that it was correct.** F-05 (`create_working_copy`'s close-failure path added an `os.fstat(fd)` check and a retry of `os.close(fd)` after a reported close failure). F-06 (`spec.md`'s Classification → Rationale line, which still said "two rounds of independent audit" from before this history grew past Audit 2, now correctly said "four completed rounds... with a fifth pending"; `docs/CURRENT.md`'s and `docs/DECISIONS.md`'s Repair 4 write-ups, which said or implied "no leaked staging resource remains" after Repair 4 — true only of the temp pathname, the one thing Repair 4 actually verified — were corrected to say so plainly). R-02 (`test_f05_create_working_copy_os_close_failure_typed_and_no_leak` was rewritten to assert a retry occurred and that `os.fstat(fd)` eventually raised — **this test has since been found insufficient by Audit 6, since it never proved the fd being retried was still safely the engine's own at the moment of the retry, and has been replaced**). 1 existing test strengthened in place (no new test added); 205 total in the suite, unchanged from Audit Repair 4. `python scripts/check_pipeline.py` still passed; the canonical ZIP was not touched. Independent Codex re-audit of this local diff (Audit 6) was subsequently performed and found this repair's F-05 fix itself unsafe, plus 2 further governance corrections — see the local Audit Repair 6 entry below. See `docs/DECISIONS.md` (PROJ-013) for the full per-finding record.
- Safe PPT Engine Audit Repair 6 fixed **locally only** (not committed, pushed, or opened as a PR — per explicit task instruction, left as working-tree changes for Codex to audit directly): an independent Codex re-audit of Audit Repair 5's local, uncommitted diff (Audit 6) found 3 findings, all concerning Repair 5's own F-05 fix and how it was described — all now fixed. F-05 (the `os.fstat(fd)`-then-retry logic Repair 5 added is removed entirely, not adjusted: once `os.close(fd)` reports an error, `create_working_copy` no longer touches that fd number again in any way — no `fstat`, no retried `close`. The reasoning is documented inline: even where POSIX guarantees the fd slot is deallocated regardless of the close error, something else in the process could already have been handed that exact fd number back by the time control returns to this function, and there is no way to safely distinguish "still ours" from "already reused" without racing that possibility. The temp pathname is still removed via `_remove_reporting_failure` and folded into the typed `TransactionIOError`, exactly as in Repair 4). F-06 (`audit-report.md`'s Audit 5 row no longer claims Audit 5 "confirmed" Repair 4's F-06/R-02 fixes closed — Audit 5's scope was Repair 4, and no round has independently re-examined those two fixes on their own since; a new Audit 6 row records **FAIL** with this round's findings, and the Audit 7 row records PASS WITH FINDINGS; `verification-report.md`, `spec.md`, and `docs/DECISIONS.md`'s PROJ-013 entry are corrected the same way — PROJ-013's F-05/R-02 bullets now carry explicit correction notes rather than being left to read as still-accurate). R-02 (the retry-mechanism test is replaced by two: one proving `create_working_copy` never calls `os.close`/`os.fstat` on the failed fd a second time at all, and an adversarial one that lets a real, unrelated file genuinely reuse that exact fd number after the close failure and proves the engine never touches it — confirmed to fail against the reinstated retry logic, proving both are genuine regressions). 1 test removed, 2 added (net +1); 206 total in the suite, all other pre-existing tests passing unmodified. `python scripts/check_pipeline.py` still passes; the canonical ZIP was not touched. Audit 7 completed with PASS WITH FINDINGS: F-05 is closed; F-06 and R-02 remained, fixed as local Audit Repair 7 (see `docs/DECISIONS.md` PROJ-015). **This Repair 6 diff, together with Repairs 3–5 and Repair 7, was subsequently committed and merged to `main` in a single change (commit `40f3a1083bf8556830abf9552fb186f629412c5c`, merge commit `be6df7fdfe8f6ab4cad24f4c3c3a7408945cb715`, PR #8) — none of it remains local-only.** Independent Codex audit of that integrated state (Audit 8) is next. See `docs/DECISIONS.md` (PROJ-014) for the full per-finding record.
- **Editorial Memory Slice 1 completed and merged.** Per the founder's Editorial Memory lifecycle clarification (living/evolving/version-aware/provenance-preserving/history-preserving knowledge layer) and the accepted design in `docs/EDITORIAL_MEMORY_IMPLEMENTATION_AUDIT.md` (three-entity model — `Evidence`/`KnowledgeItem`/`KnowledgeState` — plus six founder-approved amendments, committed here for the first time as part of this change since it had previously existed only as local, uncommitted design work), the smallest real implementation proving the lifecycle end-to-end now exists at `editorial-memory/`. Core invariant enforced in code, not only documentation: **Evidence never promotes itself into Knowledge** — `propose_state` rejects any `KnowledgeState` with no evidence references (`MissingProvenanceError`) or an unknown evidence reference (`UnknownEvidenceError`), and only an explicit `approve_state` call can make a state `current`. Supersession preserves history (a newly-approved state moves the prior current state to `superseded`, never deleted); invalidation (`invalidate_state`) is a distinct concept for knowledge that was never trustworthy, with no requirement that a replacement exist; an unresolved contradiction (`relation_to_previous=contradicts` on a still-`proposed` state) is visible via `has_pending_conflict`/`get_conflicts` without silently overwriting the existing current state. Evidence quality (`high`/`medium`/`low`) and per-state evidence-type-diversity metadata are preserved without any numeric scoring or ranking, per explicit instruction. Persistence is plain, atomically-written, git-diffable JSON files under a caller-chosen directory (`editorial-memory/lib/store.py`) — no new dependency, no database, no vector/graph store, no embeddings. 26 new tests (`editorial-memory/tests/test_editorial_memory.py`), including one deterministic 15-step end-to-end scenario proving the full evidence→item→state→approval→supersession→unresolved-conflict lifecycle in a single run. `python scripts/check_pipeline.py` still passes; Safe PPT Engine was not touched. This is Slice 1 only — no Documentation Intelligence, no CLI, no browser-observation producer, no purge implementation, and no consumer of this API exist yet; Slice 2 has explicitly not begun. See `docs/DECISIONS.md` (PROJ-016) for the full record.
- **Editorial Memory Repair 1 fixed locally only** (not committed, pushed, or opened as a PR — per explicit task instruction, left as working-tree changes for Codex to audit directly): an independent Codex audit of merged Slice 1 (Audit 1, against `93e87239ea9b55185f374f1a3aa2a390be03c24f`) found 3 findings — EM-01, EM-02, EM-03 — plus a related JSON-corruption-normalization gap, all now fixed. EM-01 (`get_current()` now re-checks that every evidence id a current state cites still resolves, raising typed `CorruptProvenanceError` rather than returning a state whose provenance can no longer be verified). EM-02 (`get_or_create_knowledge_item` now detects when a new natural key collides, via lossy slugification, with an existing item under a *different* key — e.g. `"a.b"` vs `"a-b"` — and raises typed `KeyCollisionError` instead of silently merging two distinct subjects' identity). EM-03 (`feature_area` is now validated as a safe `<platform>.<feature>` path segment before ever being used as one, raising typed `InvalidFeatureAreaError` on anything resembling `/`, `\`, or `..`, plus a defense-in-depth containment check on the resolved path). JSON-corruption normalization (`load_evidence`/`load_knowledge_item` now raise typed `StorageCorruptionError` instead of a raw `json.JSONDecodeError`). 23 new adversarial tests added (`editorial-memory/tests/test_editorial_memory.py`; 49 total in the suite, all 26 pre-existing Slice 1 tests passing unmodified) — two of the EM-01 tests were confirmed to fail against the pre-repair `get_current()` logic specifically, and the whole new test file fails to even import against the unmodified pre-repair code, both used as proof these are genuine regressions. `python scripts/check_pipeline.py` and `git diff --check` both pass; Safe PPT Engine was not touched; no Slice 2 work was started. See `docs/DECISIONS.md` (PROJ-017) for the full per-finding record.
- **Editorial Memory Repair 2 fixed locally only** (not committed, pushed, or opened as a PR — per explicit task instruction, left as working-tree changes for Codex to audit directly): an independent Codex re-audit of Repair 1's local diff (Audit 2) returned **PASS WITH FINDINGS** — EM-01, EM-02, and EM-03 confirmed closed, and one new finding, EM-04, raised (the analogous unvalidated-path pattern in `evidence_id` handling that Repair 1's own verification report had already flagged as a known, narrower, deliberately-unfixed limitation). EM-04 is now fixed: `store.validate_evidence_id()` allowlists `evidence_id` to a plain lowercase-alphanumerics/hyphens token (matching what `record_evidence()` itself generates) before `_evidence_path()` ever touches disk, raising typed `InvalidEvidenceIdError` on rejection, plus the same defense-in-depth resolved-path containment check EM-03 already used. A persisted, tampered `evidence_refs` entry now surfaces via `get_current()` as `CorruptProvenanceError` chaining `InvalidEvidenceIdError`, with no change needed to `memory.py` since the existing EM-01 check already wraps evidence lookups generically. 10 new adversarial tests added (`editorial-memory/tests/test_editorial_memory.py`; 59 total in the suite, all 49 prior tests passing unmodified) — 9 of the 10 new tests were confirmed to fail against the pre-repair `_evidence_path()` specifically, used as proof these are genuine regressions. `python scripts/check_pipeline.py`, compile checks, and `git diff --check` all pass; Safe PPT Engine was not touched; no Slice 2 work was started. See `docs/DECISIONS.md` (PROJ-018) for the full per-finding record.

## Registered skill sources

- `phuryn/pm-skills`
- `mattpocock/skills`
- `coreyhaines31/marketingskills`

See `docs/SKILLS.md` for Bridge4PS-specific availability and safety boundaries.

## Not included / not started

- Detailed Architecture v0.1 specification (not yet migrated into GitHub — do not reconstruct or invent it)
- Resolved binary content for the 8 omitted Git LFS pointer entries listed above (pointer text preserved in the canonical ZIP; actual binaries not present anywhere in this repository) — untouched by Batch 1, Safe PPT Engine Build 1, and Safe PPT Engine Build 2
- Semantic/AI capability on top of the engine: red-box identification, fuzzy shape matching, screenshot intelligence, automatic screenshot replacement, automatic affected-slide detection
- Documentation Intelligence, Human Review (Editorial Memory Slice 1 is completed — see Completed above; a `browser_observation` producer, a CLI, purge tooling, and any consumer of the Editorial Memory API remain not started, and Slice 2 has explicitly not begun)
- Ontology work, HTML migration, browser exploration, future semantic versions, Freshdesk automation, AppSheet automation, video automation
- Rotation/flip and any geometry attribute beyond left/top/width/height; shape deletion/creation; text formatting beyond replacing text content; multi-shape/multi-slide batch mutation in one call
- Hardening of `lib/plan.py`, `lib/match.py`, `lib/anchors.py`, `lib/layout.py`, `lib/geometry.py` beyond what Batch 1 touched (no defects requiring a fix were found in scope during Batch 1's inspection, but they were not exhaustively re-audited line by line, and neither Safe PPT Engine build uses or replaces them)

## Current blocker

None for Repository Hardening Batch 1, either Safe PPT Engine build, the seven Safe PPT Engine audit rounds completed so far, or Editorial Memory Slice 1. The 8 Git LFS pointer entries remain an unresolved baseline availability limitation (unchanged by any of them): their actual binary content is not present anywhere recovered, and was not fabricated. None needed the real decks — all engine work was validated entirely against synthetic fixtures. Open items are procedural, not defects: (1) Safe PPT Engine Audit 7 completed with PASS WITH FINDINGS; F-05 is closed and F-06/R-02 remained, fixed as local Audit Repair 7, which — together with Repairs 3 through 6 — is now integrated into `main` (commit `40f3a1083bf8556830abf9552fb186f629412c5c`, merge commit `be6df7fdfe8f6ab4cad24f4c3c3a7408945cb715`, PR #8); independent Codex audit of that integrated state (Audit 8) has not yet occurred; (2) Editorial Memory Slice 1 was independently audited (Audit 1), which found EM-01/EM-02/EM-03 (plus a JSON-corruption-normalization gap), fixed locally as Repair 1; Repair 1 was independently re-audited (Audit 2), which returned PASS WITH FINDINGS — EM-01/EM-02/EM-03 confirmed closed, EM-04 found — fixed locally as Repair 2; none of this is yet committed, pushed, or merged, so `main` still reflects the original merged Slice 1 state; independent Codex re-audit of the local Repair 2 diff (Audit 3) has not yet occurred, and is required before Slice 2 begins; (3) founder/human preview of a generated `.pptx` in real PowerPoint has not yet happened across any Safe PPT Engine round (see `openspec/changes/safe-ppt-engine/verification-report.md` → "Founder-preview requirement").

## Exact next action

Two independent audit items are now outstanding, on separate tracks: (1) independent Codex audit of the integrated Safe PPT Engine state on `main` (`be6df7fdfe8f6ab4cad24f4c3c3a7408945cb715`, which includes Audit Repair 7 — Audit 8) — not addressed by this change; (2) independent Codex audit of the local, uncommitted Editorial Memory Repair 2 diff (Audit 3) — the required next action before Editorial Memory Slice 2 begins, per explicit instruction not to begin Slice 2 as part of this change; it does not require these changes to be pushed first. Neither action is a further Claude implementation milestone, and neither is a commit/push/PR/merge. Documentation Intelligence (the next milestone in the approved sequence: Raw Baseline → Repository Hardening → Safe PPT Engine → Editorial Memory → Documentation Intelligence → Human Review) remains not started and requires separate founder approval before starting, and should not begin before Editorial Memory clears independent audit.
