# Bridge4PS Documentation Engineer — Architecture

## Status

Architecture v0.1 is founder-approved and locked.

## Detailed specification

The detailed Architecture v0.1 specification has not yet been migrated into GitHub. Do not reconstruct, infer, or invent it from other sources — it does not exist in this repository yet.

## Canonical raw baseline

`B4PS-TUT-main.zip` is the designated canonical raw baseline for the implementation (SHA-256 `00f5e41f475b8205535481da11f73c4e4f0bd614e0d3b1efff938e921b9eb6ee`). The canonical ZIP is the complete immutable raw baseline, committed unmodified at `baseline/source-artifacts/B4PS-TUT-main.zip`. The GitHub materialization contains 50 of 58 source files byte-for-byte at `baseline/B4PS-TUT-main/`. Eight unresolved Git LFS pointer entries are intentionally omitted from the extracted tree because their referenced objects are unavailable and GitHub rejects those unresolved references; their exact source representations remain preserved in the canonical ZIP. See `docs/CURRENT.md` for the full manifest and the exact next permitted action.

## What this repository does not contain yet

- Application code
- The detailed v0.1 architecture specification
- Resolved binary content for the 8 omitted Git LFS pointer entries (see `docs/CURRENT.md`)
